---
context: default
phase: null
task: null
total_tasks: 0
status: paused_between_milestones
last_updated: 2026-09-18T14:59:23.930Z
resume_target: monday
---

# Continue Here — GeradorExercicios

**Paused:** Friday 2026-09-18 (resume Monday)  
**Position:** v2.0 shipped — awaiting next milestone  
**No active phase** — phases archived

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Resume into execute-phase | No open phase/plan; phases 11–12 are under milestones/ | blocking | Start with `$gsd-new-milestone`, never `$gsd-execute-phase` until a new roadmap exists |
| Treat demo/ as production package | `demo/` is throwaway; real host needs PKG-01 | advisory | Keep packaging as first roadmap phase; do not invent pyproject inside demo |
| Widen `generate_batch` return | v2.0 locked `-> ExerciseBatch`; OBS-01 was deferred | advisory | New milestone may add a parallel API — do not break the v2.0 contract silently |

**Do not proceed past new-milestone until the blocking row is acknowledged.**

<current_state>
v2.0 **Embed em Produção** is complete and archived (2026-09-18).
- Tag `v2.0` exists locally (`refs/tags/v2.0`)
- Branch `gsd/ship-phase-03-tests-logging-docs` is ahead of origin by ~19 commits
- `.planning/REQUIREMENTS.md` was deleted on purpose (fresh for next milestone)
- `.planning/phases/` is empty; artifacts live in `.planning/milestones/v2.0-phases/`
- Suite baseline: 155 package tests + 17 demo tests (no live LLM)
</current_state>

<completed_work>
This session (through Friday close):
- Phase 12 Local Embed Demo executed + UAT 3/3 pass
- `$gsd-audit-milestone` → status **passed**
- `$gsd-complete-milestone` → archives + ROADMAP collapse + PROJECT evolution + tag
- `$gsd-cleanup` → already satisfied (phases archived by `milestone.complete`)
</completed_work>

<remaining_work>
- Start **next milestone** (not resume Phase 12)
- Lead requirement: **PKG-01** — `pyproject.toml` + rename `exercise_ai/`
- Candidates: OBS-01 (usage alongside batch), LOG-01 (print→logging), SEED-003 B/C later
- Optional human: push tag/branch; decide keep vs delete `demo/`
</remaining_work>

<decisions_made>
- Keep `generate_batch(...) -> ExerciseBatch` (do not widen in v2.0)
- PKG-01 is lead of next milestone (real embed pre-condition)
- Demo = stdlib `[::1]:8642`, Lock→409, outside CI
- Closeout acknowledged dormant SEED-005 / SEED-006
</decisions_made>

<blockers>
- PKG-01 still blocks real in-process host embed (`sys.path.insert` empirically broken) — expected; next milestone work
- Tag/remote push not done — non-blocking
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine state for `$gsd-resume-work`
2. `.planning/PROJECT.md` — Current State + Next Milestone Goals (PKG-01)
3. `.planning/ROADMAP.md` — collapsed post-v2.0; Next Milestone section
4. `.planning/MILESTONES.md` — v2.0 entry (closeout + archives links)
5. `.planning/milestones/v2.0-REQUIREMENTS.md` — deferred PKG/OBS/LOG wording
6. `.planning/milestones/v2.0-MILESTONE-AUDIT.md` — what passed / tech debt accepted

## Infrastructure State
- No demo server expected running
- Git: local tag `v2.0`; remote not updated for tag
- Untracked noise OK to ignore Monday: `.cursor/`, `.serena/`, `skills/`, `token-usage-logs/`
- Working tree may still show `.planning/config.json` / `.gsd-allow-shrink` / deleted `HANDOFF.json` before this pause commit

## Pre-Execution Critique Required
- N/A — not paused mid-design; next step is milestone definition, not execute

<context>
Ship gate for embed seam + throwaway demo is done. Monday is a clean milestone boundary: define version/name/requirements around packaging so a host can install the package instead of path-hacking. Do not reopen Phase 12 plans.
</context>

<next_action>
Monday: `$gsd-resume-work` → then `$gsd-new-milestone` with lead **PKG-01** (pyproject + rename `exercise_ai/`). Optionally note OBS-01 / LOG-01 as follow-ons.
</next_action>
