---
phase: 13-request-schema-plan-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/models.py
  - exercise-ai/tests/test_models.py
  - exercise-ai/tests/conftest.py
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02
  - BATCH-03
  - BATCH-04
  - CAP-01
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Uniform legacy request (dificuldade + quantidade, no plano/itens) still validates and normalizes to dificuldades of length 1 (D-05, D-06; BATCH-04)"
    - "Mixed request accepts plano XOR itens; plano expands fácil→médio→difícil into ordered specs; quantidade must equal plan length or ValidationError before any LLM call (D-01, D-02, D-03; BATCH-01, BATCH-02)"
    - "Every Exercise requires dificuldade; every ExerciseBatch requires dificuldades summarising bands used (D-04, D-09, D-10; BATCH-03)"
    - "MAX_QUANTIDADE remains 40 on GenerationRequest.quantidade and is unchanged in models (CAP-01)"
    - "generate_batch(...) -> ExerciseBatch signature unchanged; no prompt/CLI/wizard/RELY redesign in this phase"
  artifacts:
    - path: "exercise-ai/models.py"
      provides: "ExerciseSpec, plano/itens/dificuldades on GenerationRequest, echo fields, normalize + expand + echo-adherence helpers"
      contains: "ExerciseSpec|plano|itens|dificuldades|MAX_QUANTIDADE"
    - path: "exercise-ai/tests/test_models.py"
      provides: "Offline tracer + invariant coverage for uniform compat, mixed plano, XOR, qty mismatch, equal-split, echo mismatch"
      contains: "plano|itens|dificuldades|ValidationError"
    - path: "exercise-ai/tests/conftest.py"
      provides: "Factories supply required echo fields so existing suite stays green on uniform path"
      contains: "dificuldade"
  key_links:
    - from: "GenerationRequest (plano|itens|dificuldade)"
      to: "normalized dificuldades + ordered ExerciseSpec list"
      via: "model_validator / pure expand helper (D-01..D-08)"
      pattern: "model_validator|expand|itens"
    - from: "Exercise / ExerciseBatch echo fields"
      to: "verify_plan_echo (pure)"
      via: "mismatch raises; no silent correction (D-09..D-11)"
      pattern: "verify_plan_echo|dificuldade"
    - from: "service.generate_batch"
      to: "ExerciseBatch"
      via: "signature preserved; schema only this phase"
      pattern: "generate_batch"
  prohibitions:
    - "Do not rewrite prompts.py or add mixed prompt branches (PROMPT-01 / Phase 14)"
    - "Do not wire plan-adherence into validate_exercise_batch / reliability RELY loop (VAL-01, VAL-02 / Phase 14)"
    - "Do not change CLI argparse, wizard, or demo UX (UX-01, UX-02 / Phase 15)"
    - "Do not raise MAX_QUANTIDADE above 40 (CAP-02 deferred)"
    - "Do not add tipo_raciocinio / TIPO-OPEN fields"
    - "Do not widen generate_batch return type or add a second public generate API"
---

<objective>
Deliver the typed request/response plan contract for uniform and mixed batches: `plano` and/or `itens`, `dificuldades`, count invariants, and required difficulty echo fields — without touching prompts, validator RELY loop, or CLI/wizard.

Purpose: Freeze the embed-facing schema so Phase 14 can consume ordered specs + echoes, and Phase 15 can author `plano`/`itens` against the same models.
Output: Extended `models.py`, offline `test_models.py` coverage, conftest factories updated for required echoes; suite green on uniform path.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-request-schema-plan-contract/13-CONTEXT.md
@.planning/research/SUMMARY.md
@exercise-ai/models.py
@exercise-ai/service.py
@exercise-ai/validator.py
@exercise-ai/tests/test_models.py
@exercise-ai/tests/conftest.py
</context>

<proven_verify_commands>
**Reuse from repo root (CI / README):**

- Targeted models: `pytest exercise-ai/tests/test_models.py -q`
- Canonical suite: `pytest exercise-ai -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Spec model name | `ExerciseSpec` with field `dificuldade: DificuldadeEnum` | Matches research SUMMARY; PT fields already on request (`plano`, `itens`, `dificuldades`) |
| `plano` shape | Nested model `PlanoDificuldade` with `facil`/`medio`/`dificil` ints `ge=0` (default 0) | Explicit bands; sum drives qty invariant |
| Expansion home | Pure helpers on/near `GenerationRequest` in `models.py` (no new module unless file grows unwieldy) | YAGNI; single schema boundary |
| Scalar `dificuldade` after normalize | Optional on input; after validate always set to first band of `dificuldades` when missing so existing `prompts.build_prompts` keeps working until Phase 14 | Compat without prompt rewrite |
| Internal ordered specs | After validate, request exposes ordered specs (materialized `itens` or read-only property `itens_ordenados`) for Phase 14 | Single source of truth post-normalize |
| D-11 surface this phase | Pure `verify_plan_echo(batch, request) -> None` raises `ValueError` on slot mismatch; **not** called from `validator.py` yet | Honors fail-not-correct (D-11) without inventing VAL-01 pipeline wiring |

## Source audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| GOAL | Typed mixed/uniform plan contract; embed signature intact | Tasks 1–2 |
| REQ BATCH-01 | plano/itens per-item specs | Task 1 tracer + Task 2 |
| REQ BATCH-02 | quantidade ↔ plan length | Task 1 + Task 2 |
| REQ BATCH-03 | Exercise echoes dificuldade | Task 1 |
| REQ BATCH-04 | Uniform compat | Task 1 tracer |
| REQ CAP-01 | Cap stays 40 | Task 2 + truths |
| CONTEXT D-01..D-11 | All locked decisions | Tasks cite D-IDs; costly flagged |
| RESEARCH | Schema-first; no new deps; avoid qty drift | models-only; invariants |
| Deferred | TIPO-OPEN, CAP-02, PROMPT/VAL/UX | Prohibitions |

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: uniform compat + one mixed plano path with echo fields</name>
  <reversibility rating="costly">D-01 shapes CLI/wizard/host/validators long-term; D-09/D-10 required echo fields freeze Structured Outputs JSON. Flag costly — not one-way (no DB migration); no checkpoint:decision required. Do not silently weaken echo requirements.</reversibility>
  <files>exercise-ai/models.py, exercise-ai/tests/test_models.py, exercise-ai/tests/conftest.py</files>
  <read_first>exercise-ai/models.py, .planning/phases/13-request-schema-plan-contract/13-CONTEXT.md (D-01..D-11), .planning/REQUIREMENTS.md (BATCH-01..04, CAP-01), exercise-ai/tests/conftest.py, exercise-ai/tests/test_models.py</read_first>
  <behavior>
    - Uniform: `GenerationRequest(topico=..., dificuldade="medio", quantidade=3)` validates; normalized `dificuldades == [DificuldadeEnum.MEDIO]` (D-05, D-06; BATCH-04)
    - Mixed plano: e.g. `plano={facil:1, medio:1, dificil:0}, quantidade=2` expands to ordered specs `[facil, medio]` (D-01, D-02); `dificuldades == [facil, medio]` (D-04)
    - `Exercise(..., dificuldade=...)` and `ExerciseBatch(..., dificuldades=...)` construct successfully (D-09, D-10; BATCH-03)
    - `verify_plan_echo` passes when each exercise.dificuldade matches ordered slot; fails with ValueError when mismatched — no auto-correct (D-11)
    - Existing qty bound tests still pass; `MAX_QUANTIDADE == 40` (CAP-01)
  </behavior>
  <action>
    Extend `exercise-ai/models.py` only (schema + pure helpers):

    1. Add `ExerciseSpec(BaseModel)` with `dificuldade: DificuldadeEnum`.
    2. Add `PlanoDificuldade` with `facil`/`medio`/`dificil: int = Field(0, ge=0)`.
    3. On `GenerationRequest`: keep `materia`/`topico`/`quantidade` (`ge=1, le=MAX_QUANTIDADE`); make `dificuldade` optional (`None` default); add optional `dificuldades: list[DificuldadeEnum] | None`, `plano: PlanoDificuldade | None`, `itens: list[ExerciseSpec] | None`.
    4. `@model_validator(mode="after")` (or equivalent) implementing:
       - **D-03:** if both `plano` and `itens` → `ValidationError`.
       - **D-05/D-01:** if `plano` or `itens` → mixed; else uniform.
       - **D-02:** expand `plano` to ordered `ExerciseSpec` list fácil→médio→difícil (all of band A, then B, then C).
       - **D-06:** uniform with scalar `dificuldade` → set `dificuldades` to that single band.
       - **BATCH-02:** when mixed via `plano`/`itens`, require `quantidade == len(expanded specs)` else `ValidationError` (fail before LLM).
       - After normalize: ensure `dificuldades` is the sorted-unique band summary (fácil→médio→difícil among bands present, length 1–3) per **D-04**; if scalar `dificuldade` missing, set it to the first entry of `dificuldades` for prompt compat.
       - Materialize ordered specs onto the request (assign `itens` after expand, or expose a stable property — pick one and use it consistently in tests).
    5. On `Exercise`: add required `dificuldade: DificuldadeEnum` (**D-09**). On `ExerciseBatch`: add required `dificuldades: list[DificuldadeEnum]` (**D-10**).
    6. Add pure `verify_plan_echo(batch: ExerciseBatch, request: GenerationRequest) -> None` that compares each `batch.exercicios[i].dificuldade` to ordered slot spec; on mismatch raise `ValueError` (typed failure for host once Phase 14 wires it); never mutate batch (**D-11**). Do **not** import or call this from `validator.py` / `reliability.py` this phase.
    7. Keep `MAX_QUANTIDADE = 40` unchanged (**CAP-01**). Do not change `service.generate_batch` signature.

    Tests (write/adjust first where practical — tracer TDD):
    - Extend `test_models.py` with the three happy paths above + one echo mismatch failure for `verify_plan_echo`.
    - Update `conftest.py` `make_exercise` / `make_batch` to supply `dificuldade` (and batch `dificuldades`) so imports constructing `Exercise` keep working for the rest of the suite.

    Do **not** implement D-07/D-08 equal-split or full XOR/qty negative matrix in this task (Task 2).
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_models.py -q</automated>
  </verify>
  <done>Uniform + one mixed plano path normalize correctly; echo fields required; verify_plan_echo fails on mismatch; test_models tracer green.</done>
</task>

<task type="auto" tdd="true">
  <name>Complete invariants: XOR, qty drift, equal-split, CAP-01, suite green</name>
  <reversibility rating="costly">D-01/D-11 remain costly contract locks for Phases 14–15; equal-split heuristic (D-07/D-08) is reversible via tests but shapes host expectations — flag costly, no one-way checkpoint.</reversibility>
  <files>exercise-ai/models.py, exercise-ai/tests/test_models.py, exercise-ai/tests/conftest.py</files>
  <read_first>exercise-ai/models.py (validators after Task 1), .planning/phases/13-request-schema-plan-contract/13-CONTEXT.md (D-03, D-07, D-08, D-11), exercise-ai/tests/test_validator.py (make_exercise usage), exercise-ai/tests/test_service.py</read_first>
  <behavior>
    - Both `plano` and `itens` → ValidationError (D-03)
    - `quantidade != len(itens)` or `quantidade != sum(plano bands)` → ValidationError (BATCH-02)
    - `dificuldades` with 2+ bands and neither plano nor itens → equal split of `quantidade` across those bands; remainder to last band in fácil→médio→difícil order (D-07, D-08) — e.g. qty 5, bands facil+medio → 2 fáceis + 3 médios
    - Explicit `itens` path accepted and ordered as given (BATCH-01)
    - `quantidade` 0 / 41 still rejected; 1 and 40 accepted (CAP-01)
    - Full offline suite green; uniform callers unchanged in behavior
  </behavior>
  <action>
    Finish remaining `GenerationRequest` invariants in `models.py` per locked decisions:
    - **D-03 / BATCH-01:** reject plano∧itens; accept `itens`-only mixed path; accept `plano`-only.
    - **BATCH-02:** reject all quantidade↔plan length mismatches early (Pydantic `ValidationError`).
    - **D-07 / D-08:** when `dificuldades` has 2+ bands and no plano/itens, build ordered specs by splitting `quantidade` as evenly as possible; assign remainder to the **last** band in fácil→médio→difícil among the listed bands. Reject empty `dificuldades` / invalid combos with clear ValidationError messages.
    - Confirm empty plano (all zeros) with quantidade&gt;0 fails BATCH-02.
    - Expand `test_models.py` with focused negatives (XOR, qty drift, equal-split examples) and keep prior tracer tests.
    - Grep/fix any remaining `Exercise(` / `ExerciseBatch(` constructions that omit new required fields (conftest should cover most; fix stragglers in test files only if suite fails — prefer fixture defaults over drive-by refactors).
    - **Do not** touch `prompts.py`, `validator.py` adherence loop, `reliability.py`, `main.py`, `wizard.py`, or demo.

    Run full suite to prove success criterion 5 (uniform path / offline green).
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
  </verify>
  <done>All D-01..D-11 schema/normalize/echo contracts covered by offline tests; CAP-01 held; suite green; Phase 14/15 surfaces untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → GenerationRequest | Untrusted JSON/dict; Pydantic must reject illegal plano/itens/qty before LLM |
| LLM → ExerciseBatch | Model may omit/mismatch echo fields; schema requires fields; adherence check is pure until Phase 14 wires RELY |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering / Integrity | quantidade vs plan | medium | mitigate | BATCH-02 model_validator rejects drift before LLM |
| T-13-02 | Tampering | slot vs echo dificuldade | medium | mitigate | Required echoes + verify_plan_echo fail-closed (D-11); Phase 14 wires into RELY |
| T-13-03 | Denial of service | unbounded qty | low | mitigate | CAP-01 keep MAX_QUANTIDADE=40 |
| T-13-SC | Tampering | supply chain | low | accept | No new packages |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_models.py -q` passes after Task 1
- `pytest exercise-ai -q` passes after Task 2
- `rg "MAX_QUANTIDADE\\s*=\\s*40" exercise-ai/models.py` matches
- `rg "def generate_batch" exercise-ai/service.py` still shows `-> ExerciseBatch`
- No edits under `prompts.py` / wizard plan UX / CLI `--plano`
</verification>

<success_criteria>
- Host can build uniform or mixed (`plano` XOR `itens`) requests that normalize to ordered specs + `dificuldades`
- Quantidade/plan mismatches fail at Pydantic boundary
- Exercise/ExerciseBatch carry required difficulty echoes; pure echo check fails on mismatch
- Cap remains 40; `generate_batch` signature unchanged; offline suite green
</success_criteria>

<output>
Create `.planning/phases/13-request-schema-plan-contract/13-01-SUMMARY.md` when done
</output>
