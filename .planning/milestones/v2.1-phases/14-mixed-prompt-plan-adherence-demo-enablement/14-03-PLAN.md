---
phase: 14-mixed-prompt-plan-adherence-demo-enablement
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - exercise-ai/models.py
  - exercise-ai/prompts.py
  - exercise-ai/tests/test_models.py
  - exercise-ai/tests/test_prompts.py
  - exercise-ai/tests/test_reliability.py
autonomous: true
gap_closure: true
gap_ids:
  - G-14-4
requirements:
  - VAL-01
  - DEMO-01
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "ExerciseBatch.dificuldades Field description states unique bands used (1–3), order fácil→médio→difícil, NOT one entry per exercise"
    - "User prompt includes one short cue that batch dificuldades is the distinct-band plan summary (SEED-008: no field-schema dump)"
    - "verify_plan_echo still exact-compares batch.dificuldades == request.dificuldades; no silent rewrite of LLM summary before verify (D-07 / D-11 spirit)"
    - "Offline: duplicated summary (e.g. ['medio','medio'] vs request ['medio']) fails verify_plan_echo; prompt test asserts the new cue string"
  artifacts:
    - path: "exercise-ai/models.py"
      provides: "Stronger ExerciseBatch.dificuldades Field description"
      contains: "dificuldades|únicas|resumo|1–3|facil"
    - path: "exercise-ai/prompts.py"
      provides: "One-line batch dificuldades summary cue in user prompt"
      contains: "dificuldades|distintas|resumo|faixas"
    - path: "exercise-ai/tests/test_prompts.py"
      provides: "Asserts summary-shape cue present; still no enunciado/resposta/explicacao field dump"
      contains: "dificuldades|resumo|enunciado"
    - path: "exercise-ai/tests/test_models.py"
      provides: "Duplicated-padding summary fails verify_plan_echo (G-14-4 failure mode)"
      contains: "resumo dificuldades|medio"
  key_links:
    - from: "Structured Outputs Field description + prompt cue"
      to: "LLM ExerciseBatch.dificuldades shape"
      via: "response_format=ExerciseBatch + USER_PROMPT_TEMPLATE"
      pattern: "dificuldades"
    - from: "batch.dificuldades"
      to: "request.dificuldades"
      via: "verify_plan_echo exact equality (unchanged compare)"
      pattern: "resumo dificuldades diverge"
  prohibitions:
    - "Do not silently rewrite / canonicalize batch.dificuldades before verify_plan_echo"
    - "Do not dump full response field schemas into the prompt (SEED-008 / D-03)"
    - "Do not redesign RELY bounds, math_check, or move echo into validator.py"
    - "Do not weaken per-slot echo checks or exact summary equality for real mismatches"
---

<objective>
Close UAT blocker G-14-4: uniform live batches fail when the LLM echoes `dificuldades` as one entry per exercise (`['medio','medio']`) instead of the unique-band plan summary (`['medio']`), exhausting RELY with `resumo dificuldades diverge`.

Purpose: Teach the model the summary shape via schema Field description + one prompt cue, while keeping fail-closed exact compare for real summary/slot mismatches (D-07).
Output: Updated `models.py` Field + `prompts.py` cue; offline pytest covering duplicated-summary failure and cue presence.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-UAT.md
@.planning/seeds/SEED-008-response-structure-over-prompt.md
@exercise-ai/models.py
@exercise-ai/prompts.py
@exercise-ai/tests/test_models.py
@exercise-ai/tests/test_prompts.py
@exercise-ai/tests/test_reliability.py
</context>

<proven_verify_commands>
**Reuse from repo root (cwd = `exercise-ai` or with path prefix):**

- Targeted: `pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_prompts.py -q`
- Optional RELY doc fixture: `pytest exercise-ai/tests/test_reliability.py -q -k dificuldades`
- Full library smoke: `pytest exercise-ai -q`
</proven_verify_commands>

## Gap linkage

| Gap | Truth | Root cause (14-UAT) | Closure approach |
|-----|-------|---------------------|------------------|
| G-14-4 | Uniform single-band POST succeeds; `ExerciseBatch.dificuldades` echoes request summary (e.g. `['medio']`), not per-exercise repetition | Field description weak; prompt silent on summary shape; `verify_plan_echo` correctly rejects `['medio','medio']` → RELY exhausts | Strengthen Field + one prompt cue; keep exact compare (no silent rewrite) |

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Compare semantics | Keep `batch.dificuldades != request.dificuldades` exact equality | D-07; fail-closed; prefer prompt/schema over canonicalize |
| Prompt change | One short line under Requisitos (or adjacent) — not a field-schema dump | SEED-008 / D-03 |
| Field description | Unique bands present, order fácil→médio→difícil, length 1–3, not N copies | Structured Outputs surface; complements prompt |
| Canonicalize-before-verify | Out of scope unless PRODUCT later amends CONTEXT | User brief: prefer prompt/schema first |

## Source audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| UAT G-14-4 | Uniform live `['medio','medio']` → validation_exhausted | Tasks 1–2 |
| CONTEXT D-07 | Batch summary must match request; fail-closed | Task 1 (no rewrite) |
| CONTEXT D-03 / SEED-008 | No response field-contract prose dump | Task 1 prompt cue only |
| REQ VAL-01 | Plan adherence (summary + slots) | Task 1–2 |
| REQ DEMO-01 | Uniform legacy path must work live | Cue enables DEMO-01 Test 4 |
| Deferred | Silent canonicalize of padded summary | Prohibitions |

## Artifacts this phase produces

| Symbol / artifact | Kind | Location |
|-------------------|------|----------|
| Stronger `dificuldades` Field description | schema text | `exercise-ai/models.py` → `ExerciseBatch` |
| Batch-summary cue line | prompt text | `exercise-ai/prompts.py` → `USER_PROMPT_TEMPLATE` |
| Duplicated-summary echo test | unit test | `exercise-ai/tests/test_models.py` |
| Prompt cue assertion | unit test | `exercise-ai/tests/test_prompts.py` |
| Optional RELY exhaustion fixture | unit test | `exercise-ai/tests/test_reliability.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Schema + prompt: unique-band dificuldades summary shape (G-14-4)</name>
  <reversibility rating="reversible">Prompt/Field text only; verify_plan_echo compare unchanged.</reversibility>
  <files>exercise-ai/models.py, exercise-ai/prompts.py, exercise-ai/tests/test_models.py, exercise-ai/tests/test_prompts.py</files>
  <read_first>
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-UAT.md (Gaps G-14-4)
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md (D-03, D-07, SEED-008)
exercise-ai/models.py (ExerciseBatch.dificuldades Field + verify_plan_echo)
exercise-ai/prompts.py (USER_PROMPT_TEMPLATE)
exercise-ai/tests/test_models.py (test_verify_plan_echo_batch_dificuldades_summary_mismatch)
exercise-ai/tests/test_prompts.py
  </read_first>
  <action>
    Close G-14-4 without relaxing fail-closed compare:

    1. **Field description (`ExerciseBatch.dificuldades`):** Replace the thin `"Resumo das faixas ecoadas (D-10)"` with an explicit Structured Outputs description stating that `dificuldades` is the **unique-band summary** of bands present in the batch: length 1–3, order fácil → médio → difícil, **not** one entry per exercise. (Exact PT/EN wording is implementer discretion; must encode uniqueness + order + length.)

    2. **Prompt cue (SEED-008):** Add **one short line** to `USER_PROMPT_TEMPLATE` (e.g. under Requisitos) that the batch field `dificuldades` must list the **distinct** difficulty bands used, in fácil→médio→difícil order, matching the plan summary — **not** one value per exercise. Do **not** dump enunciado/resposta/explicacao contracts or a full JSON schema.

    3. **Do not** mutate `batch.dificuldades` before `verify_plan_echo`. Leave the exact `batch.dificuldades != request.dificuldades` check as-is.

    4. **Offline tests (TDD):**
       - Extend or add in `test_models.py`: uniform request with `dificuldades == [MEDIO]` and two exercises both `MEDIO`, but `batch.dificuldades == [MEDIO, MEDIO]` → `verify_plan_echo` raises `ValueError` matching `resumo dificuldades` (documents live failure mode).
       - Add in `test_prompts.py`: `build_prompts` user text contains a stable cue substring for the summary-shape instruction (pick one assertive fragment from the new line); still assert no `enunciado:` / `resposta:` / `explicacao:` field-contract dump.
  </action>
  <acceptance_criteria>
    - `ExerciseBatch.dificuldades` Field description mentions unique/distinct bands and forbids per-exercise repetition (or equivalent clear constraint)
    - User prompt from `build_prompts` includes the new summary-shape cue; does not list enunciado/resposta/explicacao as required response keys
    - `rg "batch\.dificuldades\s*=" exercise-ai/reliability.py exercise-ai/models.py` shows no assignment that rewrites LLM summary before verify (compare-only path preserved)
    - New/extended model test fails closed on `['medio','medio']` vs `['medio']`
    - `pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_prompts.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_prompts.py -q</automated>
    <fails_when>non-zero exit, collection errors, or failed tests</fails_when>
  </verify>
  <done>Schema + prompt teach unique-band summary; duplicated-padding still fail-closed offline; SEED-008 hygiene held.</done>
</task>

<task type="auto" tdd="true">
  <name>Optional RELY fixture + suite green for G-14-4 regression</name>
  <reversibility rating="reversible">Test-only optional fixture; no RELY redesign.</reversibility>
  <files>exercise-ai/tests/test_reliability.py</files>
  <read_first>
exercise-ai/tests/test_reliability.py (existing echo → RELY paths)
exercise-ai/models.py (verify_plan_echo after Task 1)
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-UAT.md (G-14-4 reported error)
  </read_first>
  <action>
    Document the RELY exhaustion path for the G-14-4 shape (optional but preferred):

    1. Add or extend an offline RELY test: mock `generate_exercises` always returns a structurally/math-valid uniform batch whose per-slot `dificuldade` matches but `dificuldades` is padded (`[MEDIO, MEDIO]` for qty=2 vs request `[MEDIO]`); with bounded retries, assert `InvalidRequestError` / `validation_exhausted` (or equivalent host error already used by suite).

    2. Confirm no production code silently rewrites `dificuldades`.

    3. Run `pytest exercise-ai -q` to ensure no regressions.
  </action>
  <acceptance_criteria>
    - Offline RELY path treats duplicated-summary `ValueError` like other validation failures (retry then exhaust) — or existing coverage already proves this and Task 1 model test is the regression lock
    - `pytest exercise-ai -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, collection errors, or any failed/errored tests</fails_when>
  </verify>
  <done>G-14-4 regression locked offline; full exercise-ai suite green; live DEMO-01 Test 4 unblocked pending re-UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → ExerciseBatch.dificuldades | Model may still pad summary; must fail closed into RELY until shape corrects |
| Operator → uniform DEMO POST | Relies on model learning summary shape; host never silently “fixes” echo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-G4-01 | Tampering / Integrity | batch dificuldades summary | medium | mitigate | Keep exact D-07 compare; prompt+Field reduce false exhaustion |
| T-14-G4-02 | Elevation / Confusion | silent canonicalize | medium | prevent | Prohibition: no rewrite before verify |
| T-14-G4-03 | Information disclosure | prompt field dump | low | mitigate | SEED-008 one-line cue only |
| T-14-SC | Supply chain | deps | low | accept | No new packages |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_prompts.py -q` after Task 1
- `pytest exercise-ai -q` after Task 2
- Manual re-UAT (out of this plan execute): DEMO-01 Test 4 uniform live path should no longer exhaust on padded summary when model follows cue
</verification>

<success_criteria>
- Field + prompt instruct unique-band `dificuldades` (1–3, ordered)
- `verify_plan_echo` remains exact and fail-closed for padded or drifted summaries
- Offline pytest documents G-14-4 failure mode and cue presence
- No RELY/validator redesign; no silent rewrite
</success_criteria>

<output>
Create `.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-03-SUMMARY.md` when done
</output>

## PLAN COMPLETE
