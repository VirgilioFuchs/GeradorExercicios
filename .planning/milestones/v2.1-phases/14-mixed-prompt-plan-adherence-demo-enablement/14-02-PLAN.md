---
phase: 14-mixed-prompt-plan-adherence-demo-enablement
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - demo/index.html
  - demo/app.js
  - demo/serve.py
  - demo/tests/test_tracer_gerar.py
  - demo/tests/test_ui_markers.py
autonomous: true
requirements:
  - DEMO-01
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Demo Contrato form has three band count inputs (facil/medio/dificil) and editable quantidade; single dificuldade select is removed (D-09, D-10; UI-SPEC)"
    - "Soft-warn region shows advisory copy for sum=0 / sum>40 / qty≠sum without blocking POST (D-11; UI-SPEC warn surface, not .error-region)"
    - "Client sends plano only when 2+ bands have count>0; uniform/single-band uses legacy {dificuldade, quantidade} with dificuldade from sole band and quantidade = that band’s count (D-12..D-14)"
    - "demo/serve.py constructs GenerationRequest with plano (when present) so mixed POST is not dead schema (D-15)"
    - "Offline demo tests cover mixed plano POST path and UI markers for bands / soft-warn / payload rules"
  artifacts:
    - path: "demo/index.html"
      provides: "Band count inputs + soft-warn region; no dificuldade select"
      contains: "name=\"facil\"|name=\"medio\"|name=\"dificil\"|soft-warn|band-soft-warn"
    - path: "demo/app.js"
      provides: "Soft validation + uniform/mixed payload builder"
      contains: "plano|soft-warn|facil|Gerando"
    - path: "demo/serve.py"
      provides: "POST /gerar passes plano into GenerationRequest"
      contains: "plano|GenerationRequest"
    - path: "demo/tests/test_tracer_gerar.py"
      provides: "Mocked mixed plano POST asserts GenerationRequest sees plano"
      contains: "plano|generate_batch"
  key_links:
    - from: "demo form band counts"
      to: "POST /gerar JSON (uniform | mixed)"
      via: "app.js submit handler"
      pattern: "plano|dificuldade|quantidade"
    - from: "POST body.plano"
      to: "GenerationRequest(plano=...)"
      via: "DemoHandler.do_POST"
      pattern: "plano|GenerationRequest"
    - from: "GenerationRequest"
      to: "service.generate_batch → RELY + prompts from Plan 14-01"
      via: "existing generate_batch seam"
      pattern: "generate_batch"
  prohibitions:
    - "Do not preventDefault / disable submit solely for soft-warn conditions (D-11)"
    - "Do not always-send plano for uniform batches (D-12 rejected alternative)"
    - "Do not redesign RELY, prompts, or validator in this plan"
    - "Do not implement CLI/wizard UX (Phase 15)"
    - "Do not invent a demo-only plan shape parallel to PlanoDificuldade"
---

<objective>
Enable the local demo to author mixed (and uniform) batches via three band counts + soft warn, posting `plano` only when mixed, with `serve.py` accepting `plano` into `GenerationRequest` — proving DEMO-01 against the Phase 13 contract and Plan 14-01 pipeline.

Purpose: Vertical-slice the mixed plan through the throwaway demo so schema is not dead code before Phase 15 CLI/wizard.
Output: Updated `demo/index.html`, `demo/app.js`, `demo/serve.py`, and offline `demo/tests` green.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-UI-SPEC.md
@.planning/REQUIREMENTS.md
@demo/index.html
@demo/app.js
@demo/serve.py
@demo/tests/test_tracer_gerar.py
@demo/tests/test_ui_markers.py
@exercise-ai/models.py
</context>

<proven_verify_commands>
**Reuse from repo root:**

- Demo suite: `pytest demo/tests -q`
- Library (regression after serve wiring): `pytest exercise-ai -q` (optional smoke if serve imports break)
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Default counts | `facil=0`, `medio=2`, `dificil=0`; `quantidade=2` | UI-SPEC locked default (prior medio+2) |
| Soft-warn class | `.soft-warn` / `#band-soft-warn`, `role="status"`, warn colors from UI-SPEC | UI-SPEC |
| Soft-warn copy | Exact PT strings from UI-SPEC Copywriting Contract (incl. `{qty}`/`{sum}`) | UI-SPEC |
| Zero-band POST | Allow submit; soft-warn visible; body may use editable qty; hard fail server-side | D-11 + UI-SPEC |
| serve Construction | Pass `plano` dict when present; omit top-level `dificuldade` when absent rather than forcing `"medio"` if easy — must not drop `plano` | D-15 |

## Source audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| REQ DEMO-01 | Demo mounts mixed plan → GenerationRequest | Tasks 1–2 |
| CONTEXT D-09..D-11 | Three counts + editable qty + soft warn | Task 1 |
| CONTEXT D-12..D-14 | Payload uniform vs mixed | Task 1 |
| CONTEXT D-15 | serve accepts plano | Task 2 |
| UI-SPEC | Soft-warn surface, labels, copy, layout | Task 1 |
| CONTEXT D-01..D-08 | Prompt/RELY | Plan 14-01 (depends_on) |
| Deferred | Hard client block; always-send plano; CLI | Prohibitions |

## Artifacts this phase produces

| Symbol / artifact | Kind | Location |
|-------------------|------|----------|
| `#band-soft-warn` / `.soft-warn` | DOM region + CSS | `demo/index.html` |
| `input[name=facil\|medio\|dificil]` | form controls | `demo/index.html` |
| Soft-warn updater + payload builder | JS functions (names discretionary) | `demo/app.js` |
| `plano` passthrough on POST `/gerar` | server field mapping | `demo/serve.py` → `DemoHandler.do_POST` |
| Mixed plano tracer test | test | `demo/tests/test_tracer_gerar.py` |
| UI marker assertions for bands / soft-warn | tests | `demo/tests/test_ui_markers.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Demo form: band counts, soft-warn UI, uniform/mixed payload builder</name>
  <reversibility rating="reversible">Throwaway demo HTML/JS only; no schema contract change.</reversibility>
  <files>demo/index.html, demo/app.js, demo/tests/test_ui_markers.py</files>
  <read_first>
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-UI-SPEC.md
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md (D-09..D-14)
demo/index.html
demo/app.js (submit payload builder ~FormData)
demo/tests/test_ui_markers.py
  </read_first>
  <action>
    Implement DEMO-01 client surface per UI-SPEC + D-09..D-14:

    **HTML (`demo/index.html`):**
    - Inside Contrato fieldset: remove the single `dificuldade` `<select>` (D-09).
    - Add three number inputs with labels `facil` / `medio` / `dificil` (same `label > input` pattern as `quantidade`).
    - Keep editable `quantidade` (`type="number" min="1" max="40"`) (D-10).
    - Defaults: `facil=0`, `medio=2`, `dificil=0`, `quantidade=2` (UI-SPEC).
    - Soft-warn region below counts/`quantidade`: e.g. `#band-soft-warn.soft-warn`, initially hidden; `role="status"`; warn surface `#fff8e1` / border `#e0c060`; hint-sized text — **not** `.error-region` / `#a00` (UI-SPEC).

    **JS (`demo/app.js`):**
    - On change and/or submit: compute `sum = facil+medio+dificil`. Soft-warn when `sum===0`, `sum>40`, or `Number(quantidade)!==sum`. Show **all** applicable UI-SPEC copy lines (with `{qty}`/`{sum}` substituted). Clear when none apply (D-11).
    - Never `preventDefault` solely for soft-warn; never disable submit for soft-warn (loading disable of `#submit-btn` only remains).
    - Build POST body (D-12..D-14):
      - **Mixed** (2+ bands with count > 0): include `plano: { facil, medio, dificil }`; omit top-level `dificuldade`; `quantidade` may be the editable field value.
      - **Uniform** (exactly one band > 0): legacy `{ dificuldade, quantidade }` with **no** `plano`; `dificuldade` = that band enum; `quantidade` = **that band’s count** (prefer band over editable qty) (D-13, D-14).
      - **Zero bands**: still allow POST; soft-warn visible; prefer editable `quantidade` without inventing a parallel schema (UI-SPEC).
    - Keep provider/model/reasoning unchanged.

    **Tests:** Extend `demo/tests/test_ui_markers.py` to assert presence of band input names, soft-warn marker, absence of dificuldade select (or that select is gone), and key payload/soft-warn strings/markers in `app.js` as appropriate for fail-closed content checks.
  </action>
  <acceptance_criteria>
    - `demo/index.html` contains `name="facil"`, `name="medio"`, `name="dificil"`, and a soft-warn region; no `name="dificuldade"` select
    - Soft-warn copy strings from UI-SPEC appear in `app.js` (or HTML if static) for sum=0, sum>40, and qty≠sum
    - Payload builder logic encodes mixed→`plano` and uniform→legacy `dificuldade`+band `quantidade` (D-12..D-14)
    - Soft-warn does not gate submit (no preventDefault-only-for-warn)
    - `pytest demo/tests/test_ui_markers.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest demo/tests/test_ui_markers.py -q</automated>
    <fails_when>non-zero exit, missing markers asserted by tests, or failed assertions</fails_when>
  </verify>
  <done>Band form + soft-warn + client payload rules implemented and marker-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>serve.py plano passthrough + demo tracer tests for mixed POST</name>
  <reversibility rating="reversible">Local demo server mapping only; GenerationRequest schema already accepts plano.</reversibility>
  <files>demo/serve.py, demo/tests/test_tracer_gerar.py</files>
  <read_first>
demo/serve.py (DemoHandler.do_POST GenerationRequest construction)
exercise-ai/models.py (PlanoDificuldade / GenerationRequest fields)
demo/tests/test_tracer_gerar.py
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md (D-15)
  </read_first>
  <action>
    Make mixed POST live end-to-end on the demo server (mocked LLM):

    1. **`demo/serve.py` (D-15):** When building `GenerationRequest` from JSON body, pass through `plano` when present (dict → accepted by Pydantic `PlanoDificuldade`). Do not silently drop `plano`. Prefer constructing with only provided fields so uniform clients still work (`dificuldade` + `quantidade`) and mixed clients can omit top-level `dificuldade`. Keep existing ValidationError → 400 mapping.

    2. **Fixture hygiene:** Update mocked `Exercise` / `ExerciseBatch` constructions in `demo/tests/test_tracer_gerar.py` to include required `dificuldade` / `dificuldades` echoes (Phase 13) so tracers stay green.

    3. **Tracer tests:**
       - Existing uniform POST still yields `GenerationRequest` with expected topico/quantidade (and no reliance on dropped fields).
       - New mixed POST body with `plano: {facil:1, medio:1, dificil:0}, quantidade:2` (no top-level dificuldade) → mocked `generate_batch` receives a request whose `plano` normalized / `itens_ordenados` is `[facil, medio]` (assert via call_args).
       - Optionally assert Pydantic rejection path still returns 400 for illegal qty↔plano drift.

    4. Run `pytest demo/tests -q`. Do not change library RELY/prompt code here (already Plan 14-01).
  </action>
  <acceptance_criteria>
    - `rg "plano" demo/serve.py` shows `plano` passed into `GenerationRequest` construction
    - Mixed POST tracer: `generate_batch` called with request that has mixed ordered specs from `plano`
    - Uniform POST tracer still passes
    - Exercise/ExerciseBatch mocks include echo fields required by models
    - `pytest demo/tests -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest demo/tests -q</automated>
    <fails_when>non-zero exit, collection errors, or failed/errored tests</fails_when>
  </verify>
  <done>DEMO-01 complete: demo can POST mixed plano into GenerationRequest; offline demo suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /gerar | Untrusted JSON; Pydantic + existing header/lock guards remain hard gate |
| Soft-warn UI | Advisory only — not a security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | client qty/band desync | low | mitigate | Soft-warn + server/Pydantic BATCH-02 hard reject |
| T-14-05 | Information disclosure | demo errors | low | accept | Local throwaway demo; no secrets in responses |
| T-14-06 | Denial of service | local concurrent generate | low | mitigate | Existing Lock → 409 unchanged |
| T-14-SC | Supply chain | deps | low | accept | Stdlib demo only; no new packages |
</threat_model>

<verification>
- `pytest demo/tests/test_ui_markers.py -q` after Task 1
- `pytest demo/tests -q` after Task 2
- Manual smoke (optional): open demo, set facil=1 medio=1, confirm soft-warn if qty desynced, POST with mock/real as available
</verification>

<success_criteria>
- Demo UI authors band counts with soft warn; mixed POST includes `plano`; uniform keeps legacy shape with band-preferred quantidade
- serve.py accepts `plano` into GenerationRequest
- Offline demo tests green
</success_criteria>

<output>
Create `.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-02-SUMMARY.md` when done
</output>
