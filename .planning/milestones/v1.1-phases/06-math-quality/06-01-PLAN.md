---
phase: 06-math-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/math_check.py
  - exercise-ai/validator.py
  - exercise-ai/reliability.py
  - exercise-ai/tests/test_math_check.py
  - exercise-ai/tests/test_validator.py
  - exercise-ai/tests/test_reliability.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MATH-01
  - MATH-02
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Clear arithmetic inconsistencies (+ − × ÷ with integers/simple decimals) are rejected by validation without a live LLM (MATH-01; D-01; D-04; D-14)"
    - "Clear ax+b=c inconsistencies (integer a,b,c with a≠0) are rejected the same way (MATH-01; D-01; D-15)"
    - "Uninterpretable enunciado/resposta (fractions, radicals, systems, inequalities, geometry, or parse failure) do not fail the batch; they are recorded for postmortem (D-02; D-16)"
    - "When multiple exercises are mathematically inconsistent, one ValueError lists all of them; the batch still fails (D-03)"
    - "Math failure uses dual channel: stderr [MATH] with index + detail; raise is minimal truncated PT without the [MATH] tag (MATH-02; D-10; D-11; D-12)"
    - "Math ValueError enters the existing generate_validated_batch loop only — regenerates up to N then stops (MATH-02; D-07; RELY-01)"
    - "After regenerations are exhausted, user-facing reason is prefixed with 'após N regenerações:' plus the minimal math/struct reason (D-13)"
    - "Postmortem diagnostic file is written only on final batch failure (never on success), without API keys or provider payloads (D-08; LOG-02)"
  artifacts:
    - path: "exercise-ai/math_check.py"
      provides: "Hybrid arithmetic / ax+b=c checks; uninterpretable records; [MATH] stderr + minimal PT ValueError"
      contains: "check_math_batch"
    - path: "exercise-ai/validator.py"
      provides: "Single validation entry calls math check after structural pass"
      contains: "check_math_batch"
    - path: "exercise-ai/reliability.py"
      provides: "Exhaustion prefix + final-failure postmortem; still one retry loop"
      contains: "após"
    - path: "exercise-ai/tests/test_math_check.py"
      provides: "Named minimal fixtures for correct/wrong arithmetic, ax+b=c, uninterpretable, multi-index"
      contains: "check_math_batch"
    - path: "exercise-ai/tests/test_validator.py"
      provides: "Integration: structural OK + math-wrong raises via validate_exercise_batch"
      contains: "MATH"
    - path: "exercise-ai/tests/test_reliability.py"
      provides: "Math fail then success regenerates; exhaustion prefix + postmortem only on final fail"
      contains: "após"
  key_links:
    - from: "validate_exercise_batch"
      to: "math_check.check_math_batch"
      via: "after structural errors empty; same ValueError contract"
      pattern: "check_math_batch"
    - from: "math ValueError"
      to: "reliability.generate_validated_batch"
      via: "existing except ValueError regen — no second loop"
      pattern: "generate_validated_batch"
    - from: "final exhausted ValueError"
      to: "postmortem artifact + 'após N regenerações:' raise"
      via: "D-08 + D-13 on last attempt only"
      pattern: "após"
  prohibitions:
    - "No second retry loop for math outside generate_validated_batch"
    - "No full CAS / sympy / vendor math library in this phase (D-06 vendor path not taken — stdlib only per D-09; no library required)"
    - "No fail-on-uninterpretable batch failure"
---

## Phase Goal

**As a** user generating math exercises via CLI, **I want** clearly inconsistent arithmetic and `ax+b=c` answers rejected with a distinct math error that reuses bounded regeneration, **so that** bad LLM answers are filtered without inventing a second retry path or failing on unparseable items.

## Spec probe

- SPEC.md: EDGE_ABSENT / PROHIB_ABSENT — truths from ROADMAP success criteria + `06-CONTEXT.md` D-01…D-17; prohibitions: no second retry loop; no full CAS; no fail-on-uninterpretable (D-02).
- Edge probe MATH-01/MATH-02 were unclassified — planner assumptions: verify MATH-01 via fixture unit tests (no LLM); verify MATH-02 via mocked `generate_exercises` + real `validate_exercise_batch` regen/exhaustion.

<objective>
**As a** CLI user, **I want** mathematical inconsistency checks on basic arithmetic and linear equations wired into the existing validation→RELY path, **so that** MATH-01 and MATH-02 land as one vertical slice with clear `[MATH]` diagnostics and limited regenerations.

Purpose: Deliver MATH-01 + MATH-02 per locked D-01…D-17 using stdlib heuristics + minimal fixtures; reuse Phase 5 `generate_validated_batch` only.
Output: `math_check.py`; validator hook; reliability exhaustion prefix + final postmortem; pytest coverage without live LLM.

Decision coverage: D-01 through D-17 (cite in tasks). Deferred not planned: fine-tune pipeline; property-style gens; fractions/radicals/systems/CAS; prompt repair.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-math-quality/06-CONTEXT.md
@.planning/phases/06-math-quality/06-PATTERNS.md
@exercise-ai/validator.py
@exercise-ai/reliability.py
@exercise-ai/models.py
@exercise-ai/tests/test_validator.py
@exercise-ai/tests/test_reliability.py
@exercise-ai/tests/conftest.py
</context>

<proven_verify_commands>
**Reuse prior phase grounding from repo root.**

- Phase 05 · Task 1 style: `pytest exercise-ai/tests/test_reliability.py exercise-ai/tests/test_main.py -q`
- Phase 05 · Task 2 style / canonical: `pytest exercise-ai -q`
- This plan focus: `pytest exercise-ai/tests/test_math_check.py exercise-ai/tests/test_validator.py exercise-ai/tests/test_reliability.py -q`
</proven_verify_commands>

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | Reject basic math inconsistencies; clear math message; RELY regen when applicable | 06-01 | COVERED | ROADMAP success 1–3 |
| REQ | MATH-01 | Reject mathematically inconsistent basic cases | 06-01 T1–T2 | COVERED | arithmetic + ax+b=c |
| REQ | MATH-02 | Clear math message; may trigger RELY regen | 06-01 T1, T3 | COVERED | [MATH] + ValueError + hook |
| RESEARCH | — | (skipped this run) | — | N/A | Plan from CONTEXT + PATTERNS + code |
| CONTEXT | D-01 | arithmetic + ax+b=c only | 06-01 T1–T2 | COVERED | costly surface |
| CONTEXT | D-02 | uninterpretable → pass + record; clear inconsistency → reject | 06-01 T2–T3 | COVERED | |
| CONTEXT | D-03 | list ALL inconsistent exercises; batch fails | 06-01 T2 | COVERED | |
| CONTEXT | D-04 | moderate strictness | 06-01 T1–T2 | COVERED | known templates + obvious contradictions |
| CONTEXT | D-05 | hybrid fixtures + heuristics | 06-01 T1–T2 | COVERED | |
| CONTEXT | D-06 | vendor only if needed | 06-01 | COVERED | prefer stdlib (D-09); no vendor |
| CONTEXT | D-07 | single path validate_exercise_batch / Phase 5 hook | 06-01 T1, T3 | COVERED | |
| CONTEXT | D-08 | postmortem only on final batch failure | 06-01 T3 | COVERED | |
| CONTEXT | D-09 | prefer stdlib | 06-01 T1–T2 | COVERED | |
| CONTEXT | D-10 | [MATH] stderr detailed; raise PT short | 06-01 T1–T2 | COVERED | |
| CONTEXT | D-11 | raise minimal; details in log/postmortem | 06-01 T1–T3 | COVERED | |
| CONTEXT | D-12 | raise truncated; full in postmortem/log | 06-01 T2–T3 | COVERED | |
| CONTEXT | D-13 | `após N regenerações:` after exhaustion | 06-01 T3 | COVERED | |
| CONTEXT | D-14 | arithmetic ints/decimals + − × ÷ | 06-01 T1–T2 | COVERED | |
| CONTEXT | D-15 | ax+b=c integers a≠0 | 06-01 T2 | COVERED | |
| CONTEXT | D-16 | % only if reduces to arithmetic; else out | 06-01 T2 | COVERED | uninterpretable if not |
| CONTEXT | D-17 | minimal named fixtures; property gens deferred | 06-01 T1–T2 | COVERED | |

Deferred (not planned): fine-tune from postmortem; property-style generators; fractions/radicals/systems/inequalities/geometry/CAS; prompt repair.

## Artifacts this phase produces

- `exercise-ai/math_check.py` — `check_math_batch` (+ helpers to drain/record uninterpretable cases); stdlib parsers for arithmetic and `ax+b=c`
- `exercise-ai/validator.py` — calls math check after structural pass; keeps `ValueError` contract for RELY
- `exercise-ai/reliability.py` — on final exhausted `ValueError`: prefix `após N regenerações:`; write postmortem only then (D-08/D-13)
- `exercise-ai/tests/test_math_check.py` — named minimal hybrid fixtures (D-05; D-17)
- `exercise-ai/tests/test_validator.py` — math integration via `validate_exercise_batch`
- `exercise-ai/tests/test_reliability.py` — MATH-02 regen + exhaustion + postmortem assertions
- `.planning/phases/06-math-quality/COVERAGE.md` — no new external API declaration
- `.planning/ROADMAP.md` — Phase 6 plans list

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one clear arithmetic inconsistency through validator → RELY</name>
  <reversibility rating="costly">D-01: check surface (arithmetic first) and fixture contract become the regression baseline for the phase.</reversibility>
  <files>exercise-ai/math_check.py, exercise-ai/validator.py, exercise-ai/tests/test_math_check.py, exercise-ai/tests/test_reliability.py</files>
  <read_first>
    .planning/phases/06-math-quality/06-CONTEXT.md,
    .planning/phases/06-math-quality/06-PATTERNS.md,
    exercise-ai/validator.py,
    exercise-ai/reliability.py,
    exercise-ai/tests/test_validator.py,
    exercise-ai/tests/test_reliability.py,
    exercise-ai/tests/conftest.py
  </read_first>
  <action>
    Deliver one production-quality end-to-end math path (tracer-first). Stdlib only (D-09). Research did not require an external library, so D-06 (pin + vendor in repo) is not taken — no vendor path. Do not add CAS/sympy or vendor a math package. Do not invent a second retry loop (D-07). Do not fail batches solely because parsing failed (D-02 — full uninterpretable matrix in Task 2).

    **A. New `exercise-ai/math_check.py` (D-01; D-04; D-05; D-06; D-09; D-10; D-14)**
    1. Add `check_math_batch(batch: ExerciseBatch) -> None` that scans exercises and raises `ValueError` only on **clear** arithmetic inconsistency for a known template (e.g. enunciado like `2 + 2` / `2+2=?` with wrong numeric `resposta`). Correct arithmetic must pass.
    2. On clear fail: print stderr lines tagged `[MATH]` including at least index and esperado vs obtido (or equivalent detail) (D-10); raise a **minimal PT** `ValueError` that does **not** include the `[MATH]` tag (mirror validator dual-channel: detail on stderr, short raise) (D-10; D-11).
    3. Stub/record API for uninterpretable cases OK to be empty or no-op for tracer; Task 2 fills D-02 fully.
    4. Prefer `re` / `decimal` / simple eval-safe parsing — never `eval` on raw unconstrained text; whitelist digits and operators for the arithmetic template.

    **B. Wire `validator.py` (D-07)**
    1. After structural checks succeed (existing `errors` empty path), call `check_math_batch(batch)` before return.
    2. Keep signature `validate_exercise_batch(batch, request) -> ExerciseBatch`. Do not introduce a new exception type — RELY already retries `ValueError` (D-07).

    **C. Tracer tests (no live LLM; D-17)**
    1. `test_math_check.py`: one named fixture wrong arithmetic → raises; stderr has `[MATH]`; raise text is short PT and lacks `[MATH]`. One correct arithmetic → returns without raise.
    2. `test_reliability.py`: first `generate_exercises` returns structurally valid batch with wrong arithmetic answer; second returns correct arithmetic → `call_count == 2`, stderr shows `1ª Regeneração`, `--out` written (MATH-02 via existing hook). Reuse `main.run` + patch pattern from `test_validation_fail_once_then_success`.

    Out of this task: full `ax+b=c`, multi-index listing, uninterpretable postmortem drain, exhaustion `após N regenerações:` prefix, postmortem file write — Tasks 2–3.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_math_check.py exercise-ai/tests/test_reliability.py -q</automated>
    <fails_when>non-zero pytest exit; math wrong answer accepted; regen call_count != 2 on tracer regen case; `[MATH]` appears in raise message; second retry loop added outside generate_validated_batch; live LLM called</fails_when>
  </verify>
  <acceptance_criteria>
    - `check_math_batch` exists and is invoked from `validate_exercise_batch` after structural pass (D-07)
    - Clear wrong arithmetic raises minimal PT `ValueError`; stderr includes `[MATH]` detail (MATH-01; D-10; D-14)
    - Correct arithmetic passes validation (D-04)
    - Math fail then success regenerates once via `generate_validated_batch` / `main.run` (MATH-02; D-07)
    - Stdlib only; no CAS package (D-09)
  </acceptance_criteria>
  <done>End-to-end arithmetic math rejection works through validator and RELY regen with green tracer tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — ax+b=c, uninterpretable pass+record, multi-index, fixtures</name>
  <reversibility rating="costly">D-01/D-05: full basic-case surface and hybrid fixture set lock what “math quality” means for v1.1.</reversibility>
  <files>exercise-ai/math_check.py, exercise-ai/tests/test_math_check.py, exercise-ai/tests/test_validator.py</files>
  <read_first>
    .planning/phases/06-math-quality/06-CONTEXT.md,
    .planning/phases/06-math-quality/06-PATTERNS.md,
    exercise-ai/math_check.py,
    exercise-ai/validator.py,
    exercise-ai/tests/test_math_check.py,
    exercise-ai/tests/test_validator.py
  </read_first>
  <behavior>
    - Wrong ax+b=c solution (integers, a≠0) → ValueError; correct solution → pass (D-15)
    - Uninterpretable item (e.g. fraction/radical/system-like text) → no raise; record available for postmortem drain (D-02; D-16)
    - Two clear inconsistencies in one batch → single ValueError lists both indices (D-03)
    - validate_exercise_batch integration: structural OK + math-wrong → raise; stderr `[MATH]`; raise truncated/minimal (D-10–D-12)
    - Simple percent that reduces to arithmetic may be checked; otherwise treat as uninterpretable (D-16)
  </behavior>
  <action>
    Expand the tracer math checker to the full D-01 surface with hybrid fixtures (D-05; D-17). Keep stdlib (D-09). Moderate strictness: only reject clear contradictions on known templates (D-04).

    **A. Heuristics (D-01; D-14; D-15; D-16)**
    1. Arithmetic: `+ − × ÷` (and ASCII `+-*/` / `x` / `×`) with integers and simple decimals (D-14).
    2. Equations: only `ax + b = c` with integer a,b,c and a ≠ 0; compare implied x to parsed `resposta` (D-15).
    3. Explicitly out of scope patterns (fractions, radicals, systems, inequalities, geometry) → uninterpretable: do **not** raise; append a structured record (index, snippets, reason=`uninterpretable`) (D-02; D-16). Percent: check only if it clearly reduces to arithmetic; else uninterpretable (D-16).
    4. Expose `drain_uninterpretable_records() -> list[dict]` (or equivalent) that returns and clears accumulated records for Task 3 postmortem (D-02; D-08).

    **B. Multi-report + UX (D-03; D-10; D-11; D-12)**
    1. Collect **all** clear inconsistencies before raising (D-03). Raise one minimal truncated PT message that identifies the problem as mathematical (distinct from structural empty-field wording) and lists affected indices; put full esperado/obtido detail only on `[MATH]` stderr lines (D-10–D-12).
    2. Do not put `[MATH]` inside `str(exc)`.

    **C. Tests first / fixtures (D-05; D-17)**
    1. Named minimal cases in `test_math_check.py` (parametrize OK): correct arith; wrong arith; correct ax+b=c; wrong ax+b=c; uninterpretable no-raise + record present; multi-index both listed.
    2. `test_validator.py`: 1–2 integration tests — structurally valid + math-wrong raises via `validate_exercise_batch`; preserve existing structural tests unchanged.
    3. No property-style generators (deferred) (D-17). No live LLM.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_math_check.py exercise-ai/tests/test_validator.py -q</automated>
    <fails_when>non-zero pytest exit; uninterpretable case raises ValueError; multi-fail batch mentions only one index; ax+b=c wrong answer accepted; structural regression in test_validator; live LLM</fails_when>
  </verify>
  <acceptance_criteria>
    - Arithmetic and ax+b=c clear inconsistencies rejected (MATH-01; D-01; D-14; D-15)
    - Uninterpretable does not fail batch; records drainable (D-02; D-16)
    - Multi-inconsistency lists all indices in one failure (D-03)
    - Dual-channel `[MATH]` stderr + minimal truncated PT raise (D-10; D-11; D-12)
    - Hybrid named fixtures only; no property gens (D-05; D-17)
  </acceptance_criteria>
  <done>Full basic math surface + fixtures green through math_check and validator integration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand — exhaustion prefix, final postmortem, MATH-02 reliability contract</name>
  <files>exercise-ai/reliability.py, exercise-ai/math_check.py, exercise-ai/tests/test_reliability.py</files>
  <read_first>
    .planning/phases/06-math-quality/06-CONTEXT.md,
    .planning/phases/06-math-quality/06-PATTERNS.md,
    exercise-ai/reliability.py,
    exercise-ai/math_check.py,
    exercise-ai/main.py,
    exercise-ai/tests/test_reliability.py
  </read_first>
  <behavior>
    - Math wrong every attempt with max_retries=1 → SystemExit(1), call_count==2, user message contains `após 1 regenerações:` (or correct N) + minimal math reason (D-13)
    - Success path writes no postmortem file (D-08)
    - Final math (or validation) failure writes one postmortem artifact with inconsistent and/or uninterpretable records; no API keys (D-08; LOG-02)
    - Still exactly one retry loop in reliability; math does not add another (D-07)
  </behavior>
  <action>
    Close MATH-02 UX at exhaustion and D-08 postmortem without a second retry path (D-07).

    **A. Exhaustion prefix (D-13; D-11; D-12)**
    1. In `generate_validated_batch`, when the final attempt raises `ValueError` (attempt == max_retries), if `max_retries > 0`, re-raise (or wrap) so the user-facing message starts with `após N regenerações:` where N is the exhausted regeneration count (`max_retries`), then the minimal reason (math or structural). Details stay on stderr/`[MATH]` / postmortem — keep raise short (D-11; D-12).
    2. When `max_retries == 0`, do not invent a regenerações prefix (no regenerations occurred).
    3. Do not print the user failure reason on intermediate attempts (Phase 5 D-13 already); only transform the final raise.

    **B. Postmortem only on final failure (D-08; D-02)**
    1. On success return: do **not** write postmortem; optionally clear any uninterpretable buffer so success leaves no stale file.
    2. On final `ValueError` (before/while re-raising): write a diagnostic artifact under a stable relative path (discretion: e.g. `exercise-ai/math_postmortem.jsonl` or tmp-path injectable for tests). Include structured records for clear inconsistencies and drained uninterpretable cases (index, short enunciado/resposta snippets, reason). UTF-8. Never include API keys, env values, or raw provider payloads (LOG-02).
    3. Format discretion (jsonl vs JSON) allowed by CONTEXT.

    **C. Tests (MATH-02)**
    1. Exhaustion on persistent math-wrong batch: `após N regenerações:` present; no `--out`; `chamadas` still logged.
    2. Success after math regen: no postmortem file created (assert path absent or injectable sink empty).
    3. Final failure: postmortem exists and contains expected index/reason fields; secret-like strings absent.
    4. Confirm module still has a single loop — do not add math-specific retry beside `generate_validated_batch` (D-07).

    Discretion: inject postmortem path via kwarg/env for tests vs fixed path with tmp_path monkeypatch.
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero pytest exit; exhaustion message missing `após` prefix when max_retries > 0; postmortem written on success; second math-only retry loop present; secrets in postmortem; live LLM</fails_when>
  </verify>
  <acceptance_criteria>
    - Exhausted regenerations surface `após N regenerações:` + minimal reason (D-13; MATH-02)
    - Postmortem only on final failure; includes inconsistent and/or uninterpretable records (D-08; D-02)
    - Math failures still only regenerate via `generate_validated_batch` (D-07)
    - Full `pytest exercise-ai -q` green with mocks only
  </acceptance_criteria>
  <done>MATH-02 exhaustion UX + final postmortem shipped; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM batch text → math_check | Untrusted enunciado/resposta strings cross into parsers and comparison logic |
| math_check / validator → stderr | `[MATH]` detail and optional batch context must not leak secrets |
| reliability → postmortem filesystem | Diagnostic write on final failure only; path must not embed API keys or provider payloads |
| reliability → user raise | Truncated PT message after exhaustion crosses to main/`SystemExit` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `[MATH]` stderr / postmortem | high | mitigate | Log index + math detail + short field snippets only; never API keys, `.env`, or raw provider payloads (D-10; D-08; LOG-02); assert in tests |
| T-06-02 | Tampering | postmortem / `--out` | high | mitigate | Write postmortem only on final failure (D-08); keep Phase 5 rule: `--out` only on success; tests cover both |
| T-06-03 | Denial of Service | parser / retry | medium | mitigate | Stdlib bounded heuristics (no CAS hang); reuse existing max_retries cap; no second retry loop (D-07; D-09) |
| T-06-04 | Elevation of Privilege | postmortem path | low | accept | Local CLI trusts operator filesystem (same as `--out`) |
| T-06-05 | Spoofing | N/A | low | accept | No new auth surface in this phase |
| T-06-SC | Tampering | dependencies | low | accept | No new pip packages; stdlib only (D-09); skip package-legitimacy install gate |

ASVS L1: block on high — T-06-01 and T-06-02 must be mitigated in Tasks 1–3.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pytest exercise-ai -q` passes from repo root without live LLM
- [ ] Clear arithmetic and ax+b=c inconsistencies rejected; uninterpretable does not fail batch
- [ ] Multi-index math failures listed in one ValueError
- [ ] `[MATH]` on stderr only; raise minimal/truncated PT
- [ ] Math ValueError regenerates via existing `generate_validated_batch` only
- [ ] Exhaustion uses `após N regenerações:` when max_retries > 0; postmortem only on final failure
- [ ] No CAS/vendor math lib; no second retry loop; no prompt repair
</verification>

<success_criteria>
- MATH-01 and MATH-02 satisfied with D-01…D-17 honored
- ROADMAP Phase 6 success criteria met (reject basic inconsistencies; distinct math message; RELY path on math failure)
- Threat mitigations T-06-01 and T-06-02 implemented and covered by tests where applicable
</success_criteria>

<output>
After completion, create `.planning/phases/06-math-quality/06-01-SUMMARY.md`
</output>
