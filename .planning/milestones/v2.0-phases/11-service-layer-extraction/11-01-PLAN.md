---
phase: 11-service-layer-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/service.py
  - exercise-ai/main.py
  - exercise-ai/tests/test_service.py
  - exercise-ai/tests/test_main.py
  - exercise-ai/tests/test_failover.py
  - exercise-ai/tests/test_output_paths.py
  - exercise-ai/tests/test_token_usage.py
autonomous: true
requirements:
  - EMBED-01
  - EMBED-03
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Host imports service and calls generate_batch(request); receives ExerciseBatch or a normal Exception — never process termination from the library path (D-01; D-02; D-14; EMBED-01)"
    - "main.run still prints batch text, writes --out JSON, fail logs, and exits on failure; wizard still enters via main.run (D-14; EMBED-03)"
    - "service.py does not import main; direction is main → service → pipeline (D-15)"
    - "Full offline suite green after re-pointing patches that targeted main.generate_with_failover / main.resolve_max_retries (D-13; D-14)"
  artifacts:
    - path: "exercise-ai/service.py"
      provides: "Public generate_batch(request) -> ExerciseBatch seam"
      contains: "generate_batch"
    - path: "exercise-ai/main.py"
      provides: "CLI adapter: run() delegates to service then presentation/exit"
      contains: "service.generate_batch|from service import"
    - path: "exercise-ai/tests/test_service.py"
      provides: "Host-path tracer: return batch / raise without process exit"
      contains: "generate_batch"
  key_links:
    - from: "main.run"
      to: "service.generate_batch"
      via: "delegate pipeline; keep print/--out/fail-log/exit in run"
      pattern: "generate_batch"
    - from: "service.generate_batch"
      to: "failover.generate_with_failover"
      via: "begin_run + resolve_max_retries(None) + flush in finally"
      pattern: "generate_with_failover"
  prohibitions:
    - "No kwargs on generate_batch (max_retries/provider/reasoning deferred)"
    - "Do not widen return type beyond ExerciseBatch"
    - "Do not add env restore, error subclasses, encoding harden, Gemini timeout, or README contract in this plan (D-13 pure move)"
    - "Do not convert internal stderr prints to logging"
    - "Do not touch packaging / rename"
---

<objective>
Extract a pure embed seam: `service.generate_batch(request) -> ExerciseBatch` with `main.run()` as CLI adapter only — suite green, zero CLI/wizard regression, no hygiene mixed in.

Purpose: Prove host-callable path before env/error/encoding changes (D-13).
Output: `exercise-ai/service.py`, delegated `run()`, re-pointed tests, tracer coverage for return/raise-without-exit.
</objective>

<execution_context>
@C:/Users/Anglo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-service-layer-extraction/11-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENT.md
@exercise-ai/main.py
@exercise-ai/failover.py
@exercise-ai/models.py
@exercise-ai/reliability.py
@exercise-ai/token_usage/collector.py
@exercise-ai/tests/test_main.py
@exercise-ai/tests/test_failover.py
@exercise-ai/tests/test_output_paths.py
@exercise-ai/tests/test_token_usage.py
</context>

<proven_verify_commands>
**Reuse from repo root (CI / README):**

- Canonical suite: `pytest exercise-ai -q`
- Targeted: `pytest exercise-ai/tests/test_service.py exercise-ai/tests/test_main.py exercise-ai/tests/test_failover.py exercise-ai/tests/test_output_paths.py exercise-ai/tests/test_token_usage.py -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Public signature | `generate_batch(request: GenerationRequest) -> ExerciseBatch` only | D-01; D-02 (already locked — no checkpoint) |
| CLI `--max-retries` bridge | When `run(..., max_retries=...)` is not None, set `RELY_MAX_RETRIES` for the call then restore that key in `finally`; service always calls `resolve_max_retries(None)` | No kwargs; preserves argparse override without permanent env leak across tests |
| Token lifecycle | `begin_run` + `flush_token_usage` inside `generate_batch` try/finally; `run()` keeps its existing `finally` flush as idempotent safety net | Matches Phase 9 single-flush idiom; pure move |
| Error types | Leave bare `ValueError` / `RuntimeError` | Hygiene is Plan 02 (D-03) |
| Postmortem / fail-log | Unchanged locations this plan | D-06 is Plan 02 |

<tasks>

<task type="tracer">
  <name>End-to-end host path: generate_batch returns batch / raises without process exit</name>
  <reversibility rating="one-way">Return type ExerciseBatch is a published host contract; widening later is a breaking change (D-02 — already locked in CONTEXT; checkpoint omitted per planner-reversibility already-made rule).</reversibility>
  <files>exercise-ai/service.py, exercise-ai/main.py, exercise-ai/tests/test_service.py</files>
  <read_first>exercise-ai/main.py (run, begin_run/flush, generate_with_failover call, except/sys.exit branches), exercise-ai/failover.py (generate_with_failover), exercise-ai/reliability.py (resolve_max_retries), .planning/phases/11-service-layer-extraction/11-CONTEXT.md (D-01, D-02, D-13, D-14, D-15), .planning/research/ARCHITECTURE.md (seam example + pinned-test table)</read_first>
  <behavior>
    - Host calls generate_batch(request) with pipeline mocked → returns ExerciseBatch; stdout has no exercise dump from the service path
    - Host calls generate_batch when pipeline raises ValueError or RuntimeError → exception propagates; catching Exception is enough (no process-terminating base exception from the library path)
    - run(request, out_path, max_retries) still writes --out on success and still exits on failure after presentation
  </behavior>
  <action>
    Create `exercise-ai/service.py` as the embed boundary (per D-01, D-02, D-15). Public API: only `generate_batch(request: GenerationRequest) -> ExerciseBatch`. Inside: namespaced logger (may stay quiet this plan), `begin_run()`, `n = resolve_max_retries(None)`, `generate_with_failover(request, max_retries=n)`, return the batch; `flush_token_usage()` in `finally`. Do not import `main`. Do not print the batch. Do not write `--out` or fail-error logs. Do not call process-exit helpers.

    Refactor `main.run` into a CLI adapter (per D-13, D-14): keep `_configure_logging`, presentation (`format_batch_text`, `--out` write), fail-error-log + exit branches, and the existing `finally: flush_token_usage()` safety net. Replace the inlined pipeline body with a call to `service.generate_batch(request)`. Bridge CLI `max_retries` without kwargs: when `max_retries is not None`, set `RELY_MAX_RETRIES` for the duration of the service call and restore prior presence/absence of that key afterward (local finally — not the full D-08 scoped-env yet). Keep `resolve_max_retries` import available if still needed for the bridge; prefer service-owned resolve for the library path.

    Add `exercise-ai/tests/test_service.py` as the tracer: mock `service.generate_with_failover` (or underlying generate path) to return a fixture batch and assert return type; mock a raise and assert a normal `Exception` subclass is caught by `except Exception` (library path must not require catching BaseException). Assert service module source does not import `main`.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_service.py -q</automated>
  </verify>
  <done>generate_batch is host-callable end-to-end on one mocked path; run still presents/exits; service has no main import.</done>
</task>

<task type="auto" tdd="true">
  <name>Re-point main-internal patches and keep suite green</name>
  <files>exercise-ai/tests/test_main.py, exercise-ai/tests/test_failover.py, exercise-ai/tests/test_output_paths.py, exercise-ai/tests/test_token_usage.py, exercise-ai/main.py, exercise-ai/service.py</files>
  <read_first>exercise-ai/tests/test_main.py (test_main_source_keeps_plain_stderr_contract, test_cli_max_retries_passes_to_run), exercise-ai/tests/test_failover.py (test_run_failover_writes_out_and_redacts), exercise-ai/tests/test_output_paths.py, exercise-ai/tests/test_token_usage.py (main_mod patches), .planning/research/ARCHITECTURE.md (pinned-test table ~598-600)</read_first>
  <behavior>
    - test_main_source_keeps_plain_stderr_contract still asserts plain stderr + exit in main; delegation string points at service.generate_batch (not require generate_with_failover in main source)
    - test_cli_max_retries_passes_to_run observes resolve_max_retries with CLI 0 via the service (or env bridge) path
    - test_run_failover_writes_out_and_redacts and output_paths/token_usage run() patches target service.generate_with_failover (or equivalent) so monkeypatch still intercepts
    - Wizard tests that patch main.run remain valid unchanged
  </behavior>
  <action>
    Per D-14 / ARCHITECTURE pinned-test map, update every test that patched `main.generate_with_failover` or `main.resolve_max_retries` so patches land where the names live after delegation (`service` module). Update `test_main_source_keeps_plain_stderr_contract` so it no longer requires `generate_with_failover` in `main.py` source; assert the adapter still has plain stderr prints, `sys.exit(1)`, `ensure_ascii=False`, and that delegation to `service.generate_batch` is visible in main source. Do not weaken SystemExit assertions on `run()` / CLI. Do not add Plan 02/03 hygiene. Confirm with full suite.
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
  </verify>
  <done>All offline tests pass; CLI/wizard SystemExit contracts intact; pure-move seam only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host process → service.generate_batch | Untrusted host supplies GenerationRequest; library must not terminate host process |
| CLI adapter → filesystem (`--out`, fail logs, token-usage) | Existing CLI write sites; unchanged this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of privilege / DoS | service vs host | high | mitigate | Library path raises only; never process-exit; tracer asserts Exception-catchable failures |
| T-11-02 | Information disclosure | service import of main | medium | mitigate | D-15: service must not import main (avoids import-time load_dotenv on host) |
| T-11-03 | Tampering | monkeypatched tests missing seam | medium | mitigate | Re-point patches to service so CI still exercises adapter behavior |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_service.py -q` passes
- `pytest exercise-ai -q` passes
- `rg "from main|import main" exercise-ai/service.py` finds no matches
</verification>

<success_criteria>
- Host-callable `generate_batch(request) -> ExerciseBatch` exists
- CLI/wizard behavior preserved under existing SystemExit tests
- Pure move only — no env/error/encoding/timeout/README scope creep
</success_criteria>

<output>
Create `.planning/phases/11-service-layer-extraction/11-01-SUMMARY.md` when done
</output>
