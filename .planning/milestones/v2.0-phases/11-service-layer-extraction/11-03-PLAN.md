---
phase: 11-service-layer-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - exercise-ai/generator_gemini.py
  - exercise-ai/failover.py
  - exercise-ai/validator.py
  - exercise-ai/main.py
  - exercise-ai/tests/test_generators.py
  - exercise-ai/tests/test_failover.py
  - exercise-ai/tests/test_encoding.py
  - README.md
autonomous: true
requirements:
  - EMBED-05
  - EMBED-06
  - EMBED-07
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Gemini client uses finite HttpOptions.timeout in milliseconds aligned to OpenAI 30s; README documents host worst-case order of magnitude (D-11; EMBED-05)"
    - "Diagnostics/CLI paths do not raise UnicodeEncodeError on cp1252 for arrows/math glyphs; success is not reported as failure (D-10; EMBED-06)"
    - "README has a short embed contract section: generate_batch signature, batch JSON shape, three error subclasses table, reserved module names, sequential one-generation-at-a-time contract (D-12; EMBED-07)"
  artifacts:
    - path: "exercise-ai/generator_gemini.py"
      provides: "genai.Client with HttpOptions.timeout=30000"
      contains: "HttpOptions|timeout"
    - path: "README.md"
      provides: "Embed contract section for hosts"
      contains: "generate_batch|ConfigError|reserved"
    - path: "exercise-ai/tests/test_encoding.py"
      provides: "cp1252-safe diagnostic / format regressions"
      contains: "cp1252|encode"
  key_links:
    - from: "generator_gemini.get_client"
      to: "types.HttpOptions(timeout=30000)"
      via: "milliseconds per google-genai SDK"
      pattern: "HttpOptions"
    - from: "failover [FAILOVER] line"
      to: "ASCII arrow"
      via: "replace Unicode arrow with ASCII so cp1252 stderr cannot crash mid-hop"
      pattern: "FAILOVER"
  prohibitions:
    - "Do not create CONTRACT.md (README section only)"
    - "Do not unpark packaging / rename"
    - "Do not convert internal prints to logging"
    - "Do not add demo/ (Phase 12)"
---

<objective>
Harden Windows encoding and Gemini HTTP timeout, then document the host embed contract (including reserved flat-module names) in README.

Purpose: Prevent latent demo-day failures and give the host a written contract (D-10…D-12).
Output: Finite Gemini timeout, ASCII-safe diagnostics + CLI stream reconfigure, README contract section, encoding tests.
</objective>

<execution_context>
@C:/Users/Anglo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-service-layer-extraction/11-CONTEXT.md
@.planning/phases/11-service-layer-extraction/11-02-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@exercise-ai/generator_gemini.py
@exercise-ai/failover.py
@exercise-ai/validator.py
@exercise-ai/main.py
@README.md
</context>

<proven_verify_commands>
- Canonical suite: `pytest exercise-ai -q`
- Targeted: `pytest exercise-ai/tests/test_encoding.py exercise-ai/tests/test_generators.py exercise-ai/tests/test_failover.py -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Gemini timeout | `types.HttpOptions(timeout=30000)` on `genai.Client` | Context7 `/googleapis/python-genai`: timeout is **milliseconds**; align magnitude to OpenAI `timeout=30.0` seconds |
| Worst-case README note | Order of several minutes possible: up to `(max_retries+1)` attempts × up to 5 Gemini model fallbacks × one failover hop × 30s HTTP timeout | Honest host guidance; not a hard SLA |
| Reserved names list | Document flat modules published via `sys.path` today: `main`, `models`, `prompts`, `validator`, `generator`, `generator_gemini`, `failover`, `reliability`, `reasoning`, `math_check`, `output_paths`, `wizard`, `token_usage`, `service` | Detection-only; packaging parked (PKG-01) |
| Name-collision helper | README list + optional one-liner detection note; no mandatory runtime self-check required this plan | Agent discretion; keep KISS |
| Encoding | ASCII `->` in runtime diagnostic format strings that previously used Unicode arrows; CLI stdout/stderr `reconfigure(encoding="utf-8", errors="replace")` where supported; avoid dumping full math batches to cp1252 streams on validation failure (truncate/ASCII-safe) | D-10 |

<tasks>

<task type="auto" tdd="true">
  <name>ASCII-safe diagnostics + CLI stream encoding harden</name>
  <reversibility rating="reversible">String/format and stream reconfigure only; no API shape change.</reversibility>
  <files>exercise-ai/failover.py, exercise-ai/validator.py, exercise-ai/main.py, exercise-ai/tests/test_encoding.py, exercise-ai/tests/test_failover.py</files>
  <read_first>exercise-ai/failover.py (FAILOVER print with Unicode arrow), exercise-ai/validator.py (_log_validation_failure), exercise-ai/main.py (format_batch_text, run print path), .planning/research/PITFALLS.md (Pitfall 3 cp1252)</read_first>
  <behavior>
    - [FAILOVER] diagnostic line encodes on a cp1252 stream without UnicodeEncodeError
    - Validation failure logging does not raise UnicodeEncodeError when batch contains radical/math glyphs under cp1252 stderr
    - Existing failover tests that match the FAILOVER line still pass (update expected ASCII form if the arrow changes)
  </behavior>
  <action>
    Per D-10 / EMBED-06: replace Unicode arrows in runtime diagnostic format strings (notably failover hop line and any success log arrows that can hit narrow consoles) with ASCII `->`. Harden `validator._log_validation_failure` so dumping a batch with math glyphs cannot raise `UnicodeEncodeError` on cp1252 (e.g. write via a safe encode path / `errors="replace"` / avoid full pretty dump to a narrow stream). In CLI adapter startup (`main` or `run`), call `reconfigure(encoding="utf-8", errors="replace")` on stdout/stderr when the streams support it. Do not print generated exercise batches from the service path (already true after Plan 01). Add `test_encoding.py` that forces a cp1252 TextIO/stream and asserts diagnostics and format helpers do not raise. Update failover assertions that embedded the Unicode arrow to expect the ASCII form.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_encoding.py exercise-ai/tests/test_failover.py -q</automated>
  </verify>
  <done>cp1252-safe diagnostics; failover tests updated; no success-as-failure from encode errors on covered paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Gemini client finite HttpOptions timeout</name>
  <reversibility rating="reversible">Client constructor option only; same provider behavior with a wall clock.</reversibility>
  <files>exercise-ai/generator_gemini.py, exercise-ai/tests/test_generators.py</files>
  <read_first>exercise-ai/generator_gemini.py (get_client), exercise-ai/generator.py (OpenAI timeout=30.0), Context7 google-genai HttpOptions.timeout (milliseconds)</read_first>
  <behavior>
    - get_client constructs Client with http_options timeout 30000 (ms)
    - Source or unit test pins the finite timeout so it cannot regress to unbounded
  </behavior>
  <action>
    Per D-11 / EMBED-05: update `get_client()` to pass `http_options=types.HttpOptions(timeout=30000)` into `genai.Client` (units are milliseconds per current `google-genai` docs — do not pass `30` thinking it is seconds). Keep existing API-key missing ConfigError/ValueError behavior. Add or extend a test (source contract or mock) that asserts the finite timeout is configured. Do not change OpenAI/Grok clients beyond noting parity in comments if useful.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_generators.py -q</automated>
  </verify>
  <done>Gemini HTTP timeout is finite and tested; aligned in magnitude to OpenAI 30s.</done>
</task>

<task type="auto">
  <name>README embed contract section + reserved module names</name>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <files>README.md</files>
  <read_first>README.md (current structure), .planning/phases/11-service-layer-extraction/11-CONTEXT.md (D-12), .planning/REQUIREMENTS.md (EMBED-07), exercise-ai/service.py (public signature + error class names after Plan 02)</read_first>
  <action>
    Per D-12 / EMBED-07: add a short **Embed / biblioteca** section to `README.md` (not a separate CONTRACT.md). Include: (1) how to put `exercise-ai/` on `sys.path` and call `generate_batch(request) -> ExerciseBatch`; (2) success JSON shape pointing at `ExerciseBatch.model_dump()` fields; (3) error table for **exactly the three catchable subclasses** — `ConfigError`, `InvalidRequestError`, `GenerationFailedError` — with how to branch (`except` each + `.kind` / attrs); do not document bare ValueError as a host branch for validation exhaustion; (4) sequential contract — one generation at a time per process; (5) reserved flat module names list for collision detection while packaging remains parked; (6) Gemini timeout + short worst-case latency note for the host (several minutes possible under retries × model walk × failover × 30s). Mention host may use `redirect_stderr` until LOG-01. Do not claim packaging/`exercise_ai` import works yet.
  </action>
  <verify>
    <automated>pytest exercise-ai -q; $anchors = @('generate_batch','ConfigError','InvalidRequestError','GenerationFailedError'); foreach ($a in $anchors) { if (-not (Select-String -Path README.md -Pattern [regex]::Escape($a) -Quiet)) { throw "README missing anchor: $a" } }; if (-not (Select-String -Path README.md -Pattern 'reserved|reservad' -Quiet)) { throw 'README missing reserved-module anchor' }; if (-not (Select-String -Path README.md -Pattern 'sequential|sequencial|uma gera' -Quiet)) { throw 'README missing sequential-contract anchor' }</automated>
  </verify>
  <done>README documents the host contract with three-subclass error table; content anchors verified; full suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows console / redirected streams | cp1252 may reject math glyphs; encode failures become false negatives |
| Host thread ↔ Gemini HTTP | Unbounded client timeout can pin the host indefinitely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Denial of service | generator_gemini.Client | high | mitigate | Finite HttpOptions.timeout=30000 ms (D-11) |
| T-11-08 | Denial of service / spoofing of failure | cp1252 encode on diagnostics | medium | mitigate | ASCII diagnostics + stream reconfigure + safe validation dump (D-10) |
| T-11-09 | Elevation of privilege | flat module name collision | medium | mitigate | Document reserved names; packaging remains next-milestone lead (PKG-01) |
| T-11-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- Encoding tests pass under simulated cp1252
- Gemini timeout pinned
- README contains generate_batch + three-subclass error table (ConfigError / InvalidRequestError / GenerationFailedError) + reserved names + sequential note
- Select-String anchors on README pass alongside `pytest exercise-ai -q`
</verification>

<success_criteria>
- EMBED-05, EMBED-06, EMBED-07 satisfied
- D-10, D-11, D-12 done; deferred items stay out
</success_criteria>

<output>
Create `.planning/phases/11-service-layer-extraction/11-03-SUMMARY.md` when done
</output>
