---
phase: 11-service-layer-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - exercise-ai/models.py
  - exercise-ai/main.py
  - exercise-ai/service.py
  - exercise-ai/generator.py
  - exercise-ai/generator_gemini.py
  - exercise-ai/failover.py
  - exercise-ai/reliability.py
  - exercise-ai/reasoning.py
  - exercise-ai/token_usage/collector.py
  - exercise-ai/tests/test_service.py
  - exercise-ai/tests/test_models.py
  - exercise-ai/tests/test_reliability.py
autonomous: true
requirements:
  - EMBED-02
  - EMBED-03
  - EMBED-04
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GenerationRequest.quantidade is Field(ge=1, le=40); topico rejects empty; argparse _positive_quantidade still exit-2 with PT message (D-09; EMBED-03)"
    - "Host distinguishes config vs invalid-request vs generation-failed via subclasses of existing ValueError/RuntimeError plus rich attrs (kind / retriable / api_error_kind); validation exhaustion raises InvalidRequestError with kind; CLI except ValueError/RuntimeError still works (D-03; D-04; D-05; EMBED-02)"
    - "After any generate_batch call (including failover env mutation), LLM_PROVIDER and LLM_REASONING_EFFORT are restored to prior presence/absence (D-08; EMBED-04)"
    - "Service path does not write postmortem or fail-error-log; flush_token_usage never masks an in-flight exception with OSError (D-06; D-07)"
  artifacts:
    - path: "exercise-ai/models.py"
      provides: "Domain bound 1–40 + non-empty topico"
      contains: "ge=1|le=40"
    - path: "exercise-ai/service.py"
      provides: "Error subclasses + _scoped_env + generate_batch lifecycle"
      contains: "ConfigError|InvalidRequestError|GenerationFailedError|_scoped_env"
    - path: "exercise-ai/reliability.py"
      provides: "Postmortem write removed from library loop (CLI owns it)"
      contains: "_write_postmortem"
  key_links:
    - from: "service.generate_batch"
      to: "_scoped_env finally"
      via: "always snapshot/restore LLM_PROVIDER and LLM_REASONING_EFFORT"
      pattern: "_scoped_env|LLM_PROVIDER"
    - from: "main.run except ValueError"
      to: "reliability._write_postmortem"
      via: "CLI adapter writes postmortem on validation exhaustion; service does not"
      pattern: "_write_postmortem"
    - from: "flush_token_usage / collector.flush"
      to: "OSError guard"
      via: "try/except OSError around mkdir/open so finally cannot mask primary error"
      pattern: "OSError"
  prohibitions:
    - "Do not add generate_batch kwargs"
    - "Do not convert ~33 internal prints to logging (LOG-01 deferred)"
    - "Do not delete argparse _positive_quantidade"
    - "Do not invent Settings object"
    - "Do not change Gemini timeout or README contract (Plan 03)"
---

<objective>
Add domain bounds, discriminable error subclasses with rich attrs, always-on scoped env restore, and filesystem hygiene (no service postmortem/fail-log; guarded flush).

Purpose: Make the seam host-safe for long-lived processes after the pure move (D-03…D-09).
Output: Updated models/service/raise sites, CLI-owned postmortem, guarded flush, tests for kind + env restore + bounds.
</objective>

<execution_context>
@C:/Users/Anglo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-service-layer-extraction/11-CONTEXT.md
@.planning/phases/11-service-layer-extraction/11-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@exercise-ai/service.py
@exercise-ai/models.py
@exercise-ai/main.py
@exercise-ai/generator.py
@exercise-ai/failover.py
@exercise-ai/reliability.py
@exercise-ai/reasoning.py
@exercise-ai/token_usage/collector.py
</context>

<proven_verify_commands>
- Canonical suite: `pytest exercise-ai -q`
- Targeted: `pytest exercise-ai/tests/test_service.py exercise-ai/tests/test_reliability.py -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Error module | Classes defined in `service.py` (re-export not required) | ARCHITECTURE: no separate errors.py |
| Class names | `ConfigError(ValueError)`, `InvalidRequestError(ValueError)`, `GenerationFailedError(RuntimeError)` | D-03; preserves CLI `except ValueError` / `except RuntimeError` |
| `kind` attr | ASCII string on ConfigError / InvalidRequestError (e.g. `missing_key`, `invalid_retries`, `invalid_reasoning`, `secondary_missing_key`, `validation_exhausted`) | D-04; host branches without PT match |
| API failures | Prefer `GenerationFailedError` where RuntimeError is raised for permanent/exhausted generation; keep `retriable` / `api_error_kind` attrs | D-03/D-04; existing failover eligibility |
| Validation exhaustion | Reliability final-failure **must** raise `InvalidRequestError(..., kind=...)` (ValueError subclass) — never bare `ValueError` | D-03/D-04/EMBED-02; CLI `except ValueError` + postmortem still fire |
| Scoped keys | Always guard `LLM_PROVIDER` and `LLM_REASONING_EFFORT` on every `generate_batch` | D-08; failover mutates provider even when caller did not |
| Postmortem ownership | Stop calling `_write_postmortem` inside `generate_validated_batch`; call it from `main.run` ValueError handler (guarded OSError) | D-06 |
| Flush guard | `try/except OSError` around flush write path (collector and/or service/run finally) | D-07; no new env flag |

## Executor scope note (keep 3 tasks — no 4th plan)

This plan touches ~12 files but stays three tasks on purpose. **One concern per edit batch; do not merge batches:**

1. Task 1 — models + argparse bound only (`models.py` / `main.py` / `test_models.py`).
2. Task 2 — error class definitions + raise-site migration only (config / generation / validation-exhaustion → subclasses); tests for `isinstance` + `.kind`.
3. Task 3 — env restore + FS hygiene (postmortem ownership move, flush OSError guard) only.

Do not open Task-3 files while finishing Task 2 raise sites (except importing the error classes). Prefer small sequential commits per task if the executor workflow allows.

<tasks>

<task type="auto" tdd="true">
  <name>Domain bound 1–40 + non-empty topico</name>
  <reversibility rating="reversible">Pydantic field constraints; argparse check retained unchanged.</reversibility>
  <files>exercise-ai/models.py, exercise-ai/main.py, exercise-ai/tests/test_models.py</files>
  <read_first>exercise-ai/models.py (GenerationRequest), exercise-ai/main.py (_positive_quantidade, _MAX_QUANTIDADE), .planning/phases/11-service-layer-extraction/11-CONTEXT.md (D-09)</read_first>
  <behavior>
    - GenerationRequest(quantidade=0) and quantidade=41 raise ValidationError
    - GenerationRequest(quantidade=1) and 40 accepted
    - empty/whitespace topico rejected
    - argparse _positive_quantidade still rejects out-of-range with exit 2 (existing tests)
  </behavior>
  <action>
    Per D-09 / EMBED-03: change `GenerationRequest.quantidade` to `Field(ge=1, le=40)` (replace `gt=0`). Constrain `topico` to non-empty (e.g. `min_length=1` after strip via validator or Field constraint — keep PT-friendly validation errors). Optionally export `MAX_QUANTIDADE = 40` from models and have main import it for `_MAX_QUANTIDADE` to avoid drift — do not delete `_positive_quantidade`. Create `exercise-ai/tests/test_models.py` (does not exist yet) covering quantidade 0/41 reject, 1/40 accept, and empty topico reject. No service API changes beyond consuming the model.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_main.py -q</automated>
  </verify>
  <done>Domain 1–40 + non-empty topico enforced; argparse bound retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Error subclasses + rich attrs + service/adapter logger</name>
  <reversibility rating="costly">Public exception types become part of the host contract (D-03).</reversibility>
  <files>exercise-ai/service.py, exercise-ai/main.py, exercise-ai/generator.py, exercise-ai/generator_gemini.py, exercise-ai/failover.py, exercise-ai/reliability.py, exercise-ai/reasoning.py, exercise-ai/tests/test_service.py</files>
  <read_first>exercise-ai/generator.py (missing-key ValueError sites ~50,57,65), exercise-ai/failover.py (ensure_secondary_key), exercise-ai/reliability.py (resolve_max_retries raises), exercise-ai/reasoning.py (invalid effort raise), exercise-ai/main.py (_ensure_provider_key), .planning/research/ARCHITECTURE.md (ConfigError table ~311-322)</read_first>
  <behavior>
    - Missing API key / invalid retries / invalid reasoning / secondary missing key raise ConfigError (subclass of ValueError) with `.kind`
    - Reliability validation exhaustion (final failure after retries) raises InvalidRequestError with `.kind` (not bare ValueError)
    - Host can `except ConfigError` / `except InvalidRequestError` and read `.kind` without matching PT message text
    - CLI `except ValueError` / `except RuntimeError` still catch the new subclasses
    - Generation API permanent errors remain RuntimeError-family (`GenerationFailedError`) with `retriable` / `api_error_kind` preserved for failover
  </behavior>
  <action>
    Per D-03, D-04, D-05, EMBED-02: define `ConfigError`, `InvalidRequestError`, and `GenerationFailedError` on the service boundary. Replace config raise sites listed in ARCHITECTURE (generator missing keys, failover secondary key, reliability resolve_max_retries, reasoning invalid effort, main `_ensure_provider_key`) with `ConfigError(..., kind=...)` keeping the same PT message strings. **Pin reliability validation-exhaustion final-failure** to `raise InvalidRequestError(..., kind=...)` (ASCII kind such as `validation_exhausted`); do not leave a bare `ValueError` escape hatch. Map generation RuntimeError construction paths to `GenerationFailedError` where it preserves existing `retriable` / `api_error_kind` attribute tagging and failover eligibility. Add service/adapter logger usage for failure context (do not mechanically convert the ~33 internal stderr prints — LOG-01 deferred). Extend `test_service.py` to assert `isinstance(..., InvalidRequestError)` and `.kind` on the validation-exhaustion path, plus ConfigError `.kind` discrimination for at least one config site. Do not change PT message text that tests already assert. Stay in raise-site ownership only — env restore / postmortem move belong to Task 3.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_service.py exercise-ai/tests/test_reliability.py exercise-ai/tests/test_failover.py -q</automated>
  </verify>
  <done>Three catchable subclasses work for host; validation exhaustion is InvalidRequestError with kind; CLI except branches unchanged in behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Scoped env restore + service FS hygiene + guarded flush</name>
  <reversibility rating="reversible">Scoped save/restore and OSError guards are local safety; no schema change.</reversibility>
  <files>exercise-ai/service.py, exercise-ai/main.py, exercise-ai/reliability.py, exercise-ai/token_usage/collector.py, exercise-ai/tests/test_service.py, exercise-ai/tests/test_reliability.py</files>
  <read_first>exercise-ai/failover.py (_default_switch_provider), exercise-ai/service.py (generate_batch), exercise-ai/reliability.py (_write_postmortem call site), exercise-ai/token_usage/collector.py (flush), .planning/phases/11-service-layer-extraction/11-CONTEXT.md (D-06, D-07, D-08), .planning/research/PITFALLS.md (Pitfalls 2, 4, 16)</read_first>
  <behavior>
    - After generate_batch where failover switches LLM_PROVIDER, env key returns to pre-call value (including absent)
    - Service path on validation exhaustion does not create postmortem file; CLI run() path still does (existing postmortem tests via main.run)
    - When flush cannot write (read-only / OSError), the original in-flight exception type is preserved
  </behavior>
  <action>
    Per D-08 / EMBED-04: wrap every `generate_batch` body in a context manager that always snapshots and restores `LLM_PROVIDER` and `LLM_REASONING_EFFORT` (presence and absence), regardless of caller overrides — failover mutates provider even when the host did not ask. No Settings object.

    Per D-06: remove the postmortem write from the reliability final-failure path so the library/service call does not write it; invoke `_write_postmortem` from `main.run`'s ValueError handler (still OSError-guarded like fail-error-log) so CLI tests that go through `run()` keep seeing the file. Fail-error-log stays CLI-only.

    Per D-07: guard token-usage flush so filesystem errors cannot replace an in-flight exception (try/except OSError around the write in collector.flush and/or the service/run finally callers). No new env flag this phase.

    Add tests: env restore after simulated failover switch; service-path exhaustion leaves no postmortem; optional read-only flush does not mask a raised generation error.
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
  </verify>
  <done>Env always restored; service has no postmortem/fail-log writes; flush cannot mask errors; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host env ↔ service | Failover mutates LLM_PROVIDER; must not leak across calls |
| Library → package filesystem | Postmortem/NDJSON writes can fail or be undesired in host installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | LLM_PROVIDER after failover | high | mitigate | Always-on scoped restore (D-08) |
| T-11-05 | Denial of service | flush/postmortem OSError masking | medium | mitigate | OSError guards; postmortem CLI-only |
| T-11-06 | Information disclosure | error messages / kind | low | mitigate | Rich attrs for host; PT messages unchanged; no secret logging added |
| T-11-SC | Tampering | package installs | low | accept | No new packages |
</threat_model>

<verification>
- Domain bound tests + argparse tests pass
- Host can discriminate ConfigError and InvalidRequestError via isinstance + `.kind` (validation exhaustion pinned)
- Env restore test covers failover mutation
- `pytest exercise-ai -q` green
</verification>

<success_criteria>
- EMBED-02, EMBED-03 (domain half), EMBED-04 satisfied
- D-03…D-09 implemented without kwargs / Settings / LOG-01 conversion
</success_criteria>

<output>
Create `.planning/phases/11-service-layer-extraction/11-02-SUMMARY.md` when done
</output>
