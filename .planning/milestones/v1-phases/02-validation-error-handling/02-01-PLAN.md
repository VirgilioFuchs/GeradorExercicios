---
phase: 02-validation-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
estimate: 45min
files_modified:
  - exercise-ai/validator.py
  - exercise-ai/main.py
  - exercise-ai/generator.py
  - exercise-ai/generator_gemini.py
autonomous: true
requirements:
  - VALD-01
  - VALD-02
  - VALD-03
  - VALD-04
  - VALD-05
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-04
user_setup: []

must_haves:
  truths:
    - "validate_exercise_batch rejects non-ExerciseBatch, missing/empty exercicios, wrong quantity, and whitespace-only enunciado/resposta/explicacao with Portuguese messages that include field paths and expected vs actual where applicable"
    - "Validation failure writes plain PT user text to stderr (print(str(err)) with no wrapper prefixes), plus a detailed [VALIDAÇÃO] log line and raw batch JSON; process exits with code 1 and never retries"
    - "Missing API keys raise ValueError naming both GEMINI_API_KEY and LLM_API_KEY before any API call"
    - "Timeout, rate limit, connection, and auth failures from OpenAI and Gemini map to distinct Portuguese RuntimeError messages; empty/unparseable API payload raises RuntimeError; detailed logs use [API:openai] or [API:gemini]"
    - "On success, stdout is only UTF-8 JSON (ensure_ascii=False); stage labels Gerando… and Validando… go to stderr only; run_demo with mocked generate succeeds without retry and ValueError yields SystemExit(1) with plain stderr"
  artifacts:
    - path: "exercise-ai/validator.py"
      provides: "Semantic post-parse validation with report-mode constant and two-layer failure logging"
      contains: "VALIDATION_REPORT_MODE"
    - path: "exercise-ai/main.py"
      provides: "Stage labels on stderr, plain print(str(err)) user errors (D-05), fail-fast exit 1, stdout JSON-only on success"
      contains: "print(str(val_err)"
    - path: "exercise-ai/generator.py"
      provides: "Unified missing-key message and typed OpenAI API error mapper"
      contains: "map_openai_error"
    - path: "exercise-ai/generator_gemini.py"
      provides: "Exact status/code Gemini error classification table + empty-response RuntimeError"
      contains: "map_gemini_error"
  key_links:
    - from: "exercise-ai/main.py"
      to: "exercise-ai/validator.py"
      via: "validate_exercise_batch(batch, request) after generation"
      pattern: "validate_exercise_batch\\("
    - from: "exercise-ai/main.py"
      to: "exercise-ai/generator.py"
      via: "generate_exercises(request) before validation"
      pattern: "generate_exercises\\(request\\)"
    - from: "exercise-ai/generator.py"
      to: "exercise-ai/generator_gemini.py"
      via: "provider dispatch on gemini"
      pattern: "generator_gemini"
    - from: "exercise-ai/validator.py"
      to: "exercise-ai/models.py"
      via: "ExerciseBatch + GenerationRequest semantic checks"
      pattern: "ExerciseBatch"
---

<objective>
As a developer running the exercise CLI, I want structurally invalid LLM batches and API/config failures rejected with clear Portuguese diagnostics, so that only valid UTF-8 JSON reaches stdout and every failure is documented on stderr without silent exceptions or retries.

Purpose: Harden the Phase 1 pipeline with semantic validation (VALD-*) and explicit API/config error handling (ERR-*) per locked D-01–D-15.
Output: Expanded `validator.py`, mapped errors in `generator.py` / `generator_gemini.py`, and `main.py` stage labels + fail-fast stderr/stdout split.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validation-error-handling/02-CONTEXT.md
@.planning/phases/02-validation-error-handling/02-AI-SPEC.md
@AGENTS.md
@exercise-ai/validator.py
@exercise-ai/main.py
@exercise-ai/generator.py
@exercise-ai/generator_gemini.py
@exercise-ai/models.py
</context>

<proven_verify_commands>
**Reuse Phase 1 path grounding (`sys.path.insert(0, 'exercise-ai')` from repo root) — do not invent alternate package roots.**

- Phase 01 · Task 1: `python -c "import sys; sys.path.insert(0, 'exercise-ai'); from models import DificuldadeEnum, GenerationRequest, Exercise, ExerciseBatch; ..."`
- Phase 01 · Task 2: `python -c "import sys; sys.path.insert(0, 'exercise-ai'); from models import ...; from validator import validate_exercise_batch; ..."`
- Phase 01 · Task 3: `python -c "import sys; sys.path.insert(0, 'exercise-ai'); import main; assert hasattr(main, 'run_demo'); ..."`
</proven_verify_commands>

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: Tracer — semantic validator + two-layer validation errors + main stage labels</name>
  <files>exercise-ai/validator.py, exercise-ai/main.py</files>
  <read_first>
    .planning/phases/02-validation-error-handling/02-CONTEXT.md,
    .planning/phases/02-validation-error-handling/02-AI-SPEC.md,
    exercise-ai/validator.py,
    exercise-ai/main.py,
    exercise-ai/models.py
  </read_first>
  <action>
    Deliver one end-to-end validated failure path: invalid batch → clear PT ValueError → stderr user message + detailed log → exit 1; valid batch still returns and main can print JSON.

    **A. Expand `exercise-ai/validator.py` (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08)**

    1. Keep signature `validate_exercise_batch(batch: ExerciseBatch, request: GenerationRequest) -> ExerciseBatch`. Operate on the already-parsed Pydantic `ExerciseBatch` (D-01). Do not add LangChain or agent frameworks.

    2. Add code constant (editable in source, not env var) near top of module (D-08):
       - `VALIDATION_REPORT_MODE: str = "all"`  # allowed values: `"all"` | `"first_exercise"`
       - Document in a one-line comment that changing this constant switches report behavior.

    3. Implement semantic checks that collect Portuguese error strings:
       - VALD-01 / D-01: if `not isinstance(batch, ExerciseBatch)` → error `"O lote retornado não é uma instância válida de ExerciseBatch."`
       - VALD-02: if `exercicios` attribute missing OR value is not a list → error naming chave `exercicios` ausente/inválida
       - Empty list → error that lista está vazia (keep/clarify existing Phase 1 message)
       - VALD-03 / D-02 / D-07: if `len(batch.exercicios) != request.quantidade` → append message including expected vs actual, e.g. `quantidade incorreta: esperado {request.quantidade} exercícios, recebido {n}`
       - VALD-04 / D-03: for each index `i` and fields `enunciado`, `resposta`, `explicacao`: if `not str(getattr(ex, field, "")).strip()` → append `exercicios[{i}].{field} está vazio` (D-06 path format exact: `exercicios[i].campo`)

    4. Report mode behavior (D-08):
       - `"all"`: collect every quantity/field error, then fail once with all messages.
       - `"first_exercise"`: after quantity check, on the first exercise that has any empty field, stop collecting further exercise field errors (still include the quantity error if present). Implement this explicitly with a break/return from the exercise loop.

    5. On any collected errors (VALD-05 / D-04 / D-05):
       - Build user-facing message as plain Portuguese joined with `"; "` — **no** `[VALIDAÇÃO]` prefix in the exception text (D-05).
       - Before raising, write detailed log to `sys.stderr`:
         - line starting with `[VALIDAÇÃO]` summarizing the failure
         - then raw batch JSON via `json.dumps(batch.model_dump() if isinstance(batch, ExerciseBatch) else str(batch), ensure_ascii=False, indent=2)` (D-04). Never log API keys.
       - `raise ValueError(user_message)`.

    6. If no errors, return `batch` unchanged (do not mutate field strings in the returned model unless needed; emptiness uses `.strip()` only for the check).

    **B. Update `exercise-ai/main.py` (D-13, D-14, D-15)**

    1. Inside `run_demo` try block, print stage labels to **stderr only** (D-15):
       - Before `generate_exercises`: `print("Gerando…", file=sys.stderr)`
       - Before `validate_exercise_batch`: `print("Validando…", file=sys.stderr)`
    2. Keep success path: `json.dumps(..., indent=2, ensure_ascii=False)` to **stdout only** — no stage labels or logs on stdout (D-14).
    3. Keep fail-fast error boundary (D-13): catch `ValueError` / `RuntimeError` / generic `Exception` → print plain message to stderr → `sys.exit(1)`. No retry loops.
    4. **D-05 REQUIRED (non-negotiable):** user-facing stderr must be plain Portuguese exception text with **no** wrapper prefixes.
       - `except ValueError as val_err:` → **exactly** `print(str(val_err), file=sys.stderr)` then `sys.exit(1)`
       - `except RuntimeError as run_err:` → **exactly** `print(str(run_err), file=sys.stderr)` then `sys.exit(1)`
       - `except Exception as exc:` → `print(str(exc), file=sys.stderr)` then `sys.exit(1)` (same spirit)
       - **FORBIDDEN:** wrappers such as `"Erro de configuração ou validação: ..."`, `"Erro na execução da geração: ..."`, or `"Erro inesperado: ..."`. Prefixes belong only on detailed log lines (`[VALIDAÇÃO]`, `[API:...]`) emitted by validator/generators — never on the main user line.
  </action>
  <verify>
    <automated>python -c "exec(r'''
import sys, io, contextlib
sys.path.insert(0, \"exercise-ai\")
import validator
from validator import validate_exercise_batch
from models import Exercise, ExerciseBatch, GenerationRequest, DificuldadeEnum

req = GenerationRequest(topico=\"Frações\", dificuldade=DificuldadeEnum.FACIL, quantidade=2)
good = ExerciseBatch(exercicios=[
    Exercise(enunciado=\"a\", resposta=\"b\", explicacao=\"c\"),
    Exercise(enunciado=\"d\", resposta=\"e\", explicacao=\"f\"),
])
assert validate_exercise_batch(good, req) is good

# VALD-01 non-instance
buf = io.StringIO()
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch({\"exercicios\": []}, req)
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        assert \"ExerciseBatch\" in str(e)
        assert \"[VALIDAÇÃO]\" not in str(e)
assert \"[VALIDAÇÃO]\" in buf.getvalue()

# empty exercicios
buf = io.StringIO()
empty = ExerciseBatch(exercicios=[])
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(empty, req)
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        assert \"vazia\" in str(e).lower() or \"exercicios\" in str(e).lower()
        assert \"[VALIDAÇÃO]\" not in str(e)

# malformed exercicios (not a list)
buf = io.StringIO()
malformed = ExerciseBatch(exercicios=[
    Exercise(enunciado=\"a\", resposta=\"b\", explicacao=\"c\"),
    Exercise(enunciado=\"d\", resposta=\"e\", explicacao=\"f\"),
])
object.__setattr__(malformed, \"exercicios\", \"nao-lista\")
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(malformed, req)
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        assert \"exercicios\" in str(e).lower()
        assert \"[VALIDAÇÃO]\" not in str(e)

# quantity + stderr raw JSON (D-04/D-05/D-07)
buf = io.StringIO()
bad_qty = ExerciseBatch(exercicios=[Exercise(enunciado=\"a\", resposta=\"b\", explicacao=\"c\")])
msg = None
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(bad_qty, req)
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        msg = str(e)
assert msg and \"esperado 2\" in msg and \"recebido 1\" in msg
assert \"[VALIDAÇÃO]\" not in msg
stderr = buf.getvalue()
assert \"[VALIDAÇÃO]\" in stderr and \"\\\"enunciado\\\"\" in stderr

# whitespace field path
buf = io.StringIO()
bad_ws = ExerciseBatch(exercicios=[
    Exercise(enunciado=\"ok\", resposta=\"  \", explicacao=\"ok\"),
    Exercise(enunciado=\"ok\", resposta=\"ok\", explicacao=\"ok\"),
])
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(bad_ws, req)
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        assert \"exercicios[0].resposta\" in str(e)
        assert not str(e).startswith(\"[VALIDAÇÃO]\")

# REPORT MODE all vs first_exercise (D-08)
multi = ExerciseBatch(exercicios=[
    Exercise(enunciado=\" \", resposta=\"b\", explicacao=\"c\"),
    Exercise(enunciado=\" \", resposta=\"e\", explicacao=\"f\"),
])
validator.VALIDATION_REPORT_MODE = \"all\"
buf = io.StringIO()
err_all = None
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(multi, req)
    except ValueError as e:
        err_all = str(e)
assert err_all and \"exercicios[0].enunciado\" in err_all and \"exercicios[1].enunciado\" in err_all

validator.VALIDATION_REPORT_MODE = \"first_exercise\"
buf = io.StringIO()
err_first = None
with contextlib.redirect_stderr(buf):
    try:
        validate_exercise_batch(multi, req)
    except ValueError as e:
        err_first = str(e)
assert err_first and \"exercicios[0].enunciado\" in err_first
assert \"exercicios[1].enunciado\" not in err_first
validator.VALIDATION_REPORT_MODE = \"all\"

# main D-05 + stage labels
msrc = open(\"exercise-ai/main.py\", encoding=\"utf-8\").read()
assert \"Gerando\" in msrc and \"Validando\" in msrc
assert \"print(str(val_err), file=sys.stderr)\" in msrc
assert \"print(str(run_err), file=sys.stderr)\" in msrc
assert \"Erro de configuração ou validação\" not in msrc
assert \"Erro na execução da geração\" not in msrc
print(\"Tracer validation path verified\")
''')"</automated>
    <fails_when>AssertionError; ImportError; invalid batch accepted; missing expected/recebido or field path; ValueError text contains [VALIDAÇÃO]; stderr missing [VALIDAÇÃO] or raw JSON enunciado; all-mode missing second exercise error; first_exercise mode still includes exercicios[1]; main retains forbidden wrapper prefixes or missing print(str(...)); missing stage labels; non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `validate_exercise_batch` enforces isinstance, `exercicios` presence/non-empty/list type, exact `quantidade`, and strip-nonempty fields (D-01, D-02, D-03; VALD-01–VALD-04)
    - Failure messages include `exercicios[i].campo` and expected vs actual for quantity (D-06, D-07; VALD-05)
    - `VALIDATION_REPORT_MODE` `"all"` reports all field errors; `"first_exercise"` stops after first failing exercise (D-08) — verified behaviorally
    - On validation failure, stderr receives `[VALIDAÇÃO]` + raw JSON; ValueError text has **no** `[VALIDAÇÃO]` prefix (D-04, D-05)
    - `main.py` uses `print(str(val_err), file=sys.stderr)` / `print(str(run_err), file=sys.stderr)` with **no** wrapper prefixes (D-05); stage labels wired in source (behavioral `run_demo` exercise is in Task 2 verify)
    - No retry on validation failure (D-13)
  </acceptance_criteria>
  <done>Invalid batches raise path-specific ValueError with detailed [VALIDAÇÃO] stderr dump; valid batches pass; main stage labels and D-05 print(str) wired</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: Expand — API/config error mapping (OpenAI + Gemini) and fail-fast CLI contract</name>
  <files>exercise-ai/generator.py, exercise-ai/generator_gemini.py, exercise-ai/main.py</files>
  <read_first>
    .planning/phases/02-validation-error-handling/02-CONTEXT.md,
    .planning/phases/02-validation-error-handling/02-AI-SPEC.md,
    exercise-ai/generator.py,
    exercise-ai/generator_gemini.py,
    exercise-ai/main.py,
    exercise-ai/validator.py
  </read_first>
  <action>
    Expand the tracer with provider error mapping so ERR-01–ERR-04 are satisfied for both OpenAI and Gemini. Do not implement retry, failover, progress %, or pytest suites (deferred).

    **A. Unified missing-key messaging (D-09, D-10, ERR-01)**

    1. In `generator.py` `_resolve_provider()`: when neither key is set, raise `ValueError` in plain PT that names **both** `GEMINI_API_KEY` and `LLM_API_KEY` and mentions `.env` (D-10). User message stays unprefixed; optional detailed log line with `[API]` is allowed but not required for this config path.
    2. Align `get_client()` in `generator.py` and `generator_gemini.py` so missing/empty key messages also mention both variable names (or clearly point users to configure the relevant key while still naming both). Prefer one consistent Portuguese sentence used across call sites.
    3. Never log or print the actual API key value (LOG-02 / security constraint).

    **B. OpenAI typed error mapping in `generator.py` (D-09, D-11, D-12, ERR-02, ERR-03, ERR-04)**

    1. Import OpenAI exception types: `APITimeoutError`, `RateLimitError`, `APIConnectionError`, `AuthenticationError`, and base `OpenAIError` (names as provided by installed `openai` SDK; adjust import path if SDK exports differ, but map these four categories).
    2. Extract a testable helper `map_openai_error(exc: BaseException) -> RuntimeError` (name may be `_map_openai_error`) that:
       - writes one detailed stderr line prefixed `[API:openai]` including `type(exc).__name__` and `str(exc)` (D-09)
       - returns a plain-PT `RuntimeError` (no `[API:...]` in the message text) per mapping:
         - `APITimeoutError` → must contain `tempo esgotado` (or equivalent timeout wording)
         - `RateLimitError` → must contain `limite` (rate limit wording)
         - `APIConnectionError` → must contain `conexão` or `rede`
         - `AuthenticationError` → must contain `autenticação` or `chave`
         - other `OpenAIError` → clear generic PT with useful detail (ERR-04)
       - caller raises with `raise mapped from exc`
    3. Inside `_generate_with_openai`, catch the specific types (or `OpenAIError`) and `raise map_openai_error(exc) from exc`.
    4. Keep empty/refusal paths as `RuntimeError` (D-12 / ERR-03) — required behavior:
       - If `message.refusal` is truthy → log `[API:openai]` then `raise RuntimeError(...)` plain PT mentioning recusa/refusal
       - If `message.parsed is None` → log `[API:openai]` then `raise RuntimeError(...)` plain PT mentioning estrutura/parse (Phase 1 wording OK)
       - No retry on these paths
    5. Preserve `from exc` chaining where an underlying exception exists.

    **C. Gemini typed error mapping in `generator_gemini.py` (D-09, D-11, D-12, ERR-02, ERR-03)**

    1. Extract a testable helper `map_gemini_error(exc: BaseException) -> RuntimeError` (name may be `_map_gemini_error`) that logs `[API:gemini]` to stderr (type + str) then returns plain-PT `RuntimeError` using this **exact classification table** (D-11 / ERR-02):

       | Priority | Condition | User-facing RuntimeError message (exact text) |
       |----------|-----------|-----------------------------------------------|
       | 1 | Prefer SDK subclass/type when available (timeout / rate-limit / auth / connection named subclasses of `genai_errors.APIError`) | Same four category messages as rows 2–5 |
       | 2 | `status_code` or `code` in `{401, 403}` (int or str) | `Falha de autenticação na API do Gemini. Verifique GEMINI_API_KEY.` |
       | 3 | `status_code` or `code` in `{429}` | `Limite de requisições da API do Gemini atingido. Tente novamente mais tarde.` |
       | 4 | `status_code` or `code` in `{408, 504}` | `Tempo esgotado ao chamar a API do Gemini.` |
       | 5 | Message/body indicates connection/network (case-insensitive: `connection`, `connect`, `network`, `conexão`, `rede`, `unreachable`, `dns`) | `Erro de conexão com a API do Gemini. Verifique a rede.` |
       | 6 | Else (generic fallback) | `Erro na chamada à API do Gemini.` |

       Implementation notes:
       - **Priority 1 FIRST:** inspect `google.genai.errors` for named subclasses matching timeout/rate/auth/connection; if present, `isinstance` map to the four exact messages **before** duck-typing `status_code`/`code`.
       - If installed `google-genai` lacks such named subclasses: do **not** invent fake classes. Add a module docstring or comment block starting with exactly `GEMINI_ERROR_CLASSIFICATION:` that states `priority-1 N/A on this SDK — use status_code table only` and lists the four categories plus status codes `401/403`, `429`, `408/504` (and network/generic fallback). Then implement rows 2–6 only.
       - Always keep a `GEMINI_ERROR_CLASSIFICATION:` block documenting the full table (even when priority-1 subclasses exist).
       - Resolve code via `getattr(exc, "status_code", None) or getattr(exc, "code", None)`; coerce to `int` when possible.
       - Message source: `str(exc)` and/or `getattr(exc, "message", "")`.

    2. In `generate_exercises`, `except genai_errors.APIError as exc: raise map_gemini_error(exc) from exc`.
    3. Empty response → `RuntimeError("A API do Gemini retornou uma resposta vazia.")` (D-12 / ERR-03); log `[API:gemini]` before raise.
    4. Unparseable/invalid JSON or schema → `RuntimeError("Não foi possível interpretar a estrutura de exercícios retornada pelo Gemini.")` with `from exc` (ERR-03); log `[API:gemini]` including raw `response.text` when safe (never keys).

    **D. CLI contract polish in `main.py` (D-05, D-13, D-14, ERR-04)**

    1. Enforce D-05: `print(str(val_err), file=sys.stderr)` / `print(str(run_err), file=sys.stderr)` / `print(str(exc), file=sys.stderr)` — **no** wrapper prefixes.
    2. `sys.exit(1)` on failure — no retry (`generate_exercises` at most once per `run_demo`).
    3. Success path writes only the JSON string to stdout.
    4. Do not introduce structured log files, progress percentages, or pytest (Phase 3).

    **Discretion (from CONTEXT):** helpers may live in each generator file; do not add agent frameworks or new packages.
  </action>
  <verify>
    <automated>python -c "exec(r'''
import sys, os, io, json, contextlib
from types import SimpleNamespace
from unittest.mock import MagicMock, patch
sys.path.insert(0, \"exercise-ai\")
import generator
import generator_gemini
import main
from models import Exercise, ExerciseBatch, GenerationRequest, DificuldadeEnum

# --- missing keys (ERR-01 / D-10) ---
old = {k: os.environ.pop(k) for k in (\"LLM_API_KEY\", \"GEMINI_API_KEY\", \"LLM_PROVIDER\") if k in os.environ}
try:
    try:
        generator._resolve_provider()
        raise AssertionError(\"expected ValueError\")
    except ValueError as e:
        err = str(e)
        assert \"GEMINI_API_KEY\" in err and \"LLM_API_KEY\" in err
finally:
    os.environ.update(old)

map_oai = getattr(generator, \"map_openai_error\", None) or getattr(generator, \"_map_openai_error\", None)
assert callable(map_oai), \"generator must expose map_openai_error or _map_openai_error\"

from openai import APITimeoutError, RateLimitError, APIConnectionError, AuthenticationError

def _make(cls, msg=\"x\"):
    for args, kwargs in (
        ((msg,), {}),
        ((), {\"message\": msg}),
        ((), {\"message\": msg, \"response\": None, \"body\": None}),
        ((), {\"message\": msg, \"request\": None, \"body\": None}),
    ):
        try:
            return cls(*args, **kwargs)
        except TypeError:
            continue
    class _E(cls):
        def __init__(self):
            Exception.__init__(self, msg)
    return _E()

def _oai_msg(exc):
    buf = io.StringIO()
    with contextlib.redirect_stderr(buf):
        out = map_oai(exc)
    assert isinstance(out, RuntimeError)
    assert \"[API:\" not in str(out)
    assert \"[API:openai]\" in buf.getvalue()
    return str(out).lower()

samples = [
    (APITimeoutError, (\"tempo\", \"esgotado\")),
    (RateLimitError, (\"limite\",)),
    (APIConnectionError, (\"conex\", \"rede\")),
    (AuthenticationError, (\"autentic\", \"chave\")),
]
for cls, needles in samples:
    text = _oai_msg(_make(cls))
    assert any(n in text for n in needles), (cls, text)

map_gem = getattr(generator_gemini, \"map_gemini_error\", None) or getattr(generator_gemini, \"_map_gemini_error\", None)
assert callable(map_gem), \"generator_gemini must expose map_gemini_error or _map_gemini_error\"

def _gem_msg(obj):
    buf = io.StringIO()
    with contextlib.redirect_stderr(buf):
        out = map_gem(obj)
    assert isinstance(out, RuntimeError)
    assert \"[API:\" not in str(out)
    assert \"[API:gemini]\" in buf.getvalue()
    return str(out)

assert _gem_msg(SimpleNamespace(code=401, message=\"no\")) == \"Falha de autenticação na API do Gemini. Verifique GEMINI_API_KEY.\"
assert _gem_msg(SimpleNamespace(status_code=403, message=\"x\")) == \"Falha de autenticação na API do Gemini. Verifique GEMINI_API_KEY.\"
assert _gem_msg(SimpleNamespace(code=429, message=\"x\")) == \"Limite de requisições da API do Gemini atingido. Tente novamente mais tarde.\"
assert _gem_msg(SimpleNamespace(code=408, message=\"x\")) == \"Tempo esgotado ao chamar a API do Gemini.\"
assert _gem_msg(SimpleNamespace(code=504, message=\"x\")) == \"Tempo esgotado ao chamar a API do Gemini.\"
assert _gem_msg(SimpleNamespace(code=500, message=\"connection reset\")) == \"Erro de conexão com a API do Gemini. Verifique a rede.\"
assert _gem_msg(SimpleNamespace(code=500, message=\"boom\")) == \"Erro na chamada à API do Gemini.\"

# Gemini priority-1 subclasses OR documented N/A
from google.genai import errors as genai_errors
api_error_base = getattr(genai_errors, \"APIError\", Exception)
named = []
for attr in dir(genai_errors):
    obj = getattr(genai_errors, attr)
    if not isinstance(obj, type) or not issubclass(obj, Exception):
        continue
    if obj is api_error_base:
        continue
    low = attr.lower()
    if any(k in low for k in (\"timeout\", \"rate\", \"auth\", \"permission\", \"connect\", \"network\")):
        named.append(obj)
src_gem = open(\"exercise-ai/generator_gemini.py\", encoding=\"utf-8\").read()
assert \"GEMINI_ERROR_CLASSIFICATION:\" in src_gem
assert all(code in src_gem for code in (\"401\", \"403\", \"429\", \"408\", \"504\"))
assert any(k in src_gem.lower() for k in (\"timeout\", \"tempo\"))
assert any(k in src_gem.lower() for k in (\"rate\", \"limite\"))
assert any(k in src_gem.lower() for k in (\"auth\", \"autentic\"))
assert any(k in src_gem.lower() for k in (\"connect\", \"conex\", \"rede\", \"network\"))
if not named:
    assert \"priority-1 N/A on this SDK\" in src_gem
else:
    mapped_ok = False
    for sub in named:
        try:
            msg = _gem_msg(_make(sub))
            if any(s in msg for s in (\"autenticação\", \"Limite\", \"Tempo esgotado\", \"conexão\", \"Erro na chamada\")):
                mapped_ok = True
                break
        except Exception:
            continue
    assert mapped_ok or \"GEMINI_ERROR_CLASSIFICATION:\" in src_gem

# ERR-03 Gemini empty + unparseable
os.environ[\"GEMINI_API_KEY\"] = \"dummy-key-for-test\"
os.environ.pop(\"LLM_API_KEY\", None)
req1 = GenerationRequest(topico=\"x\", dificuldade=DificuldadeEnum.FACIL, quantidade=1)
fake_client = MagicMock()

fake_resp_empty = MagicMock(); fake_resp_empty.text = \"\"
fake_client.models.generate_content.return_value = fake_resp_empty
buf = io.StringIO()
with contextlib.redirect_stderr(buf):
    try:
        with patch.object(generator_gemini, \"get_client\", return_value=fake_client):
            generator_gemini.generate_exercises(req1)
        raise AssertionError(\"expected RuntimeError empty\")
    except RuntimeError as e:
        assert \"vazia\" in str(e).lower()
assert \"[API:gemini]\" in buf.getvalue()

fake_resp_bad = MagicMock(); fake_resp_bad.text = \"{not-json\"
fake_client.models.generate_content.return_value = fake_resp_bad
buf = io.StringIO()
with contextlib.redirect_stderr(buf):
    try:
        with patch.object(generator_gemini, \"get_client\", return_value=fake_client):
            generator_gemini.generate_exercises(req1)
        raise AssertionError(\"expected RuntimeError unparseable\")
    except RuntimeError as e:
        assert \"interpretar\" in str(e).lower() or \"estrutura\" in str(e).lower()
assert \"[API:gemini]\" in buf.getvalue()

# ERR-03 OpenAI refusal + parsed is None
os.environ[\"LLM_API_KEY\"] = \"dummy-openai\"
os.environ.pop(\"GEMINI_API_KEY\", None)
os.environ.pop(\"LLM_PROVIDER\", None)
req3 = GenerationRequest(topico=\"x\", dificuldade=DificuldadeEnum.FACIL, quantidade=1)

def _mock_completion(refusal=None, parsed=None):
    message = MagicMock()
    message.refusal = refusal
    message.parsed = parsed
    choice = MagicMock(); choice.message = message
    completion = MagicMock(); completion.choices = [choice]
    client = MagicMock()
    client.beta.chat.completions.parse.return_value = completion
    return client

buf = io.StringIO()
with contextlib.redirect_stderr(buf):
    try:
        with patch.object(generator, \"get_client\", return_value=_mock_completion(refusal=\"não posso\", parsed=None)):
            generator._generate_with_openai(req3)
        raise AssertionError(\"expected RuntimeError refusal\")
    except RuntimeError as e:
        low = str(e).lower()
        assert \"recus\" in low or \"refusal\" in low or \"não\" in low
assert \"[API:openai]\" in buf.getvalue()

buf = io.StringIO()
with contextlib.redirect_stderr(buf):
    try:
        with patch.object(generator, \"get_client\", return_value=_mock_completion(refusal=None, parsed=None)):
            generator._generate_with_openai(req3)
        raise AssertionError(\"expected RuntimeError parsed None\")
    except RuntimeError as e:
        low = str(e).lower()
        assert \"estrutura\" in low or \"obter\" in low or \"parse\" in low
assert \"[API:openai]\" in buf.getvalue()

# --- CLI run_demo behavioral (D-05, D-13, D-14, D-15) ---
demo_batch = ExerciseBatch(exercicios=[
    Exercise(enunciado=\"e1\", resposta=\"r1\", explicacao=\"x1\"),
    Exercise(enunciado=\"e2\", resposta=\"r2\", explicacao=\"x2\"),
    Exercise(enunciado=\"e3\", resposta=\"r3\", explicacao=\"x3\"),
])
out, err = io.StringIO(), io.StringIO()
with patch.object(main, \"generate_exercises\", return_value=demo_batch) as gen:
    with contextlib.redirect_stdout(out), contextlib.redirect_stderr(err):
        main.run_demo()
assert gen.call_count == 1, \"no retry: generate_exercises must be called once\"
stdout = out.getvalue()
stderr = err.getvalue()
assert \"Gerando\" not in stdout and \"Validando\" not in stdout
parsed = json.loads(stdout)
assert \"exercicios\" in parsed and len(parsed[\"exercicios\"]) == 3
assert \"Gerando\" in stderr and \"Validando\" in stderr

out2, err2 = io.StringIO(), io.StringIO()
with patch.object(main, \"generate_exercises\", side_effect=ValueError(\"exercicios[0].resposta está vazio\")) as gen2:
    with contextlib.redirect_stdout(out2), contextlib.redirect_stderr(err2):
        try:
            main.run_demo()
            raise AssertionError(\"expected SystemExit\")
        except SystemExit as se:
            assert se.code == 1
assert gen2.call_count == 1
assert out2.getvalue() == \"\"
err_text = err2.getvalue()
assert \"exercicios[0].resposta está vazio\" in err_text
assert \"Erro de configuração ou validação\" not in err_text
assert \"Erro na execução da geração\" not in err_text

msrc = open(\"exercise-ai/main.py\", encoding=\"utf-8\").read()
assert \"sys.exit(1)\" in msrc and \"ensure_ascii=False\" in msrc
assert \"print(str(val_err), file=sys.stderr)\" in msrc
assert \"print(str(run_err), file=sys.stderr)\" in msrc
print(\"API error mapping and CLI contract verified\")
''')"</automated>
    <fails_when>AssertionError; ImportError; missing dual key names; mapper helpers missing; OpenAI categories not distinct; Gemini table messages not exact; missing GEMINI_ERROR_CLASSIFICATION docs (or subclass path); Gemini empty/unparseable not RuntimeError; OpenAI refusal/parsed-None not RuntimeError; run_demo success pollutes stdout or retries; ValueError path missing SystemExit(1) or still uses wrapper prefixes; non-zero exit of the verify script</fails_when>
  </verify>
  <acceptance_criteria>
    - Missing-key path names both `GEMINI_API_KEY` and `LLM_API_KEY` (D-10; ERR-01)
    - OpenAI `map_openai_error` maps timeout/rate-limit/connection/auth to distinct PT RuntimeErrors with `[API:openai]` detail logs (D-09, D-11; ERR-02, ERR-04)
    - OpenAI refusal and `parsed is None` raise RuntimeError with `[API:openai]` log (ERR-03)
    - Gemini `map_gemini_error` implements classification table; priority-1 subclasses when present, else `GEMINI_ERROR_CLASSIFICATION:` documents `priority-1 N/A on this SDK` + codes 401/403/429/408/504 (D-11)
    - Gemini empty and unparseable responses raise RuntimeError with `[API:gemini]` logs (D-12; ERR-03)
    - `main.run_demo` success: stdout parseable JSON only, stderr has Gerando/Validando, generate called once; ValueError → SystemExit(1), plain stderr, no wrapper prefix, generate once (D-05, D-13, D-14, D-15)
    - No secrets in logs; no pytest suite / structured log files / failover / progress % / retry added
  </acceptance_criteria>
  <done>Both providers emit typed PT errors with prefixed detail logs; ERR-03 empty/unparseable/refusal covered; CLI run_demo mocked success/failure verified; missing keys name both env vars</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Re-run Task 1 and Task 2 `<automated>` verifies from repository root using `sys.path.insert(0, 'exercise-ai')` (Phase 1 grounded path)
- [ ] Confirm Task 1 verify covers validator behaviors (paths, report modes, `[VALIDAÇÃO]`+raw JSON)
- [ ] Confirm Task 2 verify exercises `main.run_demo` with mocks (success JSON-only stdout; ValueError → SystemExit(1); generate once; no wrapper prefixes)
- [ ] Confirm Gemini `GEMINI_ERROR_CLASSIFICATION:` docs + status-code table; subclass-first when SDK has named types
- [ ] Confirm ERR-03: Gemini empty + unparseable; OpenAI refusal + parsed is None
- [ ] Confirm no retry loop, no pytest suite files, no LangChain/CrewAI
</verification>

<success_criteria>
- Roadmap Phase 2 success criteria covered: reject invalid structure/quantity/empty fields; specific failure messages; clear missing-key error; network/timeout/rate-limit reported without silencing
- Requirements VALD-01–VALD-05 and ERR-01–ERR-04 satisfied
- Locked decisions D-01–D-15 implemented (report mode via code constant; two-layer stderr; abort exit 1; stdout JSON only)
- Deferred items explicitly not built: retry/RELY, failover, progress %, microservices, pytest suite, structured log files
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-error-handling/02-01-SUMMARY.md`
</output>

## Artifacts this phase produces

- `exercise-ai/validator.py` — Semantic validation: quantity, strip-empty fields, field paths, `VALIDATION_REPORT_MODE`, `[VALIDAÇÃO]` + raw JSON on failure
- `exercise-ai/main.py` — Stage labels on stderr; `print(str(err))` plain user errors (D-05); fail-fast exit 1; stdout JSON-only UTF-8
- `exercise-ai/generator.py` — Dual-key missing message; `map_openai_error` + refusal/parsed-None RuntimeError + `[API:openai]` logs
- `exercise-ai/generator_gemini.py` — `GEMINI_ERROR_CLASSIFICATION:` + `map_gemini_error`; empty/unparseable RuntimeError + `[API:gemini]` logs
