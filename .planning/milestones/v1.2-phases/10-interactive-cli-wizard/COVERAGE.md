# API Coverage — Interactive CLI Wizard

> Full coverage by default. Opt-outs are explicit, reasoned decisions.

No external API integration: Phase 10 is CLI UX (`gerar` + tips) over the existing generation entrypoint; no new SDKs or endpoints.
