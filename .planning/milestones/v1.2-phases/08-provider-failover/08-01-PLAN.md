---
phase: 08-provider-failover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/failover.py
  - exercise-ai/generator.py
  - exercise-ai/generator_gemini.py
  - exercise-ai/main.py
  - exercise-ai/tests/test_failover.py
  - README.md
autonomous: true
requirements:
  - FAILOVER-01
  - FAILOVER-02
  - FAILOVER-03
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On eligible primary API failure (timeout / rate / connection / generic), the lab retries once on the other of OpenAI ↔ Gemini with the same GenerationRequest and max_retries (D-01; D-04; D-07; D-09; FAILOVER-01)"
    - "Failover never runs on auth, model refusal, retriable invalid LLM response, or validation/math exhaustion — those fail through on the primary path (D-02; D-08; FAILOVER-01 negative)"
    - "Envelope only re-invokes generate_validated_batch — no nested second RELY/math loop and no changes to reliability regen rules (D-07; D-08; FAILOVER-02)"
    - "At most one provider switch per CLI run; if secondary also fails, propagate that error after logging both attempts (D-09)"
    - "Primary = --provider / LLM_PROVIDER when openai|gemini, else _resolve_provider; secondary = the other of the pair; Grok primary skips failover entirely (D-05; D-06)"
    - "Missing secondary key aborts with clear PT message — no half-failover (D-03)"
    - "Explicit --provider openai|gemini does not disable failover (D-10); stderr shows [FAILOVER] primary → secondary (reason) without secrets (D-11; FAILOVER-03)"
    - "Offline mocked tests cover eligible failover, auth/math/Grok/missing-key negatives, and successful --out after secondary (D-12)"
  artifacts:
    - path: "exercise-ai/failover.py"
      provides: "Thin envelope + eligibility helper + injectable provider switch"
      contains: "generate_with_failover|is_failover_eligible"
    - path: "exercise-ai/tests/test_failover.py"
      provides: "Mocked D-12 matrix — no live LLM"
      contains: "FAILOVER|failover"
    - path: "README.md"
      provides: "Operator note: OpenAI↔Gemini failover; Grok excluded; no --no-failover flag"
      contains: "failover|FAILOVER"
  key_links:
    - from: "main.run"
      to: "failover.generate_with_failover"
      via: "replaces direct generate_validated_batch call; same request + max_retries"
      pattern: "generate_with_failover"
    - from: "failover.generate_with_failover"
      to: "reliability.generate_validated_batch"
      via: "call primary; on eligible failure switch provider once; call again (D-07)"
      pattern: "generate_validated_batch"
    - from: "generator.map_openai_compatible_error / generator_gemini.map_gemini_error / refusal"
      to: "exc.api_error_kind"
      via: "tag permanent API errors so auth/refusal ≠ timeout/rate/conn/generic (D-01; D-02)"
      pattern: "api_error_kind"
    - from: "failover.is_failover_eligible"
      to: "RuntimeError attributes retriable + api_error_kind"
      via: "eligible only when retriable is False and kind in timeout|rate_limit|connection|generic"
      pattern: "is_failover_eligible"
  prohibitions:
    - "No Grok in the failover chain (D-04; D-06; CONTEXT deferred)"
    - "No failover on math/validation failure (D-08; deferred)"
    - "No --no-failover CLI flag; --provider does not lock out failover (D-10; deferred)"
    - "No token/USD metrics driving failover (Phase 9 CONTEXT deferred; AI-SPEC)"
    - "No second nested RELY/math loop inside the envelope (FAILOVER-02; D-07)"
    - "No LangChain/CrewAI/new AI framework; no new PyPI deps (AI-SPEC Framework None)"
    - "No secrets, raw API bodies, or keys in [FAILOVER] / stderr (D-11; LOG-02)"
    - "No live LLM in pytest; CI remains pytest exercise-ai -q without API secrets (D-12)"
---

## Phase Goal

If the primary OpenAI or Gemini provider fails with an eligible availability/API error, automatically retry once on the other provider by reusing `generate_validated_batch` — with clear `[FAILOVER]` stderr and offline mocked tests — without a second RELY/math loop and without pulling Grok into the chain.

## Spec probe

- CONTEXT locked D-01…D-12; deferred ideas not planned (Grok chain, math failover, `--no-failover`, token-driven routing).
- AI-SPEC: Selected Framework **None** (thin envelope only).
- RESEARCH: SKIPPED (`research_enabled=false`) — plan from CONTEXT + current code.
- Specless phase: COVERAGE.md states no new external API — INTEGRATE = envelope over existing SDKs; OPT-OUT N/A beyond that note.
- Discretion filled below (envelope module, `api_error_kind`, injectable switch).

<objective>
Deliver FAILOVER-01, FAILOVER-02, and FAILOVER-03 as one production-quality failover slice: discriminate auth vs availability on mapped permanent API errors, thin envelope that re-calls `generate_validated_batch` once on the OpenAI↔Gemini peer, `[FAILOVER]` stderr without secrets, wired from `main.run`, with injectable hooks for offline tests.

Purpose: Lab resilience when the primary provider is unavailable/rate-limited/timed-out/connection-failed, without inventing a third retry architecture or Grok chain.
Output: `exercise-ai/failover.py`, mapper `api_error_kind` tags, `main.run` wire, `tests/test_failover.py`, README note.

Decision coverage: D-01 through D-12 (cited in tasks). Deferred not planned: Grok failover chain, math/validation failover, `--no-failover`, token-driven routing.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-provider-failover/08-CONTEXT.md
@.planning/phases/08-provider-failover/08-AI-SPEC.md
@.planning/phases/08-provider-failover/COVERAGE.md
@.cursor/rules/10-python.mdc
@.cursor/rules/20-ai-engineering.mdc
@skills/python-ai-engineering/SKILL.md
@exercise-ai/generator.py
@exercise-ai/generator_gemini.py
@exercise-ai/reliability.py
@exercise-ai/main.py
@README.md
</context>

<proven_verify_commands>
**Reuse prior phase grounding from repo root.**

- Canonical suite (Phase 3 / Phase 7 / README): `pytest exercise-ai -q`
- This plan adds targeted `pytest exercise-ai/tests/test_failover.py -q` alongside that suite command.
</proven_verify_commands>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `exercise-ai/failover.py` | module | Envelope `generate_with_failover`, `is_failover_eligible`, peer resolve, secondary-key guard, injectable switch |
| Mapper tags in `generator.py` / `generator_gemini.py` | wiring | `api_error_kind` on permanent mapped RuntimeErrors (+ refusal) |
| Hook in `main.py` | wiring | `run()` calls envelope instead of bare `generate_validated_batch` |
| `exercise-ai/tests/test_failover.py` | tests | Mocked D-12 matrix — eligible / negative / Grok / missing key / `--out` |
| README | docs | OpenAI↔Gemini auto-failover; Grok excluded; no `--no-failover` |

**Public functions / symbols (discretion — names may adjust slightly to match local style, roles fixed):**

- `is_failover_eligible(exc: BaseException) -> bool` — True only for permanent API errors with `api_error_kind` in `{timeout, rate_limit, connection, generic}` (D-01; D-02)
- `failover_peer(provider: str) -> str | None` — `openai`↔`gemini`; `None` for `grok` / unknown (D-04; D-06)
- `ensure_secondary_key(provider: str) -> None` — raise clear PT `ValueError` if secondary key missing (D-03); mirror `_ensure_provider_key` messages
- `generate_with_failover(request, max_retries, *, generate_fn=..., resolve_provider=..., switch_provider=...) -> ExerciseBatch` — thin envelope (D-07; D-09)
- Default `switch_provider(name)` sets `os.environ["LLM_PROVIDER"] = name` so existing `_resolve_provider` / generators pick the peer; tests inject a callable (or stub `generate_fn`) instead of relying only on env mutation
- Module does **not** nest a new regen loop — only up to two top-level `generate_fn` calls (primary, then optional secondary)

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | Auto OpenAI↔Gemini failover; reuse RELY path; clear logs; mocks | 08-01 | COVERED | ROADMAP success 1–5 |
| REQ | FAILOVER-01 | Retriable/unavailability → other provider | 08-01 T1–T2 | COVERED | D-01 eligible set |
| REQ | FAILOVER-02 | No second math/RELY loop | 08-01 T1 | COVERED | re-call only |
| REQ | FAILOVER-03 | Logs which provider tried/used | 08-01 T1–T3 | COVERED | `[FAILOVER]` |
| CONTEXT | D-01 | Failover on timeout/rate/conn/generic | 08-01 T1 | COVERED | |
| CONTEXT | D-02 | No failover auth/refusal/invalid/math | 08-01 T1–T2 | COVERED | `api_error_kind` |
| CONTEXT | D-03 | Missing secondary key → PT fail | 08-01 T1–T2 | COVERED | |
| CONTEXT | D-04 | Pair OpenAI↔Gemini only | 08-01 T1 | COVERED | |
| CONTEXT | D-05 | Primary from --provider / resolve | 08-01 T1 | COVERED | |
| CONTEXT | D-06 | Grok → no failover | 08-01 T2 | COVERED | |
| CONTEXT | D-07 | Thin envelope; same request/max_retries | 08-01 T1 | COVERED | costly |
| CONTEXT | D-08 | No provider switch on validation/math | 08-01 T2 | COVERED | |
| CONTEXT | D-09 | At most one switch; secondary failure propagates | 08-01 T1–T2 | COVERED | |
| CONTEXT | D-10 | --provider does not disable failover | 08-01 T2–T3 | COVERED | |
| CONTEXT | D-11 | `[FAILOVER]` stderr; no secrets | 08-01 T1–T3 | COVERED | |
| CONTEXT | D-12 | Offline mock test matrix | 08-01 T2–T3 | COVERED | |
| AI-SPEC | — | Framework None; no live LLM; no token routing | 08-01 | COVERED | prohibitions |
| COVERAGE | — | No new external API | 08-01 | COVERED | envelope only |

Deferred (not planned): Grok chain; math/validation failover; `--no-failover`; token/cost routing.

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Envelope location | New module `exercise-ai/failover.py`; `main.run` calls `generate_with_failover` instead of `generate_validated_batch` directly | KISS SRP: RELY loop stays untouched (FAILOVER-02); discretion in CONTEXT between main vs reliability — prefer dedicated thin module over growing `reliability.py` |
| Auth vs availability | Extend `_permanent_api_error` (or mapper sites) to set `api_error_kind`: `auth` \| `timeout` \| `rate_limit` \| `connection` \| `generic`; refusal uses `api_error_kind="refusal"` with `retriable=False` | Message-substring matching is fragile; attribute is explicit for D-01/D-02 and unit-testable |
| Eligibility helper | `is_failover_eligible(exc)`: `getattr(retriable) is False` **and** `api_error_kind in {timeout, rate_limit, connection, generic}` | Invalid (`retriable=True`) and auth/refusal never eligible |
| Provider switch | Default: set `os.environ["LLM_PROVIDER"]`; injectable `switch_provider: Callable[[str], None]` + injectable `generate_fn` / `resolve_provider` on the envelope | CONTEXT agent discretion for testability; avoid awkward env-only tests |
| Secondary key check | `ensure_secondary_key` in `failover.py` (same PT wording spirit as `main._ensure_provider_key`) | Fail before second `generate_fn` call (D-03) |
| Log line shape | `[FAILOVER] {primary} → {secondary} ({reason})` where `reason` is short `api_error_kind` (e.g. `timeout`) — never exception bodies/keys | D-11; FAILOVER-03 |
| Optional success hint | After successful secondary call, one stderr line naming the provider used (reuse existing `[API:…]` patterns where already printed; do not dump secrets) | ROADMAP success criterion 3 |
| Do not modify | `validate_exercise_batch` / `math_check` regen rules; Gemini in-provider model fallback list remains orthogonal | D-08; YAGNI |
| No new PyPI deps | stdlib + existing openai/google-genai only | AI-SPEC Framework None |

<tasks>

<task type="tracer">
  <name>End-to-end failover — eligibility tags + envelope + OpenAI→Gemini success</name>
  <reversibility rating="costly">Envelope + `api_error_kind` become the lab contract for when providers switch; changing eligibility later affects operator expectations and tests.</reversibility>
  <files>exercise-ai/failover.py, exercise-ai/generator.py, exercise-ai/generator_gemini.py, exercise-ai/main.py, exercise-ai/tests/test_failover.py</files>
  <read_first>exercise-ai/generator.py (map_openai_compatible_error, _permanent_api_error, refusal, _resolve_provider), exercise-ai/generator_gemini.py (map_gemini_error), exercise-ai/reliability.py (generate_validated_batch — do not nest a second loop), exercise-ai/main.py (run + _ensure_provider_key), .planning/phases/08-provider-failover/08-CONTEXT.md (D-01…D-11)</read_first>
  <action>
  Ship the thinnest production-quality vertical slice through every failover layer (D-07, FAILOVER-01..03). No Grok chain. No math failover. No `--no-failover`. No token-driven routing. No new AI framework / PyPI packages.

  **1) Discriminate auth vs availability (D-01; D-02)**  
  - Extend permanent-error construction so mapped RuntimeErrors carry `api_error_kind`:
    - OpenAI-compatible mapper: timeout→`timeout`, RateLimit→`rate_limit`, connection→`connection`, AuthenticationError→`auth`, other permanent API→`generic`.
    - Gemini mapper: 401/403→`auth`, 429→`rate_limit`, 408/504→`timeout`, network hints→`connection`, else permanent→`generic`.
    - Model refusal path (already `_permanent_api_error`): set `api_error_kind="refusal"` (not eligible).
  - Keep `retriable=False` for all of the above; keep `invalid_llm_response` as `retriable=True` **without** failover eligibility.
  - Do not break existing tests that only assert `retriable` / PT messages — additive attribute only.

  **2) Module `exercise-ai/failover.py` (D-03…D-07; D-09)**  
  - `is_failover_eligible(exc)` per Discretion table.
  - `failover_peer(provider)`: openai↔gemini only; grok/other → `None` (D-04; D-06).
  - `ensure_secondary_key(secondary)`: if key missing, raise PT `ValueError` (D-03) — do not call secondary generate.
  - `generate_with_failover(request, max_retries, *, generate_fn=generate_validated_batch, resolve_provider=_resolve_provider, switch_provider=default_env_switch)`:
    1. Resolve primary (D-05).
    2. If peer is `None` (Grok): call `generate_fn` once and return (D-06) — still document in Task 2 tests.
    3. Call `generate_fn(request, max_retries)` on primary.
    4. On exception: if **not** `is_failover_eligible`, re-raise (D-02; D-08 surfaces as non-eligible ValueError/RuntimeError from RELY).
    5. If eligible: log `[FAILOVER] {primary} → {secondary} ({api_error_kind})` to stderr without secrets (D-11); `ensure_secondary_key`; `switch_provider(secondary)`; call `generate_fn` **once more** with the **same** `request` and `max_retries` (D-07; D-09).
    6. If secondary fails, propagate (do not flip back) (D-09).
  - Default switch sets `LLM_PROVIDER` so existing generators keep working; injectables exist for tests (discretion).

  **3) Wire `main.run`**  
  - Replace direct `generate_validated_batch(...)` with `generate_with_failover(...)` (same `request` / resolved `max_retries`). Do not change argparse; `--provider` still sets env before `run` and does **not** disable failover (D-10 — covered in later tasks). Preserve token `begin_run` / `finally flush` behavior from Phase 9.

  **4) Tracer tests in `exercise-ai/tests/test_failover.py`**  
  - Mock/fake `generate_fn`: first call raises permanent timeout-tagged error (`retriable=False`, `api_error_kind="timeout"`); second returns a valid `ExerciseBatch`.
  - Assert: exactly two `generate_fn` calls; same request object (or equal fields) and same `max_retries`; stderr contains `[FAILOVER] openai → gemini (timeout)` (or primary/secondary as arranged); no API key substrings.
  - Assert mapper unit: AuthenticationError → `api_error_kind=="auth"` and `is_failover_eligible` is False; timeout → eligible True.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_failover.py -q</automated>
    <fails_when>non-zero exit, failed/errors in summary, or collection finds zero tests in test_failover.py</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
  </verify>
  <acceptance_criteria>
    - Permanent API mappers expose `api_error_kind`; auth ≠ timeout for eligibility (D-01; D-02).
    - `generate_with_failover` reuses `generate_validated_batch` (or injectable) twice at most on eligible failure (D-07; D-09; FAILOVER-02).
    - `main.run` goes through the envelope.
    - Tracer test proves OpenAI→Gemini-style success path + `[FAILOVER]` without secrets (FAILOVER-01; FAILOVER-03).
  </acceptance_criteria>
  <done>
  Tracer proves eligible API failure → one peer retry via existing RELY entrypoint with tagged errors and offline tests (D-01…D-07, D-09, D-11 partial; FAILOVER-01..03 slice).
  </done>
</task>

<task type="auto">
  <name>D-12 matrix — negatives, Grok skip, missing key, Gemini→OpenAI, one-switch</name>
  <reversibility rating="reversible">Tests and eligibility edge cases; easy to extend without changing on-disk contracts.</reversibility>
  <files>exercise-ai/failover.py, exercise-ai/tests/test_failover.py, exercise-ai/main.py</files>
  <read_first>exercise-ai/failover.py (from Task 1), exercise-ai/tests/test_reliability.py (permanent vs retriable patterns), exercise-ai/main.py (_ensure_provider_key), 08-CONTEXT.md D-02 D-03 D-06 D-08 D-09 D-10 D-12</read_first>
  <action>
  Complete the offline behavioral matrix (D-12) without live LLM. Expand `test_failover.py` (and envelope helpers if gaps remain):

  **Eligible bidirectional (D-01; D-04; D-05; FAILOVER-01)**  
  - Gemini primary + rate_limit/connection/generic → OpenAI secondary success (symmetric to tracer).
  - Confirm `switch_provider` invoked once with peer name; at most one switch (D-09).

  **No failover (D-02; D-08)**  
  - Auth-tagged permanent error → single `generate_fn` call; exception propagates; no `[FAILOVER]` line.
  - Refusal-tagged permanent error → no failover.
  - Retriable invalid response (`retriable=True`) raised from `generate_fn` → no failover (RELY already handled inside real `generate_validated_batch`; here assert envelope does not treat it as eligible).
  - Validation/math-style `ValueError` from `generate_fn` → no failover / no provider switch (D-08).

  **Grok (D-06)**  
  - When `resolve_provider` returns `grok`, exactly one `generate_fn` call even if it raises an otherwise-eligible timeout; no peer switch; no `[FAILOVER]`.

  **Missing secondary key (D-03)**  
  - Eligible primary failure + secondary key absent → clear PT `ValueError` (message mentions the missing key / `.env`); second `generate_fn` **not** called.

  **Secondary also fails (D-09)**  
  - Eligible primary failure + secondary raises → propagate secondary error; do not attempt a third provider or flip back.

  **`--provider` does not disable failover (D-10)**  
  - With primary forced to `openai` (as CLI would via env), eligible failure still switches to `gemini` when key present (injectable).

  Keep injectables preferred over brittle global env-only tests where awkward. Do not invent `--no-failover`. Do not add Grok as tertiary. Do not drive decisions from token usage collector.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_failover.py -q</automated>
    <fails_when>non-zero exit, failed/errors, or tests omit auth/math/Grok/missing-key/bidirectional cases required by this task</fails_when>
    <automated>python -c "from pathlib import Path; t=Path('exercise-ai/failover.py').read_text(encoding='utf-8'); assert 'is_failover_eligible' in t; assert 'generate_with_failover' in t; print('failover module OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or failover.py missing required symbols</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
  </verify>
  <acceptance_criteria>
    - Offline tests cover eligible failover both directions and all D-02/D-03/D-06/D-08/D-09 negatives.
    - Grok never enters the failover pair.
    - Missing secondary key fails with PT message before a second generate call.
    - Explicit primary openai|gemini still allows peer failover (D-10).
  </acceptance_criteria>
  <done>
  D-12 behavioral matrix locked in mocks; FAILOVER-01 negatives and D-02…D-10 edges falsifiable in CI (D-12).
  </done>
</task>

<task type="auto">
  <name>CLI --out success path + README operator notes + redaction check</name>
  <reversibility rating="reversible">Docs and integration-style test; easy to edit.</reversibility>
  <files>exercise-ai/tests/test_failover.py, README.md</files>
  <read_first>exercise-ai/tests/test_main.py (run / --out patterns), exercise-ai/main.py (run success path), README.md (provider / pytest sections), 08-CONTEXT.md D-11 D-12, 08-AI-SPEC.md</read_first>
  <action>
  Close operator-facing and integration gaps (D-11; D-12; FAILOVER-03):

  **Integration-style test (mocked generators, no live LLM)**  
  - Drive `main.run` (or thin harness around it) with injectable/monkeypatched envelope dependencies so: primary eligible failure → secondary returns a valid batch → `--out` / success path writes JSON as today (resolve_success_out_path behavior preserved).
  - Assert stderr `[FAILOVER]` present and does **not** contain typical secret substrings / env key values (reuse LOG-02 redaction spirit from `test_logging_security.py`).
  - Confirm token flush in `finally` still runs (no regression of Phase 9 wiring) — at least that `run` completes without error when flush is stubbed/real collector empty.

  **README (short)**  
  - Document automatic OpenAI ↔ Gemini failover on timeout/rate/connection/generic API errors.
  - Explicitly state: no failover on auth; Grok is single-provider (no chain); `--provider` does not disable failover; there is no `--no-failover` flag in this phase.
  - Point operators to stderr `[FAILOVER]` tag; keep `pytest exercise-ai -q` as the offline test command.
  - Do not document token-driven routing or Grok tertiary failover.

  Confirm full suite green offline.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_failover.py -q</automated>
    <fails_when>non-zero exit, failed/errors, or tests omit --out success / redaction assertions</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
    <automated>python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); assert 'failover' in t.lower() or 'FAILOVER' in t; assert 'pytest exercise-ai -q' in t; print('README failover note OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or README missing failover / pytest mention</fails_when>
  </verify>
  <acceptance_criteria>
    - Successful secondary path still writes `--out` JSON under the existing success routing.
    - `[FAILOVER]` logs have no secrets.
    - README documents OpenAI↔Gemini failover, Grok exclusion, and offline pytest.
    - Full `pytest exercise-ai -q` stays green without API secrets.
  </acceptance_criteria>
  <done>
  Operator docs and D-12 `--out`/redaction coverage complete; FAILOVER-03 verification path is CI-safe (D-11; D-12).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider SDK exceptions → mappers | Untrusted status/body shapes; must map to typed PT errors + short `api_error_kind` only |
| Failover envelope → stderr | Operator visibility of provider switch; must not leak keys or raw bodies |
| Envelope → `LLM_PROVIDER` / switch hook | Process env mutation for peer selection; tests inject hooks |
| Secondary key check → operator message | Config failure; mention key **name**, never key **value** |
| Tests → mocked `generate_fn` | Must not require live LLM or committed secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `[FAILOVER]` stderr | critical | mitigate | Log only provider ids + short `api_error_kind`; never `str(exc)` bodies or env secrets (D-11; LOG-02). Assert redaction in tests (D-12). |
| T-08-02 | Information Disclosure | Secondary missing-key ValueError | high | mitigate | PT message names `LLM_API_KEY` / `GEMINI_API_KEY` only — never values (D-03). |
| T-08-03 | Denial of Service | Unbounded provider flipping | medium | mitigate | Hard cap: one switch per run (D-09); Grok excluded (D-06). |
| T-08-04 | Elevation of Privilege / Misconfig | Auth treated as availability | high | mitigate | `api_error_kind=auth` never eligible (D-02); tests assert no failover on auth. |
| T-08-05 | Tampering | Unexpected second RELY loop | medium | mitigate | Envelope only re-invokes existing `generate_validated_batch`; no nested regen invent (FAILOVER-02; D-07). |
| T-08-SC | Tampering | pip installs | low | accept | No new PyPI packages; stdlib + existing SDKs only (AI-SPEC). |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_failover.py -q` covers eligible failover, auth/math/Grok/missing-key negatives, bidirectional peer, `--out`, redaction.
- `pytest exercise-ai -q` remains green offline (prior verify command).
- Manual spot-check (optional operator): force primary timeout with both keys present and observe `[FAILOVER]` + successful generation — not required for plan automation.
- README mentions OpenAI↔Gemini failover and Grok exclusion.
</verification>

<success_criteria>
- FAILOVER-01: Eligible primary API failure automatically tries the OpenAI↔Gemini peer.
- FAILOVER-02: Peer attempt reuses `generate_validated_batch` — no second nested RELY/math loop.
- FAILOVER-03: Stderr `[FAILOVER]` (and provider-used signals) identify tried/used providers without secrets; mocked tests cover the path.
- D-01…D-12 honored; deferred ideas remain out of scope.
- No Grok chain; no `--no-failover`; no token-driven routing; no live LLM in tests.
</success_criteria>

<output>
Create `.planning/phases/08-provider-failover/08-01-SUMMARY.md` when done
</output>
