---
phase: 15-cli-wizard-batch-plan-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - exercise-ai/wizard.py
  - exercise-ai/tests/test_wizard.py
  - README.md
autonomous: true
requirements:
  - UX-01
  - UX-02
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Wizard asks three band counts then quantidade; builds request via plan_ux (D-01..D-03, D-08)"
    - "Soft-warn on stdout for sum/qty issues without hard-blocking before GenerationRequest (D-02)"
    - "README documents --plano and wizard band flow briefly (D-09); offline wizard tests green (D-10)"
  artifacts:
    - path: "exercise-ai/wizard.py"
      provides: "Band prompts + plan_ux wiring in run_wizard"
      contains: "facil|medio|dificil|plan_ux|build_request_kwargs"
    - path: "README.md"
      provides: "Short --plano / wizard band note"
      contains: "--plano|wizard|plano"
  key_links:
    - from: "wizard band answers"
      to: "GenerationRequest"
      via: "plan_ux.build_request_kwargs"
      pattern: "build_request_kwargs|GenerationRequest"
  prohibitions:
    - "Do not change demo UI (already shipped Phase 14)"
    - "Do not add pedagogical tipo de raciocínio"
    - "Do not redesign RELY/service"
---

<objective>
Update interactive wizard `gerar` to collect band counts and build the same request shapes as argparse/demo (UX-01), plus a short README note and offline wizard tests (D-09, D-10).

Purpose: Close Phase 15 operator UX so CLI, wizard, demo, and service share one plan contract.
Output: Updated wizard + README + tests; `pytest exercise-ai -q` green.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cli-wizard-batch-plan-ux/15-CONTEXT.md
@.planning/phases/15-cli-wizard-batch-plan-ux/15-01-PLAN.md
@exercise-ai/plan_ux.py
@exercise-ai/wizard.py
@README.md
</context>

<proven_verify_commands>
- `pytest exercise-ai/tests/test_wizard.py -q`
- `pytest exercise-ai -q`
</proven_verify_commands>

<tasks>

<task type="auto">
  <name>Task 1: Wizard band UX</name>
  <files>exercise-ai/wizard.py, exercise-ai/tests/test_wizard.py</files>
  <action>
    Replace scalar dificuldade ask with three integer band prompts (defaults 0). Keep quantidade ask. Soft-warn then `build_request_kwargs` → `GenerationRequest`. Update `WizardAnswers` as needed. Extend offline wizard tests with injected input sequences for mixed and uniform.
  </action>
  <verify>pytest exercise-ai/tests/test_wizard.py -q</verify>
  <done>Wizard mixed/uniform paths covered offline</done>
</task>

<task type="auto">
  <name>Task 2: README touch</name>
  <files>README.md</files>
  <action>
    Add a short section or bullets: `--plano F,M,D` example; wizard now asks band counts; link that demo/CLI/wizard share GenerationRequest. Do not invent packaging docs.
  </action>
  <verify>grep -n "--plano" README.md</verify>
  <done>README mentions --plano and wizard bands</done>
</task>

</tasks>
