---
phase: 15-cli-wizard-batch-plan-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/plan_ux.py
  - exercise-ai/main.py
  - exercise-ai/tests/test_plan_ux.py
  - exercise-ai/tests/test_cli_plano.py
autonomous: true
requirements:
  - UX-02
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "Shared helper maps band counts to GenerationRequest kwargs per D-07/D-08 (mixed→plano, uniform→legacy)"
    - "Argparse accepts --plano F,M,D; builds request via the helper; legacy --dificuldade path unchanged when --plano absent (D-04, D-05)"
    - "Offline tests cover mixed, uniform-via-plano, parse errors, and XOR with conflicting flags as decided"
  artifacts:
    - path: "exercise-ai/plan_ux.py"
      provides: "build_request_kwargs(facil, medio, dificil, quantidade_field) + parse_plano_csv"
      contains: "build_request_kwargs|parse_plano"
    - path: "exercise-ai/main.py"
      provides: "--plano flag wired into GenerationRequest construction"
      contains: "--plano|plan_ux"
  key_links:
    - from: "--plano F,M,D"
      to: "GenerationRequest(plano=...) or legacy dificuldade"
      via: "plan_ux.build_request_kwargs"
      pattern: "build_request_kwargs|PlanoDificuldade"
  prohibitions:
    - "Do not add --facil/--medio/--dificil flags (D-06)"
    - "Do not change wizard yet (Plan 15-02)"
    - "Do not always-send plano for uniform (D-07)"
    - "Do not change service/RELY/prompts"
---

<objective>
Add shared band→`GenerationRequest` mapping and argparse `--plano F,M,D` so scripts can author mixed/uniform batches with the same payload rules as the demo (UX-02 / D-04..D-08).

Purpose: Close the scripted half of Phase 15 before interactive wizard work.
Output: `plan_ux.py` + argparse wiring + offline tests; `pytest exercise-ai -q` green.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-cli-wizard-batch-plan-ux/15-CONTEXT.md
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md
@exercise-ai/models.py
@exercise-ai/main.py
@demo/app.js
</context>

<proven_verify_commands>
- `pytest exercise-ai/tests/test_plan_ux.py exercise-ai/tests/test_cli_plano.py -q`
- `pytest exercise-ai -q`
</proven_verify_commands>

<tasks>

<task type="auto">
  <name>Task 1: plan_ux helper</name>
  <files>exercise-ai/plan_ux.py, exercise-ai/tests/test_plan_ux.py</files>
  <action>
    Implement `parse_plano_csv("F,M,D")` and `build_request_kwargs(facil, medio, dificil, quantidade_field)` returning kwargs for `GenerationRequest`:
    - 2+ positive bands → `plano=PlanoDificuldade(...)`, `quantidade=sum`
    - exactly 1 positive band → `dificuldade=that band`, `quantidade=that count` (ignore field qty for payload qty per D-07)
    - zero bands → raise ValueError with clear PT/EN message
    Soft-warn helper optional (print) for qty≠sum — used by wizard later.
  </action>
  <verify>pytest exercise-ai/tests/test_plan_ux.py -q</verify>
  <done>Helper covers mixed/uniform/zero; unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: argparse --plano</name>
  <files>exercise-ai/main.py, exercise-ai/tests/test_cli_plano.py</files>
  <action>
    Add `--plano` to `build_parser`. In CLI main path, if `--plano` set, parse via plan_ux and construct `GenerationRequest(**kwargs, materia=..., topico=...)`. If absent, keep existing dificuldade+quantidade path. Document in help epilog with examples.
  </action>
  <verify>pytest exercise-ai/tests/test_cli_plano.py -q</verify>
  <done>--plano mixed and uniform paths green; legacy path unchanged</done>
</task>

</tasks>
