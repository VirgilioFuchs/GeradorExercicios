---
phase: 10-interactive-cli-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/wizard.py
  - exercise-ai/main.py
  - exercise-ai/reasoning.py
  - exercise-ai/tests/test_wizard.py
  - exercise-ai/tests/test_reasoning.py
  - README.md
autonomous: true
requirements:
  - WIZ-01
  - WIZ-02
  - WIZ-03
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "First CLI token `gerar` opens the Portuguese interactive wizard on a TTY; without `gerar`, existing argparse (required `--out`, flags) is unchanged for CI/scripts (D-01; D-02; WIZ-01; WIZ-03)"
    - "Non-TTY stdin fails fast with a clear PT message and does not hang on input() (D-03)"
    - "Question order is exactly: tipo/tópico → matéria → dificuldade → quantidade → provedor → reasoning → nome JSON; tips appear under each prompt on stdout (D-04; D-10; WIZ-01)"
    - "Tipo de exercício maps only to `topico` (no new schema field); max-retries is never asked (D-05; D-06)"
    - "Empty Enter accepts demo defaults (same as argparse defaults today) except JSON path, which re-prompts until a non-empty path (D-07; D-11; WIZ-02)"
    - "Reasoning is always asked; Enter → medium; global DEFAULT_REASONING_EFFORT / env default is medium (D-08; WIZ-02)"
    - "Reasoning tip notes Grok/Gemini honor level; OpenAI gpt-4o-mini may ignore (D-09)"
    - "Wizard answers map into GenerationRequest + provider env + reasoning env + out path and call the same `main.run` entry used by argparse (whatever pipeline `run` uses at execute time — currently `generate_with_failover`; do not re-implement failover) (D-14; WIZ-02)"
    - "No profiles UX; tests mock input()/isatty; no live LLM; CI stays on argparse (D-12; D-13; WIZ-03)"
  artifacts:
    - path: "exercise-ai/wizard.py"
      provides: "Interactive Q&A collector with tips, defaults, TTY guard, validation/re-prompt"
      contains: "collect_wizard_answers|run_wizard"
    - path: "exercise-ai/main.py"
      provides: "`gerar` dispatch before argparse; thin call into wizard then run()"
      contains: "gerar"
    - path: "exercise-ai/reasoning.py"
      provides: "DEFAULT_REASONING_EFFORT = medium (D-08)"
      contains: "DEFAULT_REASONING_EFFORT"
    - path: "exercise-ai/tests/test_wizard.py"
      provides: "Mocked input/isatty tests; mapping to run kwargs; argparse path untouched"
      contains: "gerar|input"
  key_links:
    - from: "main.main (argv first token gerar)"
      to: "wizard.collect_wizard_answers / run_wizard"
      via: "strip gerar token; TTY check; collect answers"
      pattern: "gerar"
    - from: "wizard answers"
      to: "main.run(GenerationRequest, out_path=...)"
      via: "same run entry as argparse; set LLM_PROVIDER / LLM_REASONING_EFFORT when chosen"
      pattern: "run\\("
    - from: "reasoning.DEFAULT_REASONING_EFFORT"
      to: "wizard Enter default + argparse omit default"
      via: "aligned medium (D-08)"
      pattern: "medium"
  prohibitions:
    - "No GUI / web form (CONTEXT deferred)"
    - "No saved profiles / profile (--) UX (D-12)"
    - "Do not ask max-retries in wizard (D-05); use existing RELY_MAX_RETRIES / run default"
    - "Do not replace or remove argparse path (D-02; WIZ-03)"
    - "Do not re-implement Phase 8 failover inside wizard — call existing main.run only"
    - "No live LLM in pytest; no real TTY required in CI (D-13)"
    - "No new AI frameworks / LangChain / agent loops (AI-SPEC Framework None)"
    - "No new PyPI deps — stdlib input() + existing argparse"
---

## Phase Goal

Add Portuguese interactive CLI wizard via first token `gerar`: tip-backed Q&A maps into the existing generation pipeline (`GenerationRequest`, provider, reasoning, `--out` / `run`), align global reasoning default to `medium`, keep argparse for CI — without GUI, profiles, max-retries prompts, or failover re-implementation.

## Spec probe

- CONTEXT locked D-01…D-15; deferred ideas not planned.
- AI-SPEC: Selected Framework **None** (stdlib `input` / argparse only).
- RESEARCH: SKIPPED (operator: research not necessary) — plan from CONTEXT + code only.
- Specless phase: no SPEC Edge Coverage — COVERAGE.md notes no external API; INTEGRATE N/A.
- Discretion filled below (module name, return shape, invalid-value re-prompt, provider empty = auto-detect).

<objective>
Deliver WIZ-01, WIZ-02, and WIZ-03 as one production-quality UX slice: `exercise-ai/wizard.py` + `gerar` dispatch in `main.py`, tips + defaults + JSON re-prompt, global reasoning default `medium`, offline mocked tests, README note.

Purpose: Let the lab operator generate exercises through a guided PT conversation without breaking CI argparse automation.
Output: `wizard.py`, thin `main` branch, `DEFAULT_REASONING_EFFORT=medium`, `tests/test_wizard.py`, README + reasoning test updates.

Decision coverage: D-01 through D-15 (cited in tasks). Deferred not planned: GUI, profiles, max-retries in wizard, replace argparse, Phase 8 failover logic inside wizard.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-interactive-cli-wizard/10-CONTEXT.md
@.planning/phases/10-interactive-cli-wizard/10-AI-SPEC.md
@.planning/phases/10-interactive-cli-wizard/COVERAGE.md
@.planning/seeds/SEED-004-interactive-cli-wizard.md
@.cursor/rules/10-python.mdc
@.cursor/rules/20-ai-engineering.mdc
@exercise-ai/main.py
@exercise-ai/reasoning.py
@exercise-ai/models.py
@README.md
</context>

<proven_verify_commands>
**Reuse prior phase grounding from repo root.**

- Canonical suite (Phase 3 / Phase 7 / README / Phase 9): `pytest exercise-ai -q`
- This plan adds targeted `pytest exercise-ai/tests/test_wizard.py -q` alongside that suite command.
</proven_verify_commands>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `exercise-ai/wizard.py` | module | Interactive collector: TTY guard, 7 prompts + tips, defaults, re-prompt for `--out` |
| `exercise-ai/main.py` | wiring | Detect `gerar` before argparse; call wizard → `run()` |
| `exercise-ai/reasoning.py` | config | `DEFAULT_REASONING_EFFORT = "medium"` (D-08); docstring/help align |
| `exercise-ai/tests/test_wizard.py` | tests | Mock `input` / `isatty`; assert mapping; no LLM |
| `exercise-ai/tests/test_reasoning.py` | tests | Update default assertion `low` → `medium` |
| `README.md` | docs | Document `gerar` wizard + reasoning default `medium` |

**Public functions / symbols (discretion — roles fixed; names may match existing style):**

- `collect_wizard_answers() -> WizardAnswers` — run the 7-question flow; raise/sys.exit-friendly error if non-TTY
- `WizardAnswers` — dataclass or NamedTuple: `materia`, `topico`, `dificuldade`, `quantidade`, `provider` (`str | None`), `reasoning` (`str`), `out_path` (`str`)
- `is_interactive_stdin() -> bool` — `sys.stdin.isatty()` (injectable/mockable in tests)
- Optional `run_wizard_and_build_request() -> tuple[GenerationRequest, str, str | None]` — thin helper used by `main` if it keeps mapping cleaner

Reuse `main` constants `_DEFAULT_MATERIA`, `_DEFAULT_TOPICO`, `_DEFAULT_DIFICULDADE`, `_DEFAULT_QUANTIDADE`, `_MAX_QUANTIDADE` (import from main or shared module constants — prefer import from `main` or duplicate-as-literals matching those values; do not invent different demo defaults). Prefer importing defaults from `main` to avoid drift (discretion: if circular import, move defaults to a tiny shared constant block in `main` and import into wizard, or pass defaults into `collect_wizard_answers`).

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | `gerar` wizard PT Q&A → existing pipeline; argparse kept; medium default | 10-01 | COVERED | ROADMAP success 1–5 |
| REQ | WIZ-01 | `gerar` + tips + locked sequence | 10-01 T1 | COVERED | D-01, D-04, D-10 |
| REQ | WIZ-02 | Map to pipeline; Enter defaults; JSON required; reasoning medium | 10-01 T1–T2 | COVERED | D-07, D-08, D-11 |
| REQ | WIZ-03 | Argparse for CI; mock input tests; no live LLM | 10-01 T2 | COVERED | D-02, D-13 |
| CONTEXT | D-01 | First token `gerar` opens wizard | 10-01 T1 | COVERED | |
| CONTEXT | D-02 | Without `gerar`, argparse unchanged | 10-01 T1–T2 | COVERED | |
| CONTEXT | D-03 | Non-TTY fail clear PT | 10-01 T1 | COVERED | |
| CONTEXT | D-04 | Exact 7-question order | 10-01 T1 | COVERED | |
| CONTEXT | D-05 | Do not ask max-retries | 10-01 T1 | COVERED | prohibitions |
| CONTEXT | D-06 | Tipo = topico only | 10-01 T1 | COVERED | |
| CONTEXT | D-07 | Enter = demo defaults | 10-01 T1 | COVERED | |
| CONTEXT | D-08 | Reasoning always; default medium globally | 10-01 T1 | COVERED | |
| CONTEXT | D-09 | Reasoning tip gpt-4o-mini may ignore | 10-01 T1 | COVERED | |
| CONTEXT | D-10 | Tip under each prompt (stdout) | 10-01 T1 | COVERED | |
| CONTEXT | D-11 | JSON path re-prompt if empty | 10-01 T1 | COVERED | |
| CONTEXT | D-12 | No profiles | 10-01 | COVERED | prohibitions |
| CONTEXT | D-13 | Mock input; CI argparse | 10-01 T2 | COVERED | |
| CONTEXT | D-14 | Module `wizard.py`; main thin | 10-01 T1 | COVERED | discretion: wizard.py not cli_wizard.py |
| CONTEXT | D-15 | Phase 10 on v1.2; independent of failover | 10-01 | COVERED | call existing run only |
| AI-SPEC | — | Framework None; no live LLM eval | 10-01 | COVERED | |
| COVERAGE | — | No external API | 10-01 | COVERED | N/A integrate |

Deferred (not planned): GUI; profiles; max-retries in wizard; replace argparse; Phase 8 failover re-implementation inside wizard.

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Module filename | `exercise-ai/wizard.py` | D-14 preference over `cli_wizard.py` |
| Return shape | `@dataclass` `WizardAnswers` with fields above | Clear mapping to `GenerationRequest` + env + out |
| Tip + question stream | stdout for prompt+tip; errors/stderr for non-TTY and key failures | D-10 |
| Provider empty Enter | `provider=None` → do **not** set `LLM_PROVIDER` (auto-detect by key, same as argparse omit) | Matches tip D-10 / argparse `default=None` |
| Invalid dificuldade / quantidade / provider / reasoning | Re-prompt with short PT error until valid or empty-Enter default (empty only where D-07 allows) | Fail-fast UX; CONTEXT locks empty defaults but not invalid tokens |
| Quantidade bounds | Same as argparse: int 1–40 | `_MAX_QUANTIDADE` / `_positive_quantidade` rules |
| Dificuldade values | `facil` \| `medio` \| `dificil` only | `DificuldadeEnum` |
| Provider values when non-empty | `openai` \| `gemini` \| `grok` | Same as argparse choices |
| Reasoning values | `none` \| `low` \| `medium` \| `high`; empty → `medium` | D-08 |
| `gerar` detection | If `argv is None`: use `sys.argv[1:]`. If first token == `gerar`, consume it and enter wizard; remaining tokens after `gerar` are ignored or rejected with PT usage hint (discretion: **reject** extra tokens with clear PT message — keep wizard pure interactive) | Avoid half-argparse hybrids |
| Pipeline call | Always `main.run(...)` — do not call `generate_validated_batch` / `generate_with_failover` from wizard | Phase 8 may already be inside `run`; wizard stays thin (D-15) |
| max-retries from wizard | Pass `None` to `run` (existing RELY default) | D-05 |
| Key check | Reuse `main._ensure_provider_key` when provider chosen | Same as argparse path |
| No new PyPI deps | stdlib only for UX | AI-SPEC Framework None |

<tasks>

<task type="tracer">
  <name>End-to-end `gerar` wizard → run() + medium default</name>
  <reversibility rating="reversible">CLI UX module + default constant change; argparse path preserved; no on-disk schema migration.</reversibility>
  <files>exercise-ai/wizard.py, exercise-ai/main.py, exercise-ai/reasoning.py, exercise-ai/tests/test_wizard.py, exercise-ai/tests/test_reasoning.py</files>
  <read_first>exercise-ai/main.py (build_parser, main, run, defaults, _ensure_provider_key), exercise-ai/reasoning.py (DEFAULT_REASONING_EFFORT, resolve_reasoning_effort), exercise-ai/models.py (GenerationRequest, DificuldadeEnum), .planning/phases/10-interactive-cli-wizard/10-CONTEXT.md (D-01…D-15), 10-AI-SPEC.md</read_first>
  <action>
  Build the thinnest production-quality vertical slice for WIZ-01/WIZ-02 (D-01…D-11, D-14, D-15). No GUI, profiles, max-retries prompt, failover re-implementation, or new PyPI packages.

  **1. Reasoning default (D-08)**  
  In `reasoning.py`, set `DEFAULT_REASONING_EFFORT = "medium"` and update module docstring / `resolve_reasoning_effort` docstring from “default low” to “default medium”. Update `main.build_parser` `--reasoning` help text that currently says “padrão: low…” to medium. Update `test_resolve_reasoning_default_low` → assert `"medium"` (rename test to `…_medium`).

  **2. Module `exercise-ai/wizard.py` (D-14)**  
  Implement interactive collector:
  - Guard: if stdin is not a TTY, print clear PT message to stderr and exit/raise without calling `input()` (D-03). Message e.g. `O wizard 'gerar' requer um terminal interativo (TTY). Use as flags argparse para CI/scripts.`
  - Prompt sequence **exact order** (D-04), each showing tip **under** the question on **stdout** (D-10). Use CONTEXT draft tip strings (operator may tweak later via `$gsd-fast`):
    1. Tipo de exercício → maps to **`topico` only** (D-06); Enter → `_DEFAULT_TOPICO`
    2. Matéria; Enter → `_DEFAULT_MATERIA`
    3. Dificuldade; Enter → `_DEFAULT_DIFICULDADE`; invalid → re-prompt
    4. Quantidade; Enter → `_DEFAULT_QUANTIDADE`; invalid / out of 1–40 → re-prompt
    5. Provedor; Enter → `None` (auto-detect); invalid non-empty → re-prompt
    6. Reasoning **always asked**; Enter → `medium` (D-08); tip includes gpt-4o-mini may ignore (D-09); invalid → re-prompt
    7. Nome do JSON; empty Enter → **re-prompt** until non-empty path (D-11 exception to D-07); do **not** invent `exercicios.json`
  - Do **not** ask max-retries (D-05).
  - Return `WizardAnswers` (or equivalent) ready for `GenerationRequest` construction.

  **3. Wire `main.main` (D-01, D-02, D-14)**  
  Before `build_parser().parse_args`:
  - If first argv token is `gerar`, enter wizard path (do not pass `gerar` into argparse).
  - Collect answers → set `os.environ["LLM_PROVIDER"]` only when provider is non-None; call `_ensure_provider_key` when set; set `LLM_REASONING_EFFORT` from wizard reasoning; build `GenerationRequest`; call **`run(request, out_path=..., max_retries=None)`** — same entry argparse uses (today `run` already calls `generate_with_failover`; if execute-time `run` still uses that or later switches, wizard must not bypass `run`) (D-15).
  - Without `gerar`, existing argparse path unchanged (D-02; WIZ-03).

  **4. Tracer tests (partial D-13)**  
  Add `exercise-ai/tests/test_wizard.py` with at least:
  - Mock `isatty` True + scripted `input` returns (mix of Enter and values) → assert `collect_wizard_answers` / main `gerar` path builds expected kwargs / calls `run` with expected `GenerationRequest` fields and out path (patch `main.run` — **no live LLM**).
  - Empty Enter on reasoning → `medium`; empty Enter on topico/materia/dificuldade/quantidade → demo defaults; empty out → re-prompt then accept path.
  - Non-TTY → clear PT error; `input` not called (or not hung).
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_wizard.py -q</automated>
    <fails_when>non-zero exit, failed/errors in summary, or collection finds zero tests in test_wizard.py</fails_when>
    <automated>pytest exercise-ai/tests/test_reasoning.py -q</automated>
    <fails_when>non-zero exit, failed/errors, or default still asserts "low" while DEFAULT is medium</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
  </verify>
  <acceptance_criteria>
    - `python … main.py gerar` path exists in code: first token `gerar` runs wizard then `run`.
    - Tips printed under each of the 7 prompts; Enter defaults work except JSON path re-prompt.
    - `DEFAULT_REASONING_EFFORT` is `medium`; reasoning tests and argparse help agree.
    - Wizard never asks max-retries; never invents surprise out filename.
    - Offline tracer tests pass without live LLM.
  </acceptance_criteria>
  <done>
  Tracer proves `gerar` → tips/Q&A → `GenerationRequest` + env + `run()` with medium default and TTY guard (D-01…D-11, D-14, D-15; WIZ-01/WIZ-02 slice).
  </done>
</task>

<task type="auto">
  <name>CI coexistence tests + README operator notes</name>
  <reversibility rating="reversible">Tests and docs-only expansion; no schema change.</reversibility>
  <files>exercise-ai/tests/test_wizard.py, exercise-ai/tests/test_main.py, README.md</files>
  <read_first>exercise-ai/tests/test_main.py (argparse / run patterns), exercise-ai/tests/test_wizard.py (from Task 1), README.md (CLI table / reasoning default low), 10-CONTEXT.md D-02 D-12 D-13, REQUIREMENTS WIZ-03</read_first>
  <action>
  Lock WIZ-03 / D-13 without live LLM or real TTY (D-02, D-12, D-13).

  Expand `test_wizard.py` (and lightly touch `test_main.py` only if needed):
  - Assert argparse path still works when argv has `--out` and no `gerar` (existing tests may already cover — add one explicit “wizard not invoked” assert if useful).
  - Assert `gerar` with extra tokens is rejected with PT message (per Discretion) OR document if Task 1 chose ignore — keep consistent.
  - Assert provider Enter leaves provider unset (auto-detect); explicit `openai`/`gemini`/`grok` sets env before `run`.
  - Assert invalid dificuldade/quantidade/reasoning re-prompt (scripted input sequences).
  - Assert no profile UX strings / no max-retries prompt text in wizard stdout (D-05, D-12).
  - Confirm suite needs no API secrets.

  Update README briefly:
  - Document `python exercise-ai/main.py gerar` interactive flow and tip/Enter behavior (JSON path required).
  - Change documented reasoning default from `low` → **`medium`** (env table + CLI table + examples if they claim default low).
  - State CI/scripts should keep using argparse flags (not `gerar`).
  - Do not document GUI, profiles, or failover redesign.

  Confirm full suite green offline: `pytest exercise-ai -q`.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_wizard.py -q</automated>
    <fails_when>non-zero exit, failed/errors, or tests omit argparse coexistence / re-prompt / mock-input cases</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
    <automated>python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); assert 'gerar' in t; assert 'medium' in t.lower(); print('README gerar + medium OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or README missing gerar / medium</fails_when>
    <automated>python -c "from pathlib import Path; r=Path('exercise-ai/reasoning.py').read_text(encoding='utf-8'); assert 'DEFAULT_REASONING_EFFORT' in r and 'medium' in r; print('default medium OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or default not medium</fails_when>
  </verify>
  <acceptance_criteria>
    - Offline tests cover gerar vs argparse, TTY fail, defaults, JSON re-prompt, mapping to run, no live LLM.
    - README documents `gerar` and reasoning default medium; CI guidance stays argparse.
    - Full `pytest exercise-ai -q` stays green without API secrets.
  </acceptance_criteria>
  <done>
  D-02/D-12/D-13 and WIZ-03 verification path is CI-safe; operator docs updated (D-08 docs alignment).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator stdin → wizard | Untrusted free-text answers; must validate enums/bounds before pipeline |
| Wizard → env (`LLM_PROVIDER`, `LLM_REASONING_EFFORT`) | Only allowlisted values; never echo API keys |
| Wizard → `run` / filesystem `--out` | Path from operator; reuse existing `resolve_success_out_path` behavior inside `run` |
| Tests → mocked input/isatty | Isolated; must not require real TTY or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | Wizard field parsing | medium | mitigate | Re-prompt invalid dificuldade/quantidade/provider/reasoning; bounds 1–40; enum allowlists matching argparse |
| T-10-02 | Information Disclosure | Tips / errors / logs | critical | mitigate | Never print API keys or `.env` contents; reuse existing key-missing PT messages only (LOG-02 spirit) |
| T-10-03 | Denial of Service | Non-TTY hang | medium | mitigate | Fail fast when `isatty()` false (D-03); no blocking input in CI |
| T-10-04 | Tampering | Surprise file write | medium | mitigate | Re-prompt empty JSON path; do not invent default filename (D-11) |
| T-10-SC | Tampering | pip installs | low | accept | No new PyPI packages; stdlib input only (AI-SPEC Framework None) |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_wizard.py -q` covers mock input, TTY, defaults, JSON re-prompt, run mapping.
- `pytest exercise-ai -q` remains green offline (prior verify command).
- README mentions `gerar` + reasoning default `medium`.
- Manual spot-check (optional operator): local TTY `python exercise-ai/main.py gerar` — not required for plan automation.
</verification>

<success_criteria>
- WIZ-01: `gerar` opens tip-backed interactive sequence in locked order on TTY.
- WIZ-02: Answers drive existing `run` pipeline; Enter defaults (except JSON); reasoning default medium globally.
- WIZ-03: Argparse remains for CI; wizard tests mock input; no live LLM.
- D-01…D-15 honored; deferred items remain out of scope.
- No failover re-implementation in wizard; no GUI; no profiles; no max-retries question.
</success_criteria>

<output>
Create `.planning/phases/10-interactive-cli-wizard/10-01-SUMMARY.md` when done
</output>
