---
phase: 12-local-embed-demo
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - demo/index.html
  - demo/app.js
  - demo/README.md
  - README.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Page primary surfaces are form + rendered exercises; results use tabs Exercícios | JSON with schema hint before first success (D-01; D-02; D-03; DEMO-01)"
    - "Form has Contrato (GenerationRequest) vs Ambiente sections with presets and wizard-parity provider/reasoning; Gerando… disables submit only (D-05..D-09)"
    - "Errors show EN class badge + PT message + kind; 409 text includes HTTP 409; last success retained on later failure (D-10; D-11; D-12)"
    - "Dismissible throwaway banner + persistent footnote; demo/README anti-accretion pack; root README Embed pointer with delete-at-close (D-13..D-16; DEMO-02)"
  artifacts:
    - path: "demo/index.html"
      provides: "PT UI chrome: banner, form sections, tabs, footnote"
      contains: "Exercícios|Gerando|Contrato|Ambiente|GenerationRequest"
    - path: "demo/app.js"
      provides: "fetch /gerar, tab switch, busy state, keep-last-success, banner dismiss"
      contains: "/gerar|Gerando|HTTP 409|sessionStorage|localStorage"
    - path: "demo/README.md"
      provides: "Full anti-accretion pack (D-14)"
      contains: "[::1]:8642|netstat|delete|CI"
    - path: "README.md"
      provides: "Short Embed pointer to demo/ (D-15)"
      contains: "demo/|[::1]:8642"
  key_links:
    - from: "demo/app.js fetch"
      to: "POST /gerar"
      via: "Content-Type application/json; body has contract fields + provider/reasoning"
      pattern: "/gerar"
    - from: "error badge UI"
      to: "error.class / error.message / error.kind"
      via: "D-10 mapping from Plan 01 error_payload"
      pattern: "ConfigError|InvalidRequestError|GenerationFailedError"
    - from: "throwaway banner + footnote"
      to: "demo/README.md expiry language"
      via: "D-13 D-14 D-16 anti-accretion"
      pattern: "throwaway|delete"
  prohibitions:
    - "Do not add demo to CI"
    - "Do not add FastAPI/Flask/Streamlit or new npm/pip deps"
    - "Do not render stderr diagnostics or env secret values in the page"
    - "Do not document or promote an IPv4-first hostname alias in place of the bracketed IPv6 loopback URL"
    - "Do not change exercise-ai service contract or Plan 01 guard semantics"
---

<objective>
Expand the Plan 01 tracer into the locked operator UX (D-01..D-16): form composition, Exercícios|JSON tabs, busy/error behavior, throwaway chrome, and anti-accretion docs.

Purpose: Satisfy DEMO-01 visual acceptance and DEMO-02 banner/README expiry signals.
Output: complete demo/index.html + demo/app.js, demo/README.md, root README Embed pointer.
</objective>

<execution_context>
@C:/Users/Anglo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-local-embed-demo/12-CONTEXT.md
@.planning/phases/12-local-embed-demo/12-RESEARCH.md
@.planning/phases/12-local-embed-demo/12-01-PLAN.md
@demo/serve.py
@demo/index.html
@exercise-ai/wizard.py
@exercise-ai/models.py
@README.md
</context>

<proven_verify_commands>
**Reuse from repo root:**

- Demo offline: `pytest demo/tests -q`
- Package regression: `pytest exercise-ai -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Asset split | `demo/index.html` + `demo/app.js` (+ optional tiny inline CSS in HTML) | CONTEXT agent discretion; ThreadingHTTPServer serves siblings |
| Preset defaults | Matemática / equação do 1º grau / médio / quantidade 2 | D-07 example |
| Provider empty/auto | Empty `<option value="">` labeled auto (PT chrome) | D-08; wizard empty → auto |
| Schema hint | English field names for ExerciseBatch.exercicios[].enunciado/resposta/explicacao — no fake rows | D-03 |
| Banner dismiss store | sessionStorage key until reload | D-16 |

## Artifacts this phase produces

- `demo/index.html` — full page: dismissible throwaway banner (D-13, D-16), Contrato vs Ambiente fieldsets (D-05, D-06), presets (D-07), provider/reasoning controls (D-08), tablist Exercícios|JSON (D-02), error badge region (D-10), persistent footnote (D-13)
- `demo/app.js` — tab switching replaces view (D-02); schema hint until first success (D-03); Gerando… + disable submit only (D-09); error badge + keep last success (D-10, D-12); explicit HTTP 409 messaging (D-11); banner dismiss for session (D-16); PT chrome / EN identifiers (D-04)
- `demo/README.md` — anti-accretion pack (D-14): expiry/delete-at-close, literal http://[::1]:8642/, netstat check, not-for-staging, no CI / no deps, run command `python demo/serve.py`
- Root `README.md` Embed section — short pointer to demo/ + loopback URL + delete-at-close (D-15)

<tasks>

<task type="auto">
  <name>Ship form + tabs + busy/error UX (D-01..D-12)</name>
  <reversibility rating="reversible">Throwaway UI; D-01..D-12 all marked reversible in CONTEXT.</reversibility>
  <files>demo/index.html, demo/app.js</files>
  <read_first>demo/index.html (tracer), demo/serve.py (POST /gerar response shape), .planning/phases/12-local-embed-demo/12-CONTEXT.md (D-01..D-12), .planning/phases/12-local-embed-demo/12-RESEARCH.md (Minimal HTML structure), exercise-ai/wizard.py (_VALID_PROVIDER, _VALID_REASONING defaults), exercise-ai/models.py (GenerationRequest, ExerciseBatch, DificuldadeEnum)</read_first>
  <action>
    Expand the tracer page so primary surfaces are the form and rendered exercises; contract JSON is secondary via tabs (D-01). Results area uses tabs labeled Exercícios and JSON that replace the view on switch — not an inline toggle (D-02). Before first success: Exercícios empty; JSON tab shows a schema/shape hint of ExerciseBatch fields in English with no fake exercise data (D-03). UI chrome in Portuguese; technical identifiers (exception class names, GenerationRequest / JSON field names, kind) stay English (D-04).

    Form exposes materia, topico, dificuldade, quantidade plus provider and reasoning; client sends all to /gerar but server already maps only four fields into GenerationRequest (D-05). Two labeled sections: Contrato (GenerationRequest) vs Ambiente (só nesta demo) with an explicit note that env controls are not in the batch JSON (D-06). Load demo-ready presets on first paint: e.g. Matemática / equação do 1º grau / médio / quantidade 2 (D-07). Provider options mirror wizard: empty/auto + openai|gemini|grok; reasoning none|low|medium|high default medium (D-08).

    While generating: disable only the submit control and show Gerando…; leave other fields editable (D-09). On error: badge with exception class ConfigError / InvalidRequestError / GenerationFailedError plus PT message and kind when present (D-10). On HTTP 409: explicit sequential-contract message that includes the substring HTTP 409 (D-11). On failure after a prior success: keep last good Exercícios/JSON content; show error banner/badge above (D-12). Do not render stderr or env values. Keep styling throwaway-simple.
  </action>
  <verify>
    <automated>pytest demo/tests -q</automated>
    <fails_when>non-zero exit, or summary shows failed/error, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - index.html contains Contrato, Ambiente, Exercícios, JSON, Gerando… affordances and GenerationRequest labeling (D-01..D-09)
    - app.js posts to /gerar, implements tab replace, keep-last-success, and HTTP 409 handling (D-02, D-11, D-12)
    - demo offline tests remain green (no CI change)
  </acceptance_criteria>
  <done>DEMO-01 UI behaviors D-01..D-12 present on the static page + JS client.</done>
</task>

<task type="auto">
  <name>Throwaway banner, footnote, demo README, root Embed pointer (D-13..D-16)</name>
  <reversibility rating="reversible">Docs/chrome only; D-13..D-16 reversible.</reversibility>
  <files>demo/index.html, demo/app.js, demo/README.md, README.md</files>
  <read_first>demo/index.html, .planning/phases/12-local-embed-demo/12-CONTEXT.md (D-13..D-16), .planning/phases/12-local-embed-demo/12-RESEARCH.md (Pitfall 5 anti-accretion), README.md (## Embed / biblioteca section), .github/workflows/ci.yml (confirm pytest exercise-ai only — do not edit)</read_first>
  <action>
    Add a persistent top throwaway banner and a footer footnote stating throwaway / delete at v2.0 close / loopback-only URL using the literal bracketed IPv6 form http://[::1]:8642/ (D-13). Make the top banner dismissible for the browser session via an X control (sessionStorage until reload); footnote remains always visible (D-16).

    Write demo/README.md with the full anti-accretion pack (D-14): expiry/delete-at-close, literal http://[::1]:8642/ (do not substitute an IPv4-first hostname alias), netstat check expecting [::1]:8642 not wildcard, not-for-staging, no CI / no new deps, run via python demo/serve.py from repo root.

    Update root README.md Embed section with a short pointer to demo/, the same loopback URL, and delete-at-close note (D-15). Do not add demo to GitHub Actions. Do not change exercise-ai sources.
  </action>
  <verify>
    <automated>pytest demo/tests -q</automated>
    <fails_when>non-zero exit, or summary shows failed/error, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - demo/README.md documents delete-at-close, http://[::1]:8642/, netstat, no CI/deps (D-14)
    - root README Embed mentions demo/ and the loopback URL with delete-at-close (D-15)
    - page markup/JS support dismissible banner + persistent footnote (D-13, D-16)
    - `rg "demo" .github/workflows/ci.yml` finds no demo job addition; `pytest exercise-ai -q` still the CI command
  </acceptance_criteria>
  <done>DEMO-02 anti-accretion chrome and docs complete; D-01..D-16 covered across Plans 01–02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → rendered HTML/JS | XSS-ish risk low on loopback static files; still must not inject secrets into DOM |
| Screen-share / shoulder surf | Banner/docs reduce accidental staging use; page must not show .env |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information disclosure | Results / error UI | high | mitigate | Render batch fields + typed errors only; never stderr/env (carry-forward; D-10/D-12) |
| T-12-07 | Elevation of privilege | Demo accretion to staging | high | mitigate | Banner+footnote (D-13/D-16) + demo/README + root pointer delete-at-close (D-14/D-15); outside CI |
| T-12-08 | Spoofing | Docs using wrong host string | medium | mitigate | Document only http://[::1]:8642/; omit IPv4-first hostname aliases (D-14) |
| T-12-02 | Elevation / economic | Client fetch | high | mitigate | Client always sends Content-Type application/json to /gerar; server guards remain Plan 01 |
| T-12-SC | Tampering | npm/pip installs | low | accept | No new packages; static JS only |
</threat_model>

<verification>
- `pytest demo/tests -q` passes
- `pytest exercise-ai -q` passes
- Manual UAT (phase verify): open http://[::1]:8642/ — presets → Gerando… → tabs; concurrent POST → HTTP 409; banner dismiss / footnote / README pack
</verification>

<success_criteria>
- All locked UX decisions D-01..D-16 implemented
- DEMO-01 and DEMO-02 observable from UI + docs
- Demo remains throwaway, stdlib-only, outside CI
</success_criteria>

<output>
Create `.planning/phases/12-local-embed-demo/12-02-SUMMARY.md` when done
</output>
