---
phase: 12-local-embed-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/guards.py
  - demo/error_map.py
  - demo/serve.py
  - demo/index.html
  - demo/tests/test_guards.py
  - demo/tests/test_error_map.py
  - demo/tests/test_tracer_gerar.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Operator can start demo/serve.py on [::1]:8642 and POST /gerar with application/json to receive ok+batch or typed error JSON (DEMO-01; carry-forward generate_batch)"
    - "Non-loopback bind is refused at start; bad Host/Origin → 403; bad Content-Type → 415; busy Lock → HTTP 409 with literal HTTP 409 in message (DEMO-02; D-11)"
    - "Offline demo/tests cover guards and error_map without live LLM; demo is not added to CI (DEMO-02)"
    - "load_dotenv runs only under demo __main__; handler never renders stderr or env secret values (carry-forward; AGENTS.md)"
  artifacts:
    - path: "demo/guards.py"
      provides: "Pure assert_loopback + check_post_headers (+ lock busy decision helper)"
      contains: "assert_loopback|check_post_headers"
    - path: "demo/error_map.py"
      provides: "Exception → error dict for ConfigError / InvalidRequestError / GenerationFailedError (D-10)"
      contains: "error_payload|map_error"
    - path: "demo/serve.py"
      provides: "AF_INET6 ThreadingHTTPServer + POST /gerar → generate_batch under Lock"
      contains: "DemoServer|AF_INET6|/gerar|generate_batch"
    - path: "demo/tests/test_guards.py"
      provides: "Offline unit coverage for loopback/headers/lock→409"
      contains: "test_refuse_non_loopback|test_headers|test_lock_409"
    - path: "demo/tests/test_error_map.py"
      provides: "Offline unit coverage for error JSON shape (D-10)"
      contains: "ConfigError|InvalidRequestError|GenerationFailedError"
    - path: "demo/tests/test_tracer_gerar.py"
      provides: "End-to-end handler path with mocked generate_batch"
      contains: "gerar|/gerar"
  key_links:
    - from: "demo/serve.py do_POST /gerar"
      to: "service.generate_batch"
      via: "sys.path insert exercise-ai; set/clear LLM_PROVIDER + LLM_REASONING_EFFORT then call"
      pattern: "generate_batch"
    - from: "check_post_headers"
      to: "HTTP 403/415"
      via: "Host [::1]:8642; Origin http://[::1]:8642 if present; Content-Type application/json"
      pattern: "415|403"
    - from: "_GEN_LOCK.acquire(blocking=False)"
      to: "HTTP 409 body"
      via: "message includes HTTP 409 per D-11"
      pattern: "409"
  prohibitions:
    - "Do not add demo to .github/workflows/ci.yml"
    - "Do not install new pip packages; stdlib HTTP only"
    - "Do not call load_dotenv at import time of guards/error_map/serve modules used by tests"
    - "Do not import main from exercise-ai (import-time dotenv)"
    - "Do not implement full tabs/banner/README in this plan (Plan 02)"
    - "Do not widen generate_batch or change exercise-ai service contract"
---

<objective>
Wave 0 offline guards/error helpers plus a production-quality tracer: stdlib demo server on [::1]:8642 with POST /gerar → in-process generate_batch, Host/Origin/JSON guards, Lock→409 (D-11), and typed error JSON (D-10).

Purpose: Prove the embed consumer seam and DEMO-02 controls before UI chrome expansion.
Output: demo/guards.py, demo/error_map.py, demo/serve.py, minimal demo/index.html, demo/tests/* (not CI).
</objective>

<execution_context>
@C:/Users/Anglo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-local-embed-demo/12-CONTEXT.md
@.planning/phases/12-local-embed-demo/12-RESEARCH.md
@.planning/phases/12-local-embed-demo/12-VALIDATION.md
@.planning/phases/11-service-layer-extraction/11-CONTEXT.md
@exercise-ai/service.py
@exercise-ai/models.py
@exercise-ai/wizard.py
@README.md
@AGENTS.md
</context>

<proven_verify_commands>
**Reuse from repo root:**

- Package regression: `pytest exercise-ai -q`
- Service smoke: `pytest exercise-ai/tests/test_service.py -q`
- Demo offline (this plan; not CI): `pytest demo/tests -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| POST path | `/gerar` | Research open Q1; matches Gerar UX |
| Bad Content-Type status | `415` | Research open Q2 / planner pick |
| Bad Host/Origin status | `403` | PITFALLS 14 |
| Entry filename | `demo/serve.py` | STACK verified invocation |
| Pure modules | `demo/guards.py`, `demo/error_map.py` | Wave 0 / VALIDATION |
| Tracer UI | Minimal `demo/index.html` form POST only | Full D-01..D-16 chrome in Plan 02 |
| Env around call | Demo-local save/restore of LLM_PROVIDER + LLM_REASONING_EFFORT around generate_batch | Research A2; service also scopes |

## Artifacts this phase produces

- `demo/guards.py` — `assert_loopback`, `check_post_headers` (returns status or None), helper for non-blocking lock busy → 409 decision
- `demo/error_map.py` — `error_payload(exc)` mapping three service exception classes; maps `api_error_kind` into displayed `kind` for GenerationFailedError (D-10)
- `demo/serve.py` — `DemoServer` (`ThreadingHTTPServer`, `address_family=AF_INET6`), handler `do_POST` for `/gerar`, `__main__` with `load_dotenv` + `assert_loopback("::1")` + serve on port 8642
- `demo/index.html` — minimal form that POSTs JSON to `/gerar` (tracer surface; expanded in Plan 02)
- `demo/tests/test_guards.py` — refuse non-loopback; Host/Origin/Content-Type; lock→409
- `demo/tests/test_error_map.py` — three-class error JSON shape
- `demo/tests/test_tracer_gerar.py` — handler path with mocked `generate_batch` (success + one error class + busy 409)

<tasks>

<task type="auto" tdd="true">
  <name>Wave 0: pure guards + error_map with offline pytest</name>
  <reversibility rating="reversible">Throwaway demo helpers; delete-at-close (D-13/D-14).</reversibility>
  <files>demo/guards.py, demo/error_map.py, demo/tests/test_guards.py, demo/tests/test_error_map.py</files>
  <read_first>.planning/phases/12-local-embed-demo/12-CONTEXT.md (D-10, D-11, carry-forward Host/Origin/Lock), .planning/phases/12-local-embed-demo/12-RESEARCH.md (Pattern 2, Host/Origin snippet, Exception→badge, Validation Architecture Wave 0), .planning/phases/12-local-embed-demo/12-VALIDATION.md, exercise-ai/service.py (ConfigError, InvalidRequestError, GenerationFailedError attrs)</read_first>
  <behavior>
    - assert_loopback accepts ::1; refuses 0.0.0.0 and :: (raises/SystemExit-style failure used by __main__)
    - check_post_headers: Host must equal [::1]:8642 → else 403; Origin if present must equal http://[::1]:8642 → else 403; Content-Type type/subtype application/json (ignore params) → else 415; missing Origin allowed
    - lock busy path maps to status 409 with payload/message containing the substring HTTP 409 (D-11)
    - error_payload(ConfigError) / InvalidRequestError include class name EN + message + kind; GenerationFailedError surfaces api_error_kind as kind when present (D-10)
  </behavior>
  <action>
    Create pure helpers under demo/ with no socket listen and no load_dotenv (so pytest can import safely). Implement assert_loopback via ipaddress.is_loopback. Implement check_post_headers against locked allowlists Host=[::1]:8642 and Origin=http://[::1]:8642; Content-Type must be application/json → 415 on mismatch (discretion lock). Implement error_payload for the three service exception classes per D-10 (EN class names; include kind when present; for GenerationFailedError map api_error_kind into the kind field shown to the client).

    Add demo/tests/test_guards.py covering refuse non-loopback, header allowlist matrix, and lock→409 helper (DEMO-02). Add demo/tests/test_error_map.py covering the three exception shapes (DEMO-01 / D-10). Do not modify .github/workflows/ci.yml. Do not import exercise-ai.main.
  </action>
  <verify>
    <automated>pytest demo/tests/test_guards.py demo/tests/test_error_map.py -q</automated>
    <fails_when>non-zero exit, or summary shows failed/error, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - test_guards and test_error_map collect and pass offline
    - helpers are importable without binding a port or loading .env
    - D-10 and D-11 behaviors asserted by tests
  </acceptance_criteria>
  <done>Wave 0 offline guard and error_map tests green; pure helpers exist.</done>
</task>

<task type="tracer">
  <name>End-to-end POST /gerar → generate_batch under Lock and header guards</name>
  <reversibility rating="reversible">Throwaway stdlib demo process; reversible delete of demo/.</reversibility>
  <files>demo/serve.py, demo/index.html, demo/tests/test_tracer_gerar.py, demo/guards.py, demo/error_map.py</files>
  <read_first>demo/guards.py, demo/error_map.py, .planning/phases/12-local-embed-demo/12-RESEARCH.md (Patterns 1–4, architecture diagram), exercise-ai/models.py (GenerationRequest fields), exercise-ai/wizard.py (_VALID_PROVIDER, _VALID_REASONING), exercise-ai/main.py (sys.path / env set pattern around provider), README.md Embed section (sys.path insert)</read_first>
  <behavior>
    - DemoServer uses ThreadingHTTPServer with address_family AF_INET6; __main__ asserts loopback then binds (::1, 8642)
    - POST /gerar with valid headers and free lock calls generate_batch and returns JSON {ok:true, batch: model_dump()}
    - Concurrent second acquire returns 409 with message containing HTTP 409 (D-11); static GET does not take the lock
    - ConfigError/InvalidRequestError/GenerationFailedError become JSON {ok:false, error:{class,message,kind?}} via error_payload (D-10)
    - Provider/reasoning from JSON body set/clear LLM_PROVIDER and LLM_REASONING_EFFORT then build GenerationRequest with only four contract fields (D-05); dotenv only in __main__
  </behavior>
  <action>
    Implement demo/serve.py as the tracer host: subclass ThreadingHTTPServer with address_family=socket.AF_INET6; handler subclasses SimpleHTTPRequestHandler with directory=demo via functools.partial (no os.chdir). Wire POST path /gerar only: run check_post_headers; non-blocking Lock.acquire — on False respond 409 with explicit sequential-contract message including HTTP 409 (D-11 / DEMO-02); else parse JSON, set/clear env for provider/reasoning mirroring wizard option sets (D-05 / D-08 values openai|gemini|grok and none|low|medium|high), construct GenerationRequest, call service.generate_batch, return batch.model_dump() on success, map exceptions via error_payload (D-10), always release lock in finally.

    Bootstrap: insert repo exercise-ai/ onto sys.path; import service and models only (never main). In `if __name__ == "__main__"` only: load_dotenv, assert_loopback, print the literal bracketed IPv6 URL, serve_forever.

    Ship a minimal demo/index.html that can POST application/json to /gerar (enough for tracer; full Exercícios|JSON tabs and banner are Plan 02 / D-01..D-16).

    Add demo/tests/test_tracer_gerar.py that exercises the handler path end-to-end with generate_batch mocked (success batch, one typed error, busy 409) without live LLM and without requiring CI changes.
  </action>
  <verify>
    <automated>pytest demo/tests -q</automated>
    <fails_when>non-zero exit, or summary shows failed/error, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - demo/tests including tracer path pass offline
    - serve.py exposes /gerar → generate_batch with guards and Lock
    - pytest exercise-ai -q still passes; CI workflow untouched
  </acceptance_criteria>
  <done>Tracer path works: guards + Lock + generate_batch wired; offline demo suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → demo HTTP ([::1]:8642) | Untrusted HTTP headers/body on loopback; can burn API quota if guards fail |
| Demo handler → service.generate_batch | In-process library call; must not leak secrets into JSON responses |
| Demo __main__ → .env | Secrets loaded only at process entry; must not appear in responses or HTML |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information disclosure | Bind address | high | mitigate | assert_loopback before serve; refuse non-loopback hosts |
| T-12-02 | Elevation / economic | POST /gerar CSRF | high | mitigate | Host allowlist [::1]:8642; Origin allowlist when present; require application/json (415) |
| T-12-03 | Tampering / economic | Concurrent generate | high | mitigate | Module Lock acquire(blocking=False) → 409; lock only around generate not static GET |
| T-12-04 | Information disclosure | Error/success JSON | high | mitigate | Return model_dump + typed error_payload only; never stderr capture or env values in body |
| T-12-05 | Information disclosure | Import/dotenv | medium | mitigate | load_dotenv only in __main__; no import of exercise-ai.main |
| T-12-SC | Tampering | pip installs | low | accept | No new packages this plan |
</threat_model>

<verification>
- `pytest demo/tests -q` passes
- `pytest exercise-ai -q` passes (CI unchanged)
- `rg "load_dotenv" demo/` only under serve.py `__main__` path
</verification>

<success_criteria>
- Offline Wave 0 + tracer green
- POST /gerar consumes generate_batch with DEMO-02 controls
- Ready for Plan 02 UI/docs expansion (D-01..D-16 chrome)
</success_criteria>

<output>
Create `.planning/phases/12-local-embed-demo/12-01-SUMMARY.md` when done
</output>
