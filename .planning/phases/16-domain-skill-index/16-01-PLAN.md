---
phase: 16-domain-skill-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/skills/__init__.py
  - exercise-ai/skills/generation/__init__.py
  - exercise-ai/skills/generation/persona.py
  - exercise-ai/skills/generation/rules.py
  - exercise-ai/tests/test_skills_generation.py
autonomous: true
requirements:
  - CONTRACT-01
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Import `from skills.generation import get_persona_system` works when `exercise-ai/` is on sys.path (same convention as token_usage) — D-03, D-07"
    - "`get_persona_system()` returns a non-empty str that is clearly a PT-BR placeholder persona stub — D-05, D-07, D-08"
    - "Package path `exercise-ai/skills/generation/` exists with `__init__.py`, `persona.py`, and `rules.py` — D-03"
    - "`rules.py` remains docstring + pass + TODO never-do bullets only (no real rules data/API) — D-06, D-09"
    - "Skill is an API/runtime package under exercise-ai, not an agent-facing Cursor/Claude/Codex skill — D-01, D-02"
  artifacts:
    - path: "exercise-ai/skills/__init__.py"
      provides: "Parent package marker so skills.generation imports resolve"
      contains: ""
    - path: "exercise-ai/skills/generation/__init__.py"
      provides: "Public re-export of get_persona_system via __all__"
      contains: "get_persona_system"
    - path: "exercise-ai/skills/generation/persona.py"
      provides: "get_persona_system() -> str stub"
      contains: "def get_persona_system"
    - path: "exercise-ai/skills/generation/rules.py"
      provides: "Empty rules module shell with TODO nunca-faça for later phases"
      contains: "TODO"
    - path: "exercise-ai/tests/test_skills_generation.py"
      provides: "Offline smoke: import + nonempty str return"
      contains: "get_persona_system"
  key_links:
    - from: "skills.generation.persona.get_persona_system"
      to: "skills.generation.__init__ public surface"
      via: "re-export + __all__ (mirror token_usage)"
      pattern: "from skills.generation.persona import get_persona_system"
    - from: "pytest conftest sys.path (exercise-ai/)"
      to: "from skills.generation import get_persona_system"
      via: "same top-level package import style as token_usage"
      pattern: "from skills.generation import"
  prohibitions:
    - "must NOT create agent skills"
    - "must NOT modify prompts.py"
    - "must NOT fill real rules content"
---

<objective>
Deliver the API generation skill package skeleton under `exercise-ai/skills/generation/` with public `get_persona_system() -> str`, stub persona, and empty `rules.py` shell — fulfilling CONTRACT-01 as reinterpreted by CONTEXT (API/runtime skill index, not Cursor agent skill).

Purpose: Establish the importable persona/rules home the generation pipeline will consume in later phases, without wiring prompts or filling pedagogical content yet.
Output: `exercise-ai/skills/` package tree, `get_persona_system` re-export, optional offline smoke test.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-domain-skill-index/16-CONTEXT.md
@.planning/phases/16-domain-skill-index/16-PATTERNS.md
@exercise-ai/token_usage/__init__.py
@exercise-ai/tests/conftest.py
</context>

<flagged_assumptions>
| ID | Topic | Assumption | Rationale |
|----|-------|------------|-----------|
| A-16-01 | concurrency (EDGE_ABSENT) | This phase ships pure module load / immutable stub strings only. No shared mutable state, no writer, no concurrent-mutation contract. Interrupted or parallel imports are safe as normal Python import caching; no extra locking or idempotency guarantees are in scope. | Specless concurrency probe unresolved; package is read-only after import — backstop is stdlib import semantics. |
</flagged_assumptions>

<proven_verify_commands>
**From repo root:**

- Import smoke: `python -c "import sys; sys.path.insert(0,'exercise-ai'); from skills.generation import get_persona_system; assert isinstance(get_persona_system(), str) and get_persona_system().strip()"`
- Pytest smoke (after Task 3): `python -m pytest exercise-ai/tests/test_skills_generation.py -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Parent `skills/__init__.py` | Minimal package marker (empty or one-line PT-BR docstring); no `__all__` | Claude's Discretion + PATTERNS; required for `skills.generation` as regular package |
| Persona stub wording | Clear PT-BR placeholder naming Phase 16 / future replacement | D-05, D-08; exact prose implementer discretion |
| TODO bullets in `rules.py` | At least LLM self-check substitute and schema-in-prompt-as-format-contract; more CONTRACT-02/03 foreshadow OK | D-06, D-09 |
| Smoke test | Add `exercise-ai/tests/test_skills_generation.py` | Helps CONTRACT-01 verification without scope creep; PATTERNS-approved |

## Source audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|-----|------------------------|------|--------|-------|
| GOAL | — | Domain skill index for exercise generation (ROADMAP wording: Cursor/repo skill) | 01 | COVERED | CONTEXT D-01/D-02 **override** ROADMAP agent-skill wording → API package under `exercise-ai/skills/generation/` |
| REQ | CONTRACT-01 | Domain skill index exists for exercise generation | 01 | COVERED | Tasks 1–3; requirements frontmatter |
| RESEARCH | — | No phase RESEARCH.md | — | N/A | PATTERNS + CONTEXT only; zero new deps |
| CONTEXT | D-01 | API/runtime skill, not agent tooling | 01 | COVERED | Task 1 tracer + prohibitions |
| CONTEXT | D-02 | Do not create/extend .cursor/.claude/.codex for this contract | 01 | COVERED | Task 1 + must_haves.prohibitions |
| CONTEXT | D-03 | Path `exercise-ai/skills/generation/` with `__init__.py`, `persona.py`, `rules.py` | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-04 | Do not modify `prompts.py` | 01 | COVERED | files_modified excludes it; prohibitions |
| CONTEXT | D-05 | `persona.py` stub persona string OK | 01 | COVERED | Task 1 |
| CONTEXT | D-06 | `rules.py` docstring/pass + TODO nunca-faça only | 01 | COVERED | Task 2 |
| CONTEXT | D-07 | `get_persona_system() -> str` in persona.py, re-exported from generation/`__init__.py` | 01 | COVERED | Task 1 |
| CONTEXT | D-08 | PT-BR texts/docstrings | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-09 | Never-do list only as TODO in `rules.py` | 01 | COVERED | Task 2 |
| Deferred | — | Agent skill folders; real rules; prompts wiring; GenerationSkill dataclass | — | EXCLUDED | must_haves.prohibitions + deferred list |

## Artifacts this phase produces

| Symbol / artifact | Kind | Location |
|-------------------|------|----------|
| `get_persona_system` | function `() -> str` | `exercise-ai/skills/generation/persona.py` |
| `get_persona_system` (re-export) | public API | `exercise-ai/skills/generation/__init__.py` (`__all__`) |
| `exercise-ai/skills/__init__.py` | package marker | `exercise-ai/skills/` |
| `exercise-ai/skills/generation/rules.py` | stub module | docstring + `pass` + TODO bullets |
| `test_get_persona_system_returns_nonempty_str` | offline smoke test | `exercise-ai/tests/test_skills_generation.py` |

<tasks>

<task type="tracer">
  <name>Tracer: skills.generation package + get_persona_system import path</name>
  <reversibility rating="costly">D-01 locks API-vs-agent skill concept; D-03 locks package path that Phase 19+ will import. Undo means rename/rehome or reintroducing a parallel agent contract — costly, not one-way (no migration / published wire format).</reversibility>
  <files>exercise-ai/skills/__init__.py, exercise-ai/skills/generation/__init__.py, exercise-ai/skills/generation/persona.py</files>
  <read_first>
.planning/phases/16-domain-skill-index/16-CONTEXT.md
.planning/phases/16-domain-skill-index/16-PATTERNS.md
exercise-ai/token_usage/__init__.py
exercise-ai/tests/conftest.py
  </read_first>
  <action>
    Wire ONE end-to-end import path for CONTRACT-01 / D-07 (API skill, not agent tooling — D-01, D-02):

    1. Create parent package marker `exercise-ai/skills/__init__.py` (minimal; empty or one-line PT-BR docstring — Claude's Discretion). Do not invent `exercise-ai/__init__.py`.

    2. Create `exercise-ai/skills/generation/persona.py` with module docstring in PT-BR (D-08), `from __future__ import annotations`, and `get_persona_system() -> str` returning a clearly labeled PT-BR placeholder stub string (D-05, D-07). No I/O, no LLM calls, no dataclass, no import of prompts.

    3. Create `exercise-ai/skills/generation/__init__.py` mirroring `token_usage/__init__.py` pattern: PT-BR package docstring, absolute import `from skills.generation.persona import get_persona_system`, `__all__ = ["get_persona_system"]` only (D-03, D-07).

    Touch only the three files listed. Leave `prompts.py` and agent tooling directories out of this change set (D-02, D-04).
  </action>
  <acceptance_criteria>
    - Paths exist: `exercise-ai/skills/__init__.py`, `exercise-ai/skills/generation/__init__.py`, `exercise-ai/skills/generation/persona.py`
    - `persona.py` defines `get_persona_system` with annotated return `str`
    - `generation/__init__.py` re-exports `get_persona_system` and lists it in `__all__`
    - Repo-root command below exits 0 and assertion holds
  </acceptance_criteria>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'exercise-ai'); from skills.generation import get_persona_system; assert isinstance(get_persona_system(), str) and get_persona_system().strip()"</automated>
    <fails_when>non-zero exit, ImportError/ModuleNotFoundError, or AssertionError</fails_when>
  </verify>
  <done>Public `from skills.generation import get_persona_system` works with nonempty str stub (D-01, D-03, D-05, D-07, D-08).</done>
</task>

<task type="auto">
  <name>Expansion: rules.py stub + TODO nunca-faça bullets</name>
  <reversibility rating="reversible">Stub module only; real rules content arrives in Phases 17–18. Easy to replace body without changing package path.</reversibility>
  <files>exercise-ai/skills/generation/rules.py</files>
  <read_first>
.planning/phases/16-domain-skill-index/16-CONTEXT.md (D-06, D-09)
.planning/phases/16-domain-skill-index/16-PATTERNS.md (rules.py section)
exercise-ai/token_usage/rates.py
  </read_first>
  <action>
    Complete D-03 file set by adding `exercise-ai/skills/generation/rules.py` per D-06 and D-09:

    - Module docstring in PT-BR stating this is a generation-rules skeleton; real content belongs to Phases 17–18 (D-08).
    - `from __future__ import annotations`
    - Comment block `# TODO (Phases 17–18):` with PT-BR never-do bullets only (e.g. never ask the LLM to self-check as a substitute for deterministic validation; never treat schema-in-prompt as the format contract). Exact bullet wording is implementer discretion.
    - Module body is `pass` only — no functions, constants, dataclasses, or exports. Do not re-export from `generation/__init__.py`.

    Do not add pedagogical rule prose beyond the TODO foreshadow list (D-06, D-09).
  </action>
  <acceptance_criteria>
    - `exercise-ai/skills/generation/rules.py` exists
    - File contains a TODO comment block with at least two never-do bullets and a bare `pass`
    - `generation/__init__.py` still exports only `get_persona_system` (no rules symbols)
    - Tracer import command from Task 1 still exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "import sys; from pathlib import Path; p=Path('exercise-ai/skills/generation/rules.py'); assert p.is_file(); t=p.read_text(encoding='utf-8'); assert 'TODO' in t and 'pass' in t; sys.path.insert(0,'exercise-ai'); from skills.generation import get_persona_system; assert isinstance(get_persona_system(), str)"</automated>
    <fails_when>non-zero exit, missing file, missing TODO/pass markers, or ImportError</fails_when>
  </verify>
  <done>`rules.py` shell present with TODO nunca-faça only; public API unchanged (D-03, D-06, D-09).</done>
</task>

<task type="auto" tdd="true">
  <name>Smoke test: get_persona_system offline import</name>
  <reversibility rating="reversible">Test-only addition; delete without affecting runtime package.</reversibility>
  <files>exercise-ai/tests/test_skills_generation.py</files>
  <read_first>
exercise-ai/tests/conftest.py
exercise-ai/tests/test_model_catalog.py
.planning/phases/16-domain-skill-index/16-PATTERNS.md (test_skills_generation section)
  </read_first>
  <behavior>
    - Import `get_persona_system` from `skills.generation` (conftest already puts exercise-ai on sys.path)
    - Calling it returns isinstance str and nonempty after strip
    - Test does not import or assert against prompts
  </behavior>
  <action>
    Add `exercise-ai/tests/test_skills_generation.py` following PATTERNS smoke shape:

    - Module docstring noting CONTRACT-01 / Phase 16 smoke
    - `from skills.generation import get_persona_system`
    - One test asserting `isinstance(..., str)` and nonempty strip

    Rely on existing `conftest.py` path insert; do not duplicate sys.path logic unless necessary. Do not import `prompts` or assert SYSTEM_PROMPT wiring (D-04 deferred to Phase 19+).
  </action>
  <acceptance_criteria>
    - `exercise-ai/tests/test_skills_generation.py` exists and imports from `skills.generation`
    - `python -m pytest exercise-ai/tests/test_skills_generation.py -q` exits 0 with at least one passed test
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest exercise-ai/tests/test_skills_generation.py -q</automated>
    <fails_when>non-zero exit, collection errors, or "0 passed" / failed tests in the summary</fails_when>
  </verify>
  <done>Offline smoke locks importable public API for CONTRACT-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev workstation → local Python modules | New files are pure library stubs under `exercise-ai/`; no network, no secrets, no host I/O |
| Agent tooling dirs vs API package | Phase deliberately keeps generation contract out of `.cursor`/`.claude`/`.codex` (D-01/D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | persona stub string | low | accept | Stub is non-secret placeholder PT-BR text; no API keys or PII |
| T-16-02 | Tampering | local `.py` on disk | info | accept | Same trust model as existing exercise-ai modules; no elevated privileges |
| T-16-03 | Elevation of Privilege | N/A | info | accept | No auth, no network listeners, no subprocess in this phase |
| T-16-SC | Tampering | package installs | info | accept | Zero new pip/npm deps this phase — no supply-chain install task |

ASVS L1: no high/critical threats; no `checkpoint:decision` required for security.
</threat_model>

<verification>
- Import: `python -c "import sys; sys.path.insert(0,'exercise-ai'); from skills.generation import get_persona_system; assert isinstance(get_persona_system(), str) and get_persona_system().strip()"`
- Pytest: `python -m pytest exercise-ai/tests/test_skills_generation.py -q`
- Files present: `exercise-ai/skills/{__init__.py,generation/{__init__.py,persona.py,rules.py}}`
- `prompts.py` not in this plan's `files_modified`; agent skill folders not created
</verification>

<success_criteria>
- CONTRACT-01 satisfied as API skill package index under `exercise-ai/skills/generation/`
- `get_persona_system() -> str` publicly importable
- `rules.py` is skeleton-only with TODO never-do bullets
- D-01..D-09 honored; deferred items absent from deliverables
</success_criteria>

<output>
Create `.planning/phases/16-domain-skill-index/16-01-SUMMARY.md` when done
</output>
