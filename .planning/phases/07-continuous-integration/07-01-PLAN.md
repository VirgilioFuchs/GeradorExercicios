---
phase: 07-continuous-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements:
  - CI-01
  - CI-02
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Push or PR targeting master starts one GitHub Actions workflow named CI with a single job named test (D-01; D-04; D-15; CI-01; CI-02)"
    - "The job runs on ubuntu-latest with Python 3.11, installs via pip install -r exercise-ai/requirements.txt, then runs pytest exercise-ai -q from the checkout root (D-02; D-06; D-07; D-08; D-09; D-10)"
    - "The workflow never declares or injects LLM provider API secrets into the job environment (D-11; D-12; D-13; CI-01)"
    - "Pytest nonzero exit fails the job with no continue-on-error, producing a visible red check on the PR (D-14; CI-02)"
    - "README Como testar notes that push/PR to master run the CI workflow with the same local pytest command (D-17; D-18; D-19)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Single-job GitHub Actions workflow: checkout, setup-python 3.11, pip install, pytest"
      contains: "pytest exercise-ai -q"
    - path: "README.md"
      provides: "One-line CI note under Como testar; local command unchanged"
      contains: "GitHub Actions"
  key_links:
    - from: "on.push / on.pull_request"
      to: "branches: [master]"
      via: "workflow trigger filters (not main)"
      pattern: "master"
    - from: "pip install -r exercise-ai/requirements.txt"
      to: "pytest exercise-ai -q"
      via: "same README Setup + Como testar contract on runner"
      pattern: "exercise-ai/requirements.txt"
    - from: "pytest exit code"
      to: "GitHub PR check status"
      via: "default Actions job failure → red check"
      pattern: "pytest exercise-ai -q"
  prohibitions:
    - "No matrix OS/Python, ruff/mypy, coverage gate, Docker, tox, pre-commit Actions, required cache, release/publish, concurrency groups (D-03)"
    - "No workflow_dispatch, tag triggers, or all-branches triggers (D-05)"
    - "No pyproject.toml / packaging install requirement (D-10)"
    - "No live-LLM smoke job; no Actions secrets for LLM providers (D-11; D-12)"
    - "No branch-protection / Rulesets API automation (D-16)"
    - "No separate CI strategy docs (D-19)"
    - "No new PyPI packages; no Python app code changes"
    - "SEED-002 token usage logging is out of scope"
---

## Phase Goal

GitHub Actions runs the existing offline pytest suite on every push/PR to `master` without LLM API secrets, with a visible green/red check on the PR.

## Spec probe

- CONTEXT locked D-01…D-19; deferred ideas not planned.
- RESEARCH: pin `actions/checkout@v7` + `actions/setup-python@v7`; optional pip cache omitted; `permissions: contents: read`; triggers on `master`.
- Discretion filled: file `.github/workflows/ci.yml`, workflow `name: CI`, job `test`, no pip cache, no D-13 explicit env unset, paths from repo root.

<objective>
Deliver CI-01 and CI-02 as one production-quality tracer: a single GitHub Actions workflow that installs deps and runs `pytest exercise-ai -q` offline, then a short README note.

Purpose: Make regression visible on push/PR without inventing CI platform scope or touching the Python pipeline.
Output: `.github/workflows/ci.yml` + one README sentence under Como testar.

Decision coverage: D-01 through D-19 (cited in tasks). Deferred not planned: matrix, lint, coverage, Docker, branch-protection API, failover, SEED-002.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-continuous-integration/07-CONTEXT.md
@.planning/phases/07-continuous-integration/07-RESEARCH.md
@.planning/phases/07-continuous-integration/07-PATTERNS.md
@README.md
@exercise-ai/requirements.txt
@exercise-ai/tests/conftest.py
</context>

<proven_verify_commands>
**Reuse prior phase grounding from repo root.**

- Canonical suite (Phase 3 / README): `pytest exercise-ai -q`
- This plan adds workflow-file structural checks (existence + trigger/command shape + secret-omission) alongside that suite command.
</proven_verify_commands>

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | Push/PR runs Actions install+pytest without live LLM; red/green check visible | 07-01 | COVERED | ROADMAP success 1–4 |
| REQ | CI-01 | Install deps + pytest offline; no API secrets in job | 07-01 T1 | COVERED | |
| REQ | CI-02 | Failed tests → red check / merge confidence via visible status | 07-01 T1–T2 | COVERED | visible check; no Rulesets API |
| RESEARCH | — | checkout@v7 + setup-python@v7; master triggers; no LLM secrets | 07-01 T1 | COVERED | |
| RESEARCH | — | Optional pip cache needs cache-dependency-path | 07-01 T1 | COVERED | omitted (discretion + D-03) |
| RESEARCH | — | permissions contents:read | 07-01 T1 | COVERED | included (discretion) |
| CONTEXT | D-01 | One job, one workflow file | 07-01 T1 | COVERED | |
| CONTEXT | D-02 | Mirror README install + pytest | 07-01 T1 | COVERED | |
| CONTEXT | D-03 | No matrix/lint/coverage/Docker/etc. | 07-01 T1 | COVERED | prohibitions |
| CONTEXT | D-04 | push + pull_request on master | 07-01 T1 | COVERED | |
| CONTEXT | D-05 | No workflow_dispatch/tags/all branches | 07-01 T1 | COVERED | |
| CONTEXT | D-06 | ubuntu-latest only | 07-01 T1 | COVERED | |
| CONTEXT | D-07 | Python 3.11 only | 07-01 T1 | COVERED | |
| CONTEXT | D-08 | pip install -r exercise-ai/requirements.txt | 07-01 T1 | COVERED | |
| CONTEXT | D-09 | pytest exercise-ai -q from checkout root | 07-01 T1 | COVERED | costly |
| CONTEXT | D-10 | No pyproject packaging install | 07-01 T1 | COVERED | |
| CONTEXT | D-11 | No LLM API secrets in workflow | 07-01 T1 | COVERED | costly |
| CONTEXT | D-12 | Trust existing mock suite; no live smoke | 07-01 T1 | COVERED | |
| CONTEXT | D-13 | Optional unset env — skip | 07-01 T1 | COVERED | discretion: omit |
| CONTEXT | D-14 | Pytest fail → job fail → red check | 07-01 T1 | COVERED | |
| CONTEXT | D-15 | Stable names CI / test | 07-01 T1 | COVERED | |
| CONTEXT | D-16 | No branch-protection API | 07-01 T1–T2 | COVERED | |
| CONTEXT | D-17 | README one line; no branch-protection tutorial | 07-01 T2 | COVERED | |
| CONTEXT | D-18 | Update Como testar; same local command | 07-01 T2 | COVERED | |
| CONTEXT | D-19 | No separate CI strategy docs | 07-01 T2 | COVERED | |

Deferred (not planned): matrix multi-OS/Python; pip cache/concurrency polish; ruff/typecheck/coverage; Rulesets API; workflow_dispatch/all branches; Phase 8 failover; BNCC/MySQL; SEED-002.

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Workflow file | `.github/workflows/ci.yml` | RESEARCH + PATTERNS recommendation |
| Workflow `name:` | `CI` | D-15 stable operator-visible name |
| Job id | `test` | D-15 |
| `permissions` | `contents: read` | least privilege (discretion) |
| pip cache | omit | D-03 not required; RESEARCH open Q2 |
| D-13 unset keys | omit | never inject → unset unnecessary |
| Working directory | repo root paths only | D-09; no `working-directory: exercise-ai` |

<tasks>

<task type="tracer">
  <name>End-to-end CI workflow — one job, offline pytest</name>
  <reversibility rating="costly">Canonical CI command and no-LLM-secret contract (D-09, D-11) become the documented regression surface; changing them later needs coordinated README and operator expectation updates.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <action>
  Create greenfield `.github/workflows/ci.yml` (only workflow file this phase — D-01). Do not modify any Python under `exercise-ai/`. Do not add PyPI packages.

  Workflow shape (discretion filled; honor locked decisions):
  - Top-level `name: CI` and single job id `test` (D-15).
  - Triggers: `push` and `pull_request` with `branches: [master]` only — never `main`, never `workflow_dispatch`, never tags, never all-branches (D-04, D-05).
  - `permissions: contents: read` (discretion).
  - `runs-on: ubuntu-latest` (D-06). No strategy/matrix (D-03).
  - Steps in order: (1) `actions/checkout@v7`; (2) `actions/setup-python@v7` with `python-version: '3.11'` (D-07) — omit `cache: pip` (D-03 / discretion); (3) Install: upgrade pip then `pip install -r exercise-ai/requirements.txt` (D-02, D-08, D-10 — no packaging install); (4) Run tests: `pytest exercise-ai -q` with default cwd = `$GITHUB_WORKSPACE` root (D-09). Do not set `defaults.run.working-directory` to `exercise-ai`.
  - Do not set `continue-on-error` on the pytest step (D-14 / CI-02).
  - Do not declare job/step `env` mappings that pass provider API secrets from GitHub Secrets; do not add a live-API smoke job (D-11, D-12, D-13 skip). Rely on the existing mock suite.
  - Do not automate branch protection / required checks via API (D-16).

  Mirror RESEARCH Pattern 1 / PATTERNS skeleton for structure. Pin action majors `@v7` (not stale tutorial `@v5`/`@v6`). Use `pull_request` (not `pull_request_target`).
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors, or collection far below the known ~73 tests</fails_when>
    <automated>python -c "from pathlib import Path; p=Path('.github/workflows/ci.yml'); t=p.read_text(encoding='utf-8'); assert p.is_file(); assert 'name: CI' in t; assert 'master' in t; assert 'ubuntu-latest' in t; assert '3.11' in t; assert 'actions/checkout@v7' in t; assert 'actions/setup-python@v7' in t; assert 'pip install -r exercise-ai/requirements.txt' in t; assert 'pytest exercise-ai -q' in t; assert 'continue-on-error' not in t; assert 'workflow_dispatch' not in t; assert 'pull_request_target' not in t; assert 'matrix:' not in t; low=t.lower(); assert 'llm_api_key' not in low; assert 'gemini_api_key' not in low; assert 'secrets.' not in low; print('ci.yml shape OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or missing ci.yml</fails_when>
  </verify>
  <done>
  `.github/workflows/ci.yml` exists as the sole CI workflow: one job `test`, triggers on push/PR to master, ubuntu-latest + Python 3.11, README-mirrored install and `pytest exercise-ai -q` at root, actions pinned @v7, no LLM secret injection, no continue-on-error, no matrix — satisfying CI-01 orchestration and CI-02 fail-red semantics (D-01…D-16).
  </done>
</task>

<task type="auto">
  <name>README CI one-liner under Como testar</name>
  <reversibility rating="reversible">Docs-only sentence; easy to edit or remove.</reversibility>
  <files>README.md</files>
  <action>
  Update `README.md` section `## Como testar` only (D-17, D-18). After the existing local `pytest exercise-ai -q` fenced command, add exactly one short sentence stating that push/PR to `master` run the GitHub Actions workflow `CI` with the same command. Suggested wording (may tweak lightly for grammar): Em push/PR para `master`, o workflow GitHub Actions `CI` executa o mesmo comando.

  Keep the local pytest command string identical to the job (D-02, D-09, D-18). Do not add a branch-protection tutorial (D-17). Do not create any separate CI strategy document (D-19). Do not change Setup, env-var tables, or Python app docs beyond this one sentence. SEED-002 remains untouched.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); i=t.index('## Como testar'); chunk=t[i:i+500]; assert 'pytest exercise-ai -q' in chunk; assert 'GitHub Actions' in chunk or 'GitHub Action' in chunk; assert 'master' in chunk; assert 'CI' in chunk; print('README CI note OK')"</automated>
    <fails_when>AssertionError, non-zero exit, or Como testar missing the CI mention / pytest command</fails_when>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, or summary shows failed/errors</fails_when>
  </verify>
  <done>
  README Como testar documents that push/PR to master run workflow CI with the same `pytest exercise-ai -q` command; no extra CI docs; local command unchanged (D-17, D-18, D-19; CI-02 operator visibility).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event → Actions runner | Untrusted PR code runs on hosted runner with `GITHUB_TOKEN` |
| Workflow YAML → job env | Config decides whether provider API secrets enter the job |
| Job → PyPI install | Dependency install from committed `requirements.txt` (no new packages this phase) |
| Job → pytest suite | Offline mocks; must not require live LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `.github/workflows/ci.yml` env/secrets | critical | mitigate | Never inject `LLM_API_KEY` or `GEMINI_API_KEY` (or other LLM provider secrets) into the workflow; omit `secrets.*` mappings for those keys (D-11; CI-01). Verify via automated shape assert on ci.yml. |
| T-07-02 | Information Disclosure | App logs during CI | medium | mitigate | Rely on existing suite + log redaction tests; do not add live provider calls (D-12). |
| T-07-03 | Elevation of Privilege | `GITHUB_TOKEN` | medium | mitigate | Set `permissions: contents: read` on the workflow. |
| T-07-04 | Tampering / RCE | Workflow trigger type | high | mitigate | Use `pull_request` only — never `pull_request_target` (D-04). |
| T-07-05 | Denial of Service (silent skip) | `on:` branch filters | medium | mitigate | Filter `master` (repo HEAD), not `main` (D-04). |
| T-07-06 | Tampering (false green) | pytest step | high | mitigate | No `continue-on-error` on the test step so failures stay red (D-14; CI-02). |
| T-07-SC | Tampering | pip installs | low | accept | No new PyPI packages; install only from committed `exercise-ai/requirements.txt`. Official `actions/*` pins @v7 already audited OK in RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- Local proxy for the job command: `pytest exercise-ai -q` passes from repo root.
- `ci.yml` shape assert: master triggers, 3.11, checkout@v7, setup-python@v7, requirements install, root pytest, no secret key names, no `secrets.`, no matrix, no continue-on-error.
- README Como testar mentions GitHub Actions CI on master with the same command.
- After first push to GitHub (operator/runtime): Actions run named CI / job test appears green or red on the PR — platform behavior, not automated in-plan beyond YAML contract.
</verification>

<success_criteria>
- CI-01: Workflow installs deps and runs offline pytest with no LLM API secrets in the job.
- CI-02: Pytest failure fails the job (visible red check); success is green; no Rulesets API required.
- All locked D-01…D-19 honored; deferred ideas and SEED-002 not implemented.
- No Python app changes; no new PyPI packages.
</success_criteria>

<output>
Create `.planning/phases/07-continuous-integration/07-01-SUMMARY.md` when done
</output>
