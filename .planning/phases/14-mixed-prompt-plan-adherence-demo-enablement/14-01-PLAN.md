---
phase: 14-mixed-prompt-plan-adherence-demo-enablement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/prompts.py
  - exercise-ai/models.py
  - exercise-ai/reliability.py
  - exercise-ai/tests/test_prompts.py
  - exercise-ai/tests/test_models.py
  - exercise-ai/tests/test_reliability.py
autonomous: true
requirements:
  - PROMPT-01
  - VAL-01
  - VAL-02
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "User prompt always includes a numbered slot list from request.itens_ordenados (uniform and mixed) with hybrid PT+enum labels (D-01..D-04; PROMPT-01)"
    - "Prompt does not re-list enunciado/resposta/explicacao field contracts — Structured Outputs remain format authority (D-03; SEED-008)"
    - "reliability.generate_validated_batch calls validate_exercise_batch then verify_plan_echo; plan echo is not invoked from validator.py (D-05, D-06; VAL-01)"
    - "verify_plan_echo fails closed on per-slot dificuldade mismatch and on batch.dificuldades ≠ request.dificuldades summary (D-07)"
    - "Plan-echo ValueError enters the existing bounded RELY path (same as validation); math_check / retry bounds / failover are not redesigned (D-08; VAL-02)"
  artifacts:
    - path: "exercise-ai/prompts.py"
      provides: "Slot enumeration in user prompt from itens_ordenados; hybrid labels; no field-list prose"
      contains: "itens_ordenados|fácil \\(facil\\)|build_prompts"
    - path: "exercise-ai/models.py"
      provides: "verify_plan_echo extended for batch dificuldades summary (D-07)"
      contains: "verify_plan_echo|dificuldades"
    - path: "exercise-ai/reliability.py"
      provides: "Post-validate plan-echo hook inside generate_validated_batch"
      contains: "verify_plan_echo|validate_exercise_batch"
    - path: "exercise-ai/tests/test_reliability.py"
      provides: "Offline mismatch → RELY regeneration / exhaustion path"
      contains: "verify_plan_echo|echo mismatch|max_retries"
  key_links:
    - from: "GenerationRequest.itens_ordenados"
      to: "USER prompt slot list"
      via: "build_prompts"
      pattern: "itens_ordenados|build_prompts"
    - from: "validate_exercise_batch"
      to: "verify_plan_echo"
      via: "generate_validated_batch order (validate then echo)"
      pattern: "validate_exercise_batch|verify_plan_echo"
    - from: "verify_plan_echo ValueError"
      to: "bounded RELY retry / InvalidRequestError(kind=validation_exhausted)"
      via: "existing except ValueError in generate_validated_batch"
      pattern: "validation_exhausted|ValueError"
  prohibitions:
    - "Do not call verify_plan_echo from validator.py (D-05)"
    - "Do not redesign RELY loop bounds, math_check, or failover (D-08; VAL-02)"
    - "Do not re-introduce response field-list prose in the user prompt (D-03)"
    - "Do not implement demo band UX or serve.py plano wiring (DEMO-01 / Plan 14-02)"
    - "Do not implement CLI/wizard compact plano UX (UX-01/UX-02 / Phase 15)"
---

<objective>
Wire mixed (and uniform) plan instruction into prompts and plan-adherence into the existing RELY loop: numbered slots from `itens_ordenados`, `verify_plan_echo` after validate (including batch `dificuldades` summary), offline proof that echo mismatch retries — without redesigning math_check or RELY bounds.

Purpose: Close PROMPT-01 + VAL-01 + VAL-02 so mixed batches are instructed and fail-closed before demo enablement.
Output: Updated `prompts.py` / `verify_plan_echo` / `reliability.py` plus offline prompt + RELY tests; `pytest exercise-ai -q` green.
</objective>

<execution_context>
@C:/Users/Anglo/.cursor/gsd-core/workflows/execute-plan.md
@C:/Users/Anglo/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md
@.planning/phases/13-request-schema-plan-contract/13-CONTEXT.md
@.planning/phases/13-request-schema-plan-contract/13-01-SUMMARY.md
@exercise-ai/prompts.py
@exercise-ai/models.py
@exercise-ai/validator.py
@exercise-ai/reliability.py
@exercise-ai/tests/test_models.py
@exercise-ai/tests/test_reliability.py
</context>

<proven_verify_commands>
**Reuse from repo root:**

- Targeted models: `pytest exercise-ai/tests/test_models.py -q`
- Canonical suite: `pytest exercise-ai -q`
</proven_verify_commands>

## Discretion locked by this plan

| Choice | Value | Rationale |
|--------|-------|-----------|
| Slot list format | Numbered 1-based lines: `1. fácil (facil)` style hybrid labels | CONTEXT D-01/D-04 example |
| Uniform prompt | Same enumeration shape (N lines of one band) — no scalar-only branch that omits the list | D-02 |
| Field-list prose | Remove the "enunciado/resposta/explicacao" bullet block from USER template | D-03 / SEED-008 |
| Batch summary check | Extend `verify_plan_echo` in `models.py` (same function) | CONTEXT discretion; keeps one adherence entrypoint |
| RELY import | `from models import verify_plan_echo` inside reliability (or top-level) after validate | D-05/D-06 |

## Source audit

| Source | Item | Plan coverage |
|--------|------|---------------|
| GOAL | Mixed prompt + plan adherence via RELY | Tasks 1–2 |
| REQ PROMPT-01 | Enumerate slot → dificuldade | Task 1 |
| REQ VAL-01 | Count + per-slot adherence | Task 1 (echo) + existing validate length |
| REQ VAL-02 | RELY + math_check unchanged | Task 1 wire; Task 2 prohibitions |
| CONTEXT D-01..D-04 | Prompt slots / always enum / no field list / hybrid | Task 1 |
| CONTEXT D-05..D-08 | RELY hook order + batch summary + no redesign | Tasks 1–2 |
| CONTEXT D-09..D-15 | Demo | Deferred to 14-02 |
| Deferred | UX-01/02, SEED-005/007, TIPO-OPEN | Prohibitions |

## Artifacts this phase produces

| Symbol / artifact | Kind | Location |
|-------------------|------|----------|
| Slot enumeration block in user prompt | prompt text | `exercise-ai/prompts.py` (`USER_PROMPT_TEMPLATE` / `build_prompts`) |
| Hybrid label helper (optional small local fn) | function (optional) | `exercise-ai/prompts.py` |
| `verify_plan_echo` batch-summary branch | function extension | `exercise-ai/models.py` |
| Post-validate `verify_plan_echo` call | call site | `exercise-ai/reliability.py` → `generate_validated_batch` |
| `exercise-ai/tests/test_prompts.py` | test module (create if absent) | `exercise-ai/tests/` |
| RELY echo-mismatch offline test(s) | tests | `exercise-ai/tests/test_reliability.py` and/or `test_models.py` |

<tasks>

<task type="tracer" tdd="true">
  <name>Tracer: prompt slots + verify_plan_echo after validate + RELY on mismatch</name>
  <reversibility rating="costly">Wiring plan echo into RELY locks host-visible failure semantics for mixed batches (Phase 13 D-11 carry-forward). Reversible by code change (no migration) — flag costly; not one-way; no checkpoint:decision.</reversibility>
  <files>exercise-ai/prompts.py, exercise-ai/models.py, exercise-ai/reliability.py, exercise-ai/tests/test_prompts.py, exercise-ai/tests/test_models.py, exercise-ai/tests/test_reliability.py</files>
  <read_first>
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md
exercise-ai/prompts.py
exercise-ai/models.py (itens_ordenados, verify_plan_echo)
exercise-ai/reliability.py (generate_validated_batch validate try/except)
exercise-ai/validator.py (confirm: do not add plan echo here)
exercise-ai/tests/test_reliability.py
exercise-ai/tests/test_models.py (existing verify_plan_echo tests)
  </read_first>
  <action>
    End-to-end tracer slice for PROMPT-01 + VAL-01/02 (library path only; no demo):

    1. **Prompt (D-01..D-04):** Update `build_prompts` / `USER_PROMPT_TEMPLATE` so the user prompt:
       - Always builds a numbered slot list from `request.itens_ordenados` (uniform and mixed — D-02).
       - Uses hybrid labels: `fácil (facil)`, `médio (medio)`, `difícil (dificil)` (D-04).
       - Drops the response field-list bullets (`enunciado` / `resposta` / `explicacao`) from prompt prose (D-03).
       - Keeps matéria/tópico/quantidade framing; exact surrounding wording is implementer discretion.

    2. **Plan echo (D-07):** Extend pure `verify_plan_echo(batch, request)` so it still fails on per-slot `dificuldade` mismatch **and** fails when `batch.dificuldades` ≠ `request.dificuldades` (order-sensitive summary). Do not mutate batch. Do not move this into `validator.py`.

    3. **RELY wire (D-05, D-06):** In `generate_validated_batch`, after `validate_exercise_batch(batch, request)` succeeds, call `verify_plan_echo(validated, request)` (or on the same object) before `return`. Any `ValueError` from echo must hit the **existing** `except ValueError` retry / exhaustion path (no new loop, no new max_retries semantics — D-08).

    4. **Offline tracer tests (TDD):**
       - Prompt: mixed request → user prompt contains numbered hybrid lines for each slot; uniform request still enumerates N slots of one band.
       - Models: batch `dificuldades` summary mismatch raises from `verify_plan_echo`.
       - RELY: mock `generate_exercises` to return a structurally valid batch with wrong slot echo (and math that would pass or bypass math via fixture that validate accepts); with `max_retries=1`, assert a second generate call (retry) or exhaustion `InvalidRequestError(kind=validation_exhausted)` when never corrected — prove mismatch enters RELY path.

    Do not touch `demo/`, CLI, wizard, or `validator.py` plan logic.
  </action>
  <acceptance_criteria>
    - `build_prompts` user text for a mixed `plano` request includes numbered hybrid slot labels derived from `itens_ordenados`
    - User prompt for the same request does not contain a field-contract bullet list naming enunciado/resposta/explicacao as required response keys
    - `rg "verify_plan_echo" exercise-ai/reliability.py` matches; `rg "verify_plan_echo" exercise-ai/validator.py` returns no matches
    - In `generate_validated_batch`, `verify_plan_echo` runs only after successful `validate_exercise_batch`
    - Offline test shows echo mismatch raises `ValueError` that is handled by the existing RELY `except ValueError` path (retry or `validation_exhausted`)
    - `pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_reliability.py -q` passes (plus new prompt tests if in a separate module)
  </acceptance_criteria>
  <verify>
    <automated>pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_reliability.py -q</automated>
    <fails_when>non-zero exit, collection errors, or summary shows failed tests</fails_when>
  </verify>
  <done>Prompt enumerates slots; verify_plan_echo (slots + summary) runs after validate inside RELY; offline mismatch→RELY proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Hardening: prompt/VAL coverage + suite green; no RELY redesign</name>
  <reversibility rating="reversible">Coverage and small prompt wording tweaks only; RELY bounds stay as Phase 5/6.</reversibility>
  <files>exercise-ai/prompts.py, exercise-ai/models.py, exercise-ai/tests/test_prompts.py, exercise-ai/tests/test_models.py, exercise-ai/tests/test_reliability.py</files>
  <read_first>
exercise-ai/prompts.py
exercise-ai/reliability.py (after Task 1)
exercise-ai/tests/test_reliability.py (single-loop assertion if present)
.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-CONTEXT.md (D-08)
  </read_first>
  <action>
    Close remaining PROMPT/VAL edge coverage without expanding scope:

    1. Ensure prompt tests cover **always enumerate** for uniform (`dificuldade` + `quantidade`, no `plano`) — N hybrid lines of the same band (D-02).
    2. Ensure `verify_plan_echo` tests cover: happy path; per-slot mismatch; batch `dificuldades` mismatch; length mismatch (already partially present — extend as needed).
    3. Confirm `validate_exercise_batch` still owns length/emptiness + `check_math_batch` only — no plan-echo import in `validator.py` (D-05).
    4. Confirm RELY still has a single retry loop and math_check is unchanged (VAL-02 / D-08) — reuse existing reliability structural assertions; do not add a second loop or change `resolve_max_retries` bounds.
    5. Run full offline suite: `pytest exercise-ai -q`.

    Do not start demo work (Plan 14-02).
  </action>
  <acceptance_criteria>
    - Uniform and mixed prompt tests both assert numbered hybrid slot lists
    - `verify_plan_echo` fails on batch `dificuldades` summary drift
    - `validator.py` has no `verify_plan_echo` reference
    - Existing RELY bound / single-loop tests still pass
    - `pytest exercise-ai -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest exercise-ai -q</automated>
    <fails_when>non-zero exit, collection errors, or any failed/errored tests in the summary</fails_when>
  </verify>
  <done>PROMPT-01 + VAL-01 + VAL-02 library path complete; full exercise-ai suite green; demo untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → ExerciseBatch | Model may mismatch slot/band echoes; must fail closed into RELY |
| Host → generate_validated_batch | Same typed errors as other validation failures (`validation_exhausted`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering / Integrity | slot vs echo | medium | mitigate | `verify_plan_echo` after validate; RELY bounded retry; no silent correct |
| T-14-02 | Tampering | batch dificuldades summary | medium | mitigate | D-07 summary check in `verify_plan_echo` |
| T-14-03 | Elevation / Confusion | plan check in wrong layer | low | mitigate | Keep echo out of `validator.py` (D-05) |
| T-14-SC | Supply chain | deps | low | accept | No new packages |
</threat_model>

<verification>
- `pytest exercise-ai/tests/test_models.py exercise-ai/tests/test_reliability.py -q` after Task 1
- `pytest exercise-ai -q` after Task 2
- `rg "verify_plan_echo" exercise-ai/reliability.py` matches
- `rg "verify_plan_echo" exercise-ai/validator.py` has no matches
</verification>

<success_criteria>
- Prompts always enumerate slot → hybrid dificuldade
- Plan adherence runs after validate inside RELY; mismatches retry/exhaust like other ValueErrors
- math_check and RELY bounds unchanged
- Offline suite green
</success_criteria>

<output>
Create `.planning/phases/14-mixed-prompt-plan-adherence-demo-enablement/14-01-SUMMARY.md` when done
</output>
