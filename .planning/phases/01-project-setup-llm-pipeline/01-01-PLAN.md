---
phase: 01-project-setup-llm-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/requirements.txt
  - exercise-ai/.env.example
  - .gitignore
  - exercise-ai/models.py
  - exercise-ai/prompts.py
  - exercise-ai/generator.py
  - exercise-ai/validator.py
  - exercise-ai/main.py
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02
  - SCAF-03
  - MODL-01
  - MODL-02
  - MODL-03
  - PRMT-01
  - PRMT-02
  - PRMT-03
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - CLI-01
  - CLI-02
  - CLI-03
user_setup:
  - service: openai
    why: "OpenAI API access is required for exercise generation"
    env_vars:
      - name: LLM_API_KEY
        source: "OpenAI Platform Dashboard → API keys → Create new secret key"
    local_dev:
      - "Copy exercise-ai/.env.example to exercise-ai/.env and populate LLM_API_KEY"

must_haves:
  truths:
    - "Running main.py loads LLM_API_KEY from environment/.env and executes the generation pipeline"
    - "Generator invokes OpenAI API with Structured Outputs (ExerciseBatch schema) and returns typed Pydantic models"
    - "CLI outputs valid JSON containing 'exercicios' list with 'enunciado', 'resposta', and 'explicacao' fields formatted with UTF-8 Portuguese text"
    - "Missing LLM_API_KEY or API communication failure raises clear descriptive errors without crashing with unhandled tracebacks"
  artifacts:
    - path: "exercise-ai/models.py"
      provides: "Pydantic data schemas for request, single exercise, and batch"
      contains: "class ExerciseBatch"
    - path: "exercise-ai/prompts.py"
      provides: "Centralized prompt templates for math exercise generation"
      contains: "def build_prompts"
    - path: "exercise-ai/generator.py"
      provides: "OpenAI client wrapper with Structured Outputs parsing"
      contains: "def generate_exercises"
    - path: "exercise-ai/validator.py"
      provides: "Phase 1 validator stub and type check"
      contains: "def validate_exercise_batch"
    - path: "exercise-ai/main.py"
      provides: "CLI entry point and pipeline orchestrator"
      contains: "def run_demo"
    - path: "exercise-ai/requirements.txt"
      provides: "Documented minimum dependencies"
      contains: "openai>="
    - path: "exercise-ai/.env.example"
      provides: "Environment variable template for API key"
      contains: "LLM_API_KEY="
    - path: ".gitignore"
      provides: "Git ignore configuration for .env and Python caches"
      contains: ".env"
  key_links:
    - from: "exercise-ai/main.py"
      to: "exercise-ai/generator.py"
      via: "invokes generate_exercises(request)"
      pattern: "generate_exercises\\(request\\)"
    - from: "exercise-ai/generator.py"
      to: "exercise-ai/prompts.py"
      via: "calls build_prompts(request)"
      pattern: "build_prompts\\(request\\)"
    - from: "exercise-ai/generator.py"
      to: "exercise-ai/models.py"
      via: "passes ExerciseBatch as response_format"
      pattern: "response_format=ExerciseBatch"
    - from: "exercise-ai/main.py"
      to: "exercise-ai/validator.py"
      via: "calls validate_exercise_batch(batch, request)"
      pattern: "validate_exercise_batch\\("
---

<objective>
Implement the core project scaffolding and the end-to-end LLM generation pipeline as a vertical Walking Skeleton inside `exercise-ai/`.

Purpose: Establish the foundational architecture for the math exercise generator, validating that parameterized input flows through prompt generation, OpenAI Structured Outputs, typed Pydantic models, and clean CLI JSON output.
Output: Working Python package in `exercise-ai/` with `main.py`, `generator.py`, `validator.py`, `prompts.py`, `models.py`, `requirements.txt`, `.env.example`, and `.gitignore`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-setup-llm-pipeline/01-CONTEXT.md
@.planning/phases/01-project-setup-llm-pipeline/01-RESEARCH.md
@AGENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup project scaffolding, environment configuration, and Pydantic data models</name>
  <files>exercise-ai/requirements.txt, exercise-ai/.env.example, .gitignore, exercise-ai/models.py</files>
  <read_first>AGENT.md, .planning/phases/01-project-setup-llm-pipeline/01-RESEARCH.md, .planning/phases/01-project-setup-llm-pipeline/01-CONTEXT.md</read_first>
  <action>
    Create the directory structure and foundational configuration files in `exercise-ai/` (D-07, D-08):
    1. Create `exercise-ai/requirements.txt` with pinned minimum dependencies:
       - `openai>=1.50.0`
       - `pydantic>=2.0.0`
       - `python-dotenv>=1.0.0`
       - `pytest>=8.0.0`
    2. Create `exercise-ai/.env.example` documenting `LLM_API_KEY=` without real secrets (D-03).
    3. Create or update `.gitignore` in repository root to ignore `.env`, `__pycache__/`, `.pytest_cache/`, `*.pyc`, `.venv/`, and `venv/`.
    4. Create `exercise-ai/models.py` using Pydantic v2 (D-04) with strict schemas for OpenAI Structured Outputs:
       - Define `DificuldadeEnum(str, Enum)` with members `FACIL = "facil"`, `MEDIO = "medio"`, `DIFICIL = "dificil"` (D-06).
       - Define `GenerationRequest(BaseModel)` with fields:
         - `materia: str = Field(default="Matemática", description="Matéria dos exercícios")`
         - `topico: str = Field(..., description="Tópico específico da matéria")`
         - `dificuldade: DificuldadeEnum = Field(..., description="Nível de dificuldade")`
         - `quantidade: int = Field(..., gt=0, description="Quantidade exata de exercícios a gerar")`
       - Define `Exercise(BaseModel)` with Portuguese fields (D-05):
         - `enunciado: str = Field(..., description="Texto do enunciado do exercício")`
         - `resposta: str = Field(..., description="Resposta correta ou solução direta")`
         - `explicacao: str = Field(..., description="Explicação passo a passo da resolução")`
       - Define `ExerciseBatch(BaseModel)` with Portuguese field (D-05):
         - `exercicios: list[Exercise] = Field(..., description="Lista de exercícios gerados")`
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'exercise-ai'); from models import DificuldadeEnum, GenerationRequest, Exercise, ExerciseBatch; r = GenerationRequest(topico='Equação do 1º grau', dificuldade=DificuldadeEnum.FACIL, quantidade=3); e = Exercise(enunciado='2x = 4', resposta='x = 2', explicacao='Divida por 2'); b = ExerciseBatch(exercicios=[e]); assert len(b.exercicios) == 1; assert b.exercicios[0].enunciado == '2x = 4'; print('Models verified successfully')"</automated>
    <fails_when>AssertionError, ImportError, ValidationError, or non-zero exit code</fails_when>
  </verify>
  <acceptance_criteria>
    - `exercise-ai/requirements.txt` contains `openai>=1.50.0`, `pydantic>=2.0.0`, `python-dotenv>=1.0.0`, and `pytest>=8.0.0`
    - `exercise-ai/.env.example` contains `LLM_API_KEY=` (D-03)
    - `.gitignore` contains `.env` and `__pycache__`
    - `exercise-ai/models.py` exports `DificuldadeEnum` (D-06), `GenerationRequest`, `Exercise`, `ExerciseBatch` (D-04)
    - Pydantic models use exact Portuguese field names: `enunciado`, `resposta`, `explicacao`, `exercicios`, `materia`, `topico`, `dificuldade`, `quantidade` (D-05)
    - Files created in `exercise-ai/` layout (D-07, D-08)
  </acceptance_criteria>
  <done>Scaffolding files exist and Pydantic models pass type checking and instantiation tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement prompt templates, OpenAI Structured Outputs generator, and validator stub</name>
  <files>exercise-ai/prompts.py, exercise-ai/generator.py, exercise-ai/validator.py</files>
  <read_first>exercise-ai/models.py, AGENT.md, .planning/phases/01-project-setup-llm-pipeline/01-RESEARCH.md</read_first>
  <action>
    1. Create `exercise-ai/prompts.py` (D-13):
       - Define `SYSTEM_PROMPT` instructing the model as an expert Brazilian Portuguese math teacher creating educational exercises, enforcing exact adherence to difficulty, quantity, and structured JSON output only (D-13, D-14).
       - Define `USER_PROMPT_TEMPLATE` requiring parameters `{materia}`, `{topico}`, `{dificuldade}`, `{quantidade}` and setting explicit rules: exact quantity of exercises, topic containment, mandatory `enunciado`, `resposta`, and step-by-step `explicacao` (D-14).
       - Define `build_prompts(request: GenerationRequest) -> tuple[str, str]` which receives `GenerationRequest` and returns formatted `(system_prompt, user_prompt)` strings.
    2. Create `exercise-ai/validator.py` (D-08, D-09):
       - Implement Phase 1 baseline validator stub `validate_exercise_batch(batch: ExerciseBatch, request: GenerationRequest) -> ExerciseBatch` (D-09).
       - Verify that `batch` is an instance of `ExerciseBatch` and that `batch.exercicios` is non-empty. Raise `ValueError` if invalid, otherwise return `batch`. (Detailed field emptiness and count checks are expanded in Phase 2).
    3. Create `exercise-ai/generator.py` (D-01, D-08):
       - Define `get_client() -> OpenAI`: reads `os.getenv("LLM_API_KEY")` (D-03). If missing or empty, raises `ValueError("Chave de API não configurada. Defina a variável LLM_API_KEY no arquivo .env.")` (D-15). Instantiates `OpenAI(api_key=api_key.strip(), timeout=30.0)`.
       - Define `generate_exercises(request: GenerationRequest, model: str = "gpt-4o-mini") -> ExerciseBatch` (D-01, D-02):
         - Calls `prompts.build_prompts(request)` to generate system and user prompts.
         - Executes `client.beta.chat.completions.parse` with `model=model` (D-02), `messages=[{"role": "system", "content": system_prompt}, {"role": "user", "content": user_prompt}]`, and `response_format=ExerciseBatch` (D-01).
         - Inspects `completion.choices[0].message`:
           - If `message.refusal`: raises `RuntimeError(f"O modelo recusou a geração: {message.refusal}")` (D-15, D-16).
           - If `message.parsed is None`: raises `RuntimeError("Não foi possível obter a estrutura de exercícios da resposta do modelo.")` (D-15, D-16).
         - Catches `OpenAIError` and re-raises as `RuntimeError(f"Erro na chamada à API da OpenAI: {exc}") from exc` without silencing exceptions (D-15, D-16).
         - Returns `message.parsed` typed as `ExerciseBatch`.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'exercise-ai'); from models import GenerationRequest, DificuldadeEnum, ExerciseBatch, Exercise; from prompts import build_prompts; from validator import validate_exercise_batch; import generator; req = GenerationRequest(topico='Frações', dificuldade=DificuldadeEnum.MEDIO, quantidade=2); s_p, u_p = build_prompts(req); assert 'Frações' in u_p; assert 'medio' in u_p; batch = ExerciseBatch(exercicios=[Exercise(enunciado='1/2+1/2', resposta='1', explicacao='Soma de denominadores iguais')]); val_batch = validate_exercise_batch(batch, req); assert len(val_batch.exercicios) == 1; print('Prompt and validator pipeline verified')"</automated>
    <fails_when>AssertionError, ImportError, ValueError, or non-zero exit code</fails_when>
  </verify>
  <acceptance_criteria>
    - `exercise-ai/prompts.py` defines `SYSTEM_PROMPT`, `USER_PROMPT_TEMPLATE`, and `build_prompts(request)` in Portuguese (D-13, D-14)
    - `exercise-ai/generator.py` defines `get_client()` loading `LLM_API_KEY` (D-03) with clear missing-key error (D-15)
    - `exercise-ai/generator.py` defines `generate_exercises(request, model)` calling `client.beta.chat.completions.parse` using `gpt-4o-mini` (D-02) and `response_format=ExerciseBatch` (D-01)
    - `exercise-ai/generator.py` checks `message.refusal` and wraps `OpenAIError` without silencing exceptions (D-15, D-16)
    - `exercise-ai/validator.py` defines baseline `validate_exercise_batch(batch, request)` stub (D-09)
  </acceptance_criteria>
  <done>Prompts, generator with Structured Outputs integration, and validator stub are implemented and verified</done>
</task>

<task type="auto">
  <name>Task 3: Implement CLI orchestration and end-to-end execution pipeline in main.py</name>
  <files>exercise-ai/main.py</files>
  <read_first>exercise-ai/models.py, exercise-ai/generator.py, exercise-ai/validator.py, AGENT.md, .planning/phases/01-project-setup-llm-pipeline/01-RESEARCH.md</read_first>
  <action>
    1. Create `exercise-ai/main.py` (D-08):
       - Use `dotenv.load_dotenv()` with multi-path resolution (`Path(__file__).resolve().parent / ".env"` and fallback to `Path(__file__).resolve().parent.parent / ".env"`) so running from either repository root or `exercise-ai/` works seamlessly (D-03).
       - Implement `run_demo()` (D-10):
         - Construct a demonstration `GenerationRequest` matching the canonical AGENT.md specification: `materia="Matemática"`, `topico="Equação do primeiro grau"`, `dificuldade=DificuldadeEnum.FACIL`, `quantidade=3` (D-10, argparse deferred to v2 per D-12).
         - Orchestrate the linear pipeline:
           1. Call `batch = generate_exercises(request)`
           2. Call `validated_batch = validate_exercise_batch(batch, request)` (D-09)
           3. Format output with `json.dumps(validated_batch.model_dump(), indent=2, ensure_ascii=False)` to prevent unicode escaping in Portuguese text (D-11).
           4. Output formatted JSON string to `sys.stdout` (D-11).
       - Implement high-level error handling boundary (D-15, D-16):
         - Catch `ValueError` (config/validation error): print `f"Erro de configuração ou validação: {err}"` to `sys.stderr` and exit with code 1.
         - Catch `RuntimeError` (API/generation error): print `f"Erro na execução da geração: {err}"` to `sys.stderr` and exit with code 1.
         - Catch generic `Exception`: print `f"Erro inesperado: {err}"` to `sys.stderr` and exit with code 1.
       - Add standard Python entry point `if __name__ == "__main__": run_demo()`.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'exercise-ai'); import main; assert hasattr(main, 'run_demo'); print('CLI entry point imported and verified')"</automated>
    <fails_when>ImportError, SyntaxError, or non-zero exit code</fails_when>
  </verify>
  <acceptance_criteria>
    - `exercise-ai/main.py` loads `.env` safely using `load_dotenv` (D-03)
    - `exercise-ai/main.py` defines `run_demo()` with demo parameters (`Matemática`, `Equação do primeiro grau`, `facil`, `3`) (D-10; argparse deferred per D-12)
    - `exercise-ai/main.py` prints JSON output to stdout using `ensure_ascii=False` and `indent=2` (D-11)
    - `exercise-ai/main.py` catches errors and writes descriptive messages to `sys.stderr` with exit code 1 without silencing exceptions (D-15, D-16)
    - `exercise-ai/main.py` contains executable `if __name__ == '__main__':` block
  </acceptance_criteria>
  <done>CLI entry point orchestrates the full pipeline end-to-end with proper error handling and JSON output</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify all 5 modular files exist in `exercise-ai/`: `main.py`, `generator.py`, `validator.py`, `prompts.py`, `models.py`
- [ ] Verify `requirements.txt`, `.env.example`, and `.gitignore` exist and follow security rules (no hardcoded secrets)
- [ ] Run Python verification script checking importability, model instantiation, prompt generation, validator pass-through, and CLI pipeline structure
- [ ] Verify `models.py` schema matches contract: `exercicios` list with `enunciado`, `resposta`, `explicacao`
</verification>

<success_criteria>
- All tasks completed
- Clean separation of concerns between `models`, `prompts`, `generator`, `validator`, and `main`
- Structured Outputs integration configured with `gpt-4o-mini` and `ExerciseBatch` Pydantic model
- Environment variable `LLM_API_KEY` supported via `python-dotenv`
- All 16 Phase 1 requirement IDs satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-llm-pipeline/01-01-SUMMARY.md`
</output>

## Artifacts this phase produces

- `exercise-ai/requirements.txt` — Minimal dependencies (`openai`, `pydantic`, `python-dotenv`, `pytest`)
- `exercise-ai/.env.example` — Environment template documenting `LLM_API_KEY`
- `.gitignore` — Repository ignore configuration for secrets and build caches
- `exercise-ai/models.py` — Pydantic schemas: `DificuldadeEnum`, `GenerationRequest`, `Exercise`, `ExerciseBatch`
- `exercise-ai/prompts.py` — Prompt templates: `SYSTEM_PROMPT`, `USER_PROMPT_TEMPLATE`, `build_prompts`
- `exercise-ai/generator.py` — LLM client: `get_client`, `generate_exercises` using OpenAI Structured Outputs
- `exercise-ai/validator.py` — Validation stub: `validate_exercise_batch`
- `exercise-ai/main.py` — CLI entry point: `run_demo`, linear pipeline orchestration, JSON stdout formatting
