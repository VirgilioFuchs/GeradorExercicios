---
phase: 17-validation-vs-thinking-vs-response-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/skills/generation/TRIAD.md
  - exercise-ai/skills/generation/rules.py
autonomous: true
requirements:
  - CONTRACT-02

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "TRIAD.md exists under exercise-ai/skills/generation/ and is the authoritative CONTRACT-02 text (D-01, D-02, D-04)"
    - "Docs state validation = deterministic code only; thinking = run-level reasoning_effort; response = Exercise Structured Outputs fields only (CONTRACT-02 / D-05–D-08)"
    - "rules.py exposes TRIAD_DOC Path + docstring pointing at TRIAD.md; TODO never-do bullets retargeted to Phase 18 (D-03, D-14)"
    - "Public __all__ remains get_persona_system only; persona.py and prompts.py untouched; no new pytest file (D-10–D-12)"
  artifacts:
    - path: exercise-ai/skills/generation/TRIAD.md
      provides: "Authoritative PT-BR triad (pipeline + Validação + Pensamento callout + Resposta + Fora desta fase) with code-path pointers"
      contains: "prompt → LLM → validate → math_check → verify_plan_echo → RELY"
    - path: exercise-ai/skills/generation/rules.py
      provides: "TRIAD_DOC Path constant, thin docstring, Phase-18 TODO foreshadow bullets"
      exports: ["TRIAD_DOC"]
  key_links:
    - from: exercise-ai/skills/generation/rules.py
      to: exercise-ai/skills/generation/TRIAD.md
      via: "TRIAD_DOC = Path(__file__).with_name('TRIAD.md')"
      pattern: "TRIAD_DOC"
    - from: exercise-ai/skills/generation/TRIAD.md
      to: "validator / math_check / verify_plan_echo / RELY / reasoning / models.Exercise / AGENTS.md"
      via: "short code-path pointers per leg (D-05, D-08)"
      pattern: "validator"
---

<objective>
Ship CONTRACT-02 as an authoritative PT-BR `TRIAD.md` beside the Phase 16 `skills.generation` package, with a thin `rules.py` pointer (`TRIAD_DOC`) and Phase-18-retargeted never-do TODOs — no prompt wiring, no persona edits, no new pytest.

Purpose: Stop hosts/planners from inventing prompt-side validation or confusing API `reasoning_effort` with pedagogical fields or `Exercise` response shape.
Output: `TRIAD.md` + updated `rules.py`; existing smoke test still green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-validation-vs-thinking-vs-response-docs/17-CONTEXT.md
@.planning/phases/17-validation-vs-thinking-vs-response-docs/17-RESEARCH.md
@exercise-ai/skills/generation/rules.py
@exercise-ai/skills/generation/__init__.py
@AGENTS.md

# Prior wave pattern (do not re-expand public API)
@.planning/phases/16-domain-skill-index/16-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Role |
|----------|------|
| `exercise-ai/skills/generation/TRIAD.md` | Authoritative CONTRACT-02 triad text (D-02) |
| `exercise-ai/skills/generation/rules.py` (modified) | `TRIAD_DOC` discovery Path + docstring + Phase-18 TODO retarget (D-03, D-14) |
| `.planning/phases/17-validation-vs-thinking-vs-response-docs/17-01-SUMMARY.md` | Executor summary after completion |

**Not produced:** new pytest files; `__init__` / `persona.py` / `prompts.py` edits; never-do catalog body; authority map; rule loaders.

<tasks>

<task type="tracer">
  <name>Tracer: create TRIAD.md (pipeline + three legs + callout + Fora desta fase)</name>
  <files>exercise-ai/skills/generation/TRIAD.md</files>
  <read_first>
    - .planning/phases/17-validation-vs-thinking-vs-response-docs/17-CONTEXT.md (D-01..D-15)
    - .planning/phases/17-validation-vs-thinking-vs-response-docs/17-RESEARCH.md (Pattern 2 outline + code pointer table + pitfalls)
    - AGENTS.md (LLM not source of truth; pipeline constraints)
    - exercise-ai/models.py (Exercise fields)
    - exercise-ai/reasoning.py (resolve_reasoning_effort / ReasoningLevel)
    - exercise-ai/validator.py, exercise-ai/math_check.py, exercise-ai/reliability.py (validation ownership)
  </read_first>
  <action>
Create `exercise-ai/skills/generation/TRIAD.md` as the authoritative CONTRACT-02 home under the API skill package (D-01, D-04, D-09). Body language PT-BR; keep English identifiers (`reasoning_effort`, `Exercise`, module paths) (D-06).

Required structure (D-05–D-08, D-15) — short sections, not a FAQ:
1. Title stating the generation triad (validation / thinking / response).
2. One-line pipeline exactly: `prompt → LLM → validate → math_check → verify_plan_echo → RELY`, plus a pointer that LLM is not source of truth (cite `AGENTS.md` / project constraints) (D-08).
3. **Validação** — first sentence: validation is deterministic code only after the LLM; pointers covering `validator` / `validate_exercise_batch`, `math_check` / `check_math_batch`, plan-echo `verify_plan_echo`, and RELY / `reliability` (D-05). Do not frame validation as a SYSTEM prompt section.
4. **Pensamento** — `reasoning_effort` is a run-level API knob resolved via `reasoning.py` / `resolve_reasoning_effort`; not an `Exercise` field. Explicit callout: not pedagogical `tipo_raciocinio` (out of v2.2 product scope) (D-07).
5. **Resposta** — Structured Outputs `Exercise` fields only (name `enunciado`, `resposta`, `explicacao`, and optionally `dificuldade`); point at `models.Exercise` — do not dump JSON Schema or redefine format in prompts (authority map is Phase 18).
6. **Fora desta fase** — 2–3 lines only: detailed never-do catalog + authority map → Phase 18 (D-13, D-15).

Header note: if prose elsewhere diverges from this file, fix the other side to match `TRIAD.md` (D-02). Audience = runtime/API package readers, not a Cursor agent skill (D-09).

Do not create loaders, YAML/JSON rule stubs, or edit `persona.py` / `__init__.py` / `prompts.py` / `rules.py` in this task.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('exercise-ai/skills/generation/TRIAD.md'); t=p.read_text(encoding='utf-8'); assert p.is_file(); assert 'Validação' in t; assert 'Pensamento' in t; assert 'Resposta' in t; assert 'Fora desta fase' in t; assert 'prompt → LLM → validate → math_check → verify_plan_echo → RELY' in t; assert 'reasoning_effort' in t; assert 'tipo_raciocinio' in t; assert 'Exercise' in t; assert 'validator' in t; assert 'math_check' in t; assert 'verify_plan_echo' in t; assert 'reasoning' in t; assert 'AGENTS.md' in t or 'AGENTS' in t"</automated>
    <fails_when>
      - TRIAD.md missing under exercise-ai/skills/generation/
      - Missing required section headings (Validação/Pensamento/Resposta/Fora desta fase) or pipeline one-liner
      - Missing reasoning_effort vs tipo_raciocinio callout identifiers
      - File is FAQ-length with JSON Schema dumps instead of short legs + pointers
    </fails_when>
  </verify>
  <done>TRIAD.md exists with pipeline, three triad legs, pedagogy callout, code pointers, and Fora desta fase → Phase 18 (D-01, D-04–D-08, D-15).</done>
</task>

<task type="auto">
  <name>Expansion: rules.py TRIAD_DOC + docstring + Phase-18 TODO retarget</name>
  <files>exercise-ai/skills/generation/rules.py</files>
  <read_first>
    - exercise-ai/skills/generation/rules.py (current Phase 16 stub)
    - exercise-ai/skills/generation/TRIAD.md (must already exist from tracer)
    - .planning/phases/17-validation-vs-thinking-vs-response-docs/17-CONTEXT.md (D-02, D-03, D-14)
    - .planning/phases/17-validation-vs-thinking-vs-response-docs/17-RESEARCH.md (Pattern 1 example shape)
  </read_first>
  <action>
Update only `exercise-ai/skills/generation/rules.py` (D-03, D-14):
- Module docstring: short PT-BR bridge stating authoritative text lives in `TRIAD.md` (same directory); if this module and `TRIAD.md` diverge, fix to match `TRIAD.md` (D-02).
- `from pathlib import Path` and `TRIAD_DOC = Path(__file__).with_name("TRIAD.md")` (D-03).
- Retarget TODO header from Phases 17–18 triad work to Phase 18 never-do / authority map; keep the three existing never-do foreshadow bullets; add a one-line pointer that the triad lives in `TRIAD.md` / `TRIAD_DOC` (D-14).
- Keep module importable (`pass` allowed after constant + comments).

Do not re-export `TRIAD_DOC` from `skills.generation.__init__` (D-10). Do not modify `persona.py` (D-11), `prompts.py`, or `__init__.py`. Do not implement the never-do catalog body (D-13).
  </action>
  <verify>
    <automated>python -c "import sys; from pathlib import Path; sys.path.insert(0,'exercise-ai'); from skills.generation.rules import TRIAD_DOC; assert TRIAD_DOC.name=='TRIAD.md'; assert TRIAD_DOC.is_file(); t=Path('exercise-ai/skills/generation/rules.py').read_text(encoding='utf-8'); assert 'TRIAD_DOC' in t; assert 'Phase 18' in t; assert 'TODO' in t; assert 'TRIAD.md' in t"</automated>
    <fails_when>
      - TRIAD_DOC missing, wrong name, or path does not resolve to an existing TRIAD.md
      - TODO still framed as Phase 17 triad delivery instead of Phase 18 never-do
      - Docstring omits pointer to TRIAD.md / doc-over-code rule
    </fails_when>
  </verify>
  <done>rules.py exposes TRIAD_DOC, docstring points at TRIAD.md, never-do TODOs retargeted to Phase 18 (D-02, D-03, D-14).</done>
</task>

<task type="auto">
  <name>Light verify: headings + pointer + regression smoke (no new pytest)</name>
  <files>exercise-ai/skills/generation/TRIAD.md, exercise-ai/skills/generation/rules.py</files>
  <read_first>
    - exercise-ai/skills/generation/TRIAD.md
    - exercise-ai/skills/generation/rules.py
    - exercise-ai/skills/generation/__init__.py (__all__ must stay get_persona_system only)
    - exercise-ai/tests/test_skills_generation.py
  </read_first>
  <action>
Run light verification only — do not add a new pytest file (D-12). Confirm:
1. TRIAD.md still has the three legs, pipeline, callout identifiers, code-path tokens (`validator`, `reasoning`, `Exercise`), and Fora desta fase.
2. `TRIAD_DOC` imports and `.is_file()` is true from `exercise-ai` on sys.path.
3. `skills.generation.__all__` is still only `get_persona_system` (D-10); confirm `persona.py` and `prompts.py` were not modified in this plan's diff.
4. Reuse Phase 16 smoke: `python -m pytest exercise-ai/tests/test_skills_generation.py -q`.

If any check fails, fix TRIAD.md / rules.py only (still no new tests, no `__init__`/persona/prompts edits).
  </action>
  <verify>
    <automated>python -c "import sys; from pathlib import Path; sys.path.insert(0,'exercise-ai'); from skills.generation.rules import TRIAD_DOC; from skills.generation import get_persona_system; import skills.generation as g; t=TRIAD_DOC.read_text(encoding='utf-8'); assert TRIAD_DOC.is_file(); assert all(x in t for x in ('Validação','Pensamento','Resposta','Fora desta fase','reasoning_effort','tipo_raciocinio','validator','Exercise')); assert g.__all__==['get_persona_system']; assert isinstance(get_persona_system(), str) and get_persona_system().strip()" && python -m pytest exercise-ai/tests/test_skills_generation.py -q</automated>
    <fails_when>
      - TRIAD.md missing required headings/identifiers or TRIAD_DOC not a real file
      - __all__ expanded beyond get_persona_system
      - pytest exercise-ai/tests/test_skills_generation.py regresses
      - A new test_*.py for TRIAD was added (violates D-12)
    </fails_when>
  </verify>
  <done>CONTRACT-02 docs slice verified by file/heading checks + existing Phase 16 smoke; no new pytest (D-10, D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host/lab developer → package docs | Untrusted readers of TRIAD.md / rules.py; docs must not teach LLM self-check as validation |
| Docs → runtime pipeline | Documentation only — no new trust boundary into validators; must not invent a second validation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Spoofing | TRIAD.md Validação leg | low | mitigate | State validation = deterministic code only; forbid framing validation as model self-check |
| T-17-02 | Tampering | Dual prose (rules.py vs TRIAD.md) | low | mitigate | D-02: TRIAD.md authoritative; rules.py thin pointer only |
| T-17-03 | Information Disclosure | never-do TODO foreshadow | low | accept | Keep existing bullets; no secret-logging guidance expansion until Phase 18 |
| T-17-04 | Elevation of Privilege | Process / agent misread | low | mitigate | Audience = API package (D-09); no agent-skill folders; no prompt wiring this phase |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
Phase gate (all must pass):
1. Task 1–3 automated verifies green.
2. `python -m pytest exercise-ai/tests/test_skills_generation.py -q` green (reuse Phase 16 prior verify).
3. Diff touches only `TRIAD.md` + `rules.py` (plus planning SUMMARY) — not `persona.py`, `__init__.py`, `prompts.py`, and no new `test_*.py`.
4. Manual CONTRACT-02 checklist from 17-RESEARCH.md Validation Architecture satisfied.
</verification>

<success_criteria>
- CONTRACT-02 observable: validation / thinking / response separated in authoritative TRIAD.md
- `TRIAD_DOC` discoverable from rules.py; TODO never-do → Phase 18
- Existing skills.generation smoke test still passes; public API unchanged
</success_criteria>

## Source Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Written triad so agents do not invent prompt-side validation or confuse API effort with response fields | 01 | COVERED | Tasks 1–3 |
| REQ | CONTRACT-02 | Docs separate validation vs thinking vs response | 01 | COVERED | TRIAD.md + rules.py pointer |
| RESEARCH | — | Doc-as-source-of-truth + thin Path pointer | 01 | COVERED | Task 2 Pattern 1 |
| RESEARCH | — | Short legs + code pointers + pipeline + callout + Fora desta fase | 01 | COVERED | Task 1 Pattern 2 |
| RESEARCH | — | No new pytest; reuse smoke | 01 | COVERED | Task 3 / D-12 |
| RESEARCH | — | Pitfalls: no prompt validation / no pedagogy≠effort / no schema dump | 01 | COVERED | Task 1 actions |
| CONTEXT | D-01 | Ship rules.py + TRIAD.md under generation/ | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-02 | TRIAD.md authoritative | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-03 | TRIAD_DOC Path + docstring | 01 | COVERED | Task 2 |
| CONTEXT | D-04 | Filename TRIAD.md | 01 | COVERED | Task 1 |
| CONTEXT | D-05 | Short section per leg + pointers | 01 | COVERED | Task 1 |
| CONTEXT | D-06 | Body PT-BR; identifiers English | 01 | COVERED | Task 1 |
| CONTEXT | D-07 | reasoning_effort ≠ tipo_raciocinio callout | 01 | COVERED | Task 1 |
| CONTEXT | D-08 | Pipeline one-liner + AGENTS.md | 01 | COVERED | Task 1 |
| CONTEXT | D-09 | Audience = runtime/API package | 01 | COVERED | Task 1 |
| CONTEXT | D-10 | Do not re-export TRIAD_DOC from __init__ | 01 | COVERED | Tasks 2–3 negative scope |
| CONTEXT | D-11 | Do not modify persona.py | 01 | COVERED | Tasks 1–3 negative scope |
| CONTEXT | D-12 | No new offline pytest | 01 | COVERED | Task 3 |
| CONTEXT | D-13 | Triad docs only; never-do detail → 18 | 01 | COVERED | Tasks 1–2 |
| CONTEXT | D-14 | Retarget TODO never-do → Phase 18 | 01 | COVERED | Task 2 |
| CONTEXT | D-15 | Fora desta fase block | 01 | COVERED | Task 1 |

Deferred (not gaps): DEF-01 loaders; never-do catalog; authority map; prompt wiring; Phases 24–25 policy tests; TRIAD_DOC re-export.

<output>
Create `.planning/phases/17-validation-vs-thinking-vs-response-docs/17-01-SUMMARY.md` when done
</output>
