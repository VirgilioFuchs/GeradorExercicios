---
phase: 03-tests-logging-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exercise-ai/tests/conftest.py
  - exercise-ai/tests/test_validator.py
  - exercise-ai/tests/test_generators.py
  - exercise-ai/tests/test_main.py
  - exercise-ai/tests/test_logging_security.py
  - exercise-ai/main.py
  - exercise-ai/generator.py
  - exercise-ai/generator_gemini.py
  - README.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - LOG-01
  - LOG-02
  - SCAF-04
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "From repo root, `pytest exercise-ai -q` passes with no live LLM network calls"
    - "Validator suite covers valid batch, non-ExerciseBatch/invalid structure, empty exercicios list, wrong quantity, and whitespace/missing fields with path messages"
    - "Parameterized mapper tests cover OpenAI four categories and Gemini status-code table using mocks only"
    - "run_demo with mocked generate: success stdout is parseable JSON only; ValueError yields SystemExit(1) and plain stderr"
    - "LOG-01 events on stderr/logging record generation start, parameters without secrets, success or failure, and validation failure reason"
    - "LOG-02: dummy LLM_API_KEY / GEMINI_API_KEY values never appear in captured stderr from mappers or failure paths; [API:*] logs type + status/code only"
    - "Root README.md documents setup, env vars, run command, pytest command, and stdout JSON vs stderr notes in Portuguese"
  artifacts:
    - path: "exercise-ai/tests/conftest.py"
      provides: "sys.path / import alignment + Pydantic factories for GenerationRequest/ExerciseBatch"
      contains: "sys.path"
    - path: "exercise-ai/tests/test_validator.py"
      provides: "Durable TEST-02 validator cases promoted from Phase 2 verifies"
      contains: "validate_exercise_batch"
    - path: "exercise-ai/tests/test_generators.py"
      provides: "Mocked OpenAI/Gemini mapper + missing-key coverage"
      contains: "map_openai_error"
    - path: "exercise-ai/tests/test_main.py"
      provides: "stdout purity and fail-fast exit contract for run_demo"
      contains: "run_demo"
    - path: "exercise-ai/tests/test_logging_security.py"
      provides: "Anti-leakage asserts for API key substrings on failure paths"
      contains: "LLM_API_KEY"
    - path: "exercise-ai/main.py"
      provides: "LOG-01 stdlib logging events for start/params/success/failure"
      contains: "logging"
    - path: "exercise-ai/generator.py"
      provides: "Sanitized [API:openai] detail logs (type + status/code, no raw str(exc))"
      contains: "map_openai_error"
    - path: "exercise-ai/generator_gemini.py"
      provides: "Sanitized [API:gemini] detail logs (type + status/code)"
      contains: "map_gemini_error"
    - path: "README.md"
      provides: "Portuguese setup, env, run, and pytest docs (SCAF-04)"
      contains: "pytest"
  key_links:
    - from: "exercise-ai/tests/conftest.py"
      to: "exercise-ai/models.py"
      via: "factories build GenerationRequest / Exercise / ExerciseBatch"
      pattern: "ExerciseBatch"
    - from: "exercise-ai/tests/test_validator.py"
      to: "exercise-ai/validator.py"
      via: "validate_exercise_batch under pytest"
      pattern: "validate_exercise_batch"
    - from: "exercise-ai/main.py"
      to: "logging"
      via: "LOG-01 events around generate/validate"
      pattern: "logging"
    - from: "exercise-ai/generator.py"
      to: "stderr [API:openai]"
      via: "map_openai_error redacted detail line"
      pattern: "\\[API:openai\\]"
    - from: "README.md"
      to: "exercise-ai/requirements.txt"
      via: "pip install -r exercise-ai/requirements.txt"
      pattern: "requirements.txt"
---

<objective>
**As a** developer maintaining the exercise generator, **I want to** run durable pytest coverage, safe development logs, and follow a root README, **so that** I can verify quality and onboard without leaking secrets or calling live LLMs.

Purpose: Close Phase 3 quality gates (TEST-*, LOG-*, SCAF-04) and EVAL-REVIEW must-fix remediations (durable suite, secret-leakage eval, mapper/stdout fixtures) per locked decisions D-01: through D-10:.
Output: `exercise-ai/tests/` suite, redacted `[API:*]` logs + LOG-01 events, root `README.md`.

Decision coverage: D-01: tests layout; D-02: factories; D-03: full TEST-02+EVAL suite; D-04: pytest command; D-05: LOG-01 events; D-06: LOG-02 redaction; D-07: no log files/Phoenix; D-08: root README PT; D-09: README sections; D-10: no GitHub Actions.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tests-logging-docs/03-CONTEXT.md
@.planning/phases/02-validation-error-handling/02-EVAL-REVIEW.md
@.planning/phases/02-validation-error-handling/02-REVIEW.md
@.planning/phases/02-validation-error-handling/02-LEARNINGS.md
@.planning/phases/02-validation-error-handling/02-01-PLAN.md
@AGENTS.md
@exercise-ai/validator.py
@exercise-ai/main.py
@exercise-ai/generator.py
@exercise-ai/generator_gemini.py
@exercise-ai/models.py
@exercise-ai/requirements.txt
</context>

<proven_verify_commands>
**Reuse Phase 2 path grounding from repo root. Prefer pytest once the suite exists; do not invent alternate package roots.**

- Phase 02 · Task 1: `python -c` with `sys.path.insert(0, 'exercise-ai')` + `validate_exercise_batch` asserts (promote into `test_validator.py`)
- Phase 02 · Task 2: mocked `map_openai_error` / `map_gemini_error` + `main.run_demo` stdout purity (promote into `test_generators.py` / `test_main.py`)
- Canonical Phase 3 command (D-04): `pytest exercise-ai -q`
</proven_verify_commands>

## Source coverage audit

| SOURCE | ID | Item | Plan | Status | Notes |
|--------|-----|------|------|--------|-------|
| GOAL | — | Cobertura de testes, observabilidade básica, docs de uso | 03-01 | COVERED | User-story framed in objective |
| REQ | TEST-01 | Unit tests of validator without live LLM | 03-01 T1–T2 | COVERED | |
| REQ | TEST-02 | valid / invalid / missing key / missing field / wrong qty / empty list | 03-01 T1–T2 | COVERED | |
| REQ | LOG-01 | Logs: start, params (no secrets), success/fail, validation reason | 03-01 T2 | COVERED | |
| REQ | LOG-02 | API keys never in logs | 03-01 T2 | COVERED | WR-01 redaction |
| REQ | SCAF-04 | README setup + run | 03-01 T3 | COVERED | |
| RESEARCH | — | (none — research_enabled false) | — | N/A | Plan from CONTEXT + prior artifacts |
| CONTEXT | D-01 | tests under `exercise-ai/tests/` + conftest path | 03-01 T1 | COVERED | |
| CONTEXT | D-02 | Pydantic factories in fixtures | 03-01 T1 | COVERED | |
| CONTEXT | D-03 | TEST-02 + EVAL remediations (mappers, stdout, anti-leak) | 03-01 T1–T2 | COVERED | costly |
| CONTEXT | D-04 | Canonical `pytest exercise-ai -q` | 03-01 T1–T3 | COVERED | |
| CONTEXT | D-05 | LOG-01 via logging/helper on stderr | 03-01 T2 | COVERED | |
| CONTEXT | D-06 | Never log key values; sanitize mapper detail | 03-01 T2 | COVERED | costly |
| CONTEXT | D-07 | No persistent log files / Phoenix | 03-01 | COVERED | explicit non-goal |
| CONTEXT | D-08 | Root README.md PT | 03-01 T3 | COVERED | |
| CONTEXT | D-09 | README sections: setup, env, run, pytest, stdout/stderr | 03-01 T3 | COVERED | |
| CONTEXT | D-10 | No GitHub Actions this phase | 03-01 T3 | COVERED | document pytest only |

Deferred (not planned): CI Actions, Phoenix, RELY-*, argparse, MATH-01, WR-03/WR-04.

<tasks>

<task type="tracer" reversibility="costly">
  <name>Task 1: Tracer — pytest harness + core TEST-02 validator path</name>
  <reversibility rating="costly">D-03: durable regression suite layout becomes the MVP quality baseline; moving paths later churns imports and README.</reversibility>
  <files>exercise-ai/tests/conftest.py, exercise-ai/tests/test_validator.py</files>
  <read_first>
    .planning/phases/03-tests-logging-docs/03-CONTEXT.md,
    .planning/phases/02-validation-error-handling/02-01-PLAN.md,
    .planning/phases/02-validation-error-handling/02-EVAL-REVIEW.md,
    exercise-ai/validator.py,
    exercise-ai/models.py,
    exercise-ai/requirements.txt
  </read_first>
  <action>
    Deliver one end-to-end durable test path: from repo root, pytest discovers and runs validator cases with no live LLM calls (TEST-01; D-01:; D-02:; D-04:).

    1. Create `exercise-ai/tests/conftest.py` per D-01: and D-02:
       - Insert `exercise-ai/` onto `sys.path` (same grounding as Phase 2: path relative to repo so imports of `validator`, `models` work).
       - Provide pytest fixtures/factories building `GenerationRequest`, `Exercise`, and `ExerciseBatch` in code (Pydantic). Optional `tests/fixtures/` JSON only if it clearly improves readability — prefer factories.
       - Do not call OpenAI/Gemini SDKs or the network.

    2. Create `exercise-ai/tests/test_validator.py` promoting Phase 2 Task 1 asserts into durable tests (TEST-02; D-03: core slice):
       - Valid batch returns the same instance / passes.
       - Non-`ExerciseBatch` / invalid structure → `ValueError`; user message without `[VALIDAÇÃO]` prefix; stderr has `[VALIDAÇÃO]`.
       - Empty `exercicios` list → failure mentioning empty/lista.
       - Wrong quantity → message includes expected vs actual (`esperado` / `recebido`).
       - Whitespace-only field → path form `exercicios[i].campo`.
       - Optionally assert `VALIDATION_REPORT_MODE` `all` vs `first_exercise` if cheap to include (still no LLM).

    3. Do not add README, LOG-01 wiring, or mapper redaction in this task — expansion owns those. Do not add GitHub Actions (D-10: deferred).

    Discretion: exact test function names; keep files under `exercise-ai/tests/`.
  </action>
  <verify>
    <automated>pytest exercise-ai/tests/test_validator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `exercise-ai/tests/conftest.py` exists and aligns imports to the local package (D-01:)
    - Factories/fixtures build Pydantic models in code (D-02:)
    - `pytest exercise-ai/tests/test_validator.py -q` exits 0 covering valid, invalid structure, empty list, wrong quantity, whitespace field (TEST-02 core; D-03:)
    - Tests use static/fixture data only — no live LLM calls (TEST-01)
  </acceptance_criteria>
  <done>Runnable pytest validator suite passes from repo root with conftest path grounding</done>
</task>

<task type="auto" reversibility="costly">
  <name>Task 2: Expand — LOG-01 events, LOG-02 redaction, full EVAL suite</name>
  <reversibility rating="costly">D-06: sanitized [API:*] log contract is a security baseline; changing it later rewrites every consumer of stderr detail.</reversibility>
  <files>exercise-ai/main.py, exercise-ai/generator.py, exercise-ai/generator_gemini.py, exercise-ai/tests/test_generators.py, exercise-ai/tests/test_main.py, exercise-ai/tests/test_logging_security.py</files>
  <read_first>
    .planning/phases/03-tests-logging-docs/03-CONTEXT.md,
    .planning/phases/02-validation-error-handling/02-REVIEW.md,
    .planning/phases/02-validation-error-handling/02-EVAL-REVIEW.md,
    .planning/phases/02-validation-error-handling/02-LEARNINGS.md,
    exercise-ai/main.py,
    exercise-ai/generator.py,
    exercise-ai/generator_gemini.py,
    exercise-ai/tests/conftest.py,
    exercise-ai/tests/test_validator.py
  </read_first>
  <action>
    Expand the tracer into full D-03: remediations plus LOG-01 / LOG-02. No CI, Phoenix, persistent log files, retry, argparse, or MATH-01 (deferred).

    **A. LOG-01 in `main.py` per D-05:**
    1. Configure stdlib `logging` (or a thin helper) so development events go to stderr.
    2. Emit events for: generation start; parameters (`materia`, `topico`, `dificuldade`, `quantidade`) with **no** env key values; success after validation; failure (including validation reason / exception text already plain PT).
    3. Keep existing stage labels and two-layer `[VALIDAÇÃO]` / `[API:*]` behavior. Keep success stdout as JSON-only.
    4. Do not write log files to disk (D-07:).

    **B. LOG-02 redaction — fix WR-01 (and related WR-02) per D-06:**
    1. In `map_openai_error` and `map_gemini_error`: detail stderr lines must log exception **type** plus `status_code`/`code` when present — **do not** interpolate raw `str(exc)` into `[API:*]` lines (WR-01).
    2. OpenAI generic fallback user `RuntimeError` must be plain PT without embedding raw `exc` (align with Gemini fixed generic message; closes WR-02 leakage surface).
    3. Never log values of `LLM_API_KEY` or `GEMINI_API_KEY`. Sanitizing other debug dumps (e.g. unparseable response text) must not print env key values.
    4. Do not implement WR-03/WR-04 unless trivial leftover — CONTEXT defers them.

    **C. Durable tests (D-03: remainder / EVAL must-fix)**
    1. `test_generators.py`: missing dual-key `ValueError`; parameterized OpenAI timeout/rate/connection/auth → distinct PT messages + `[API:openai]` present; Gemini status-code table exact messages + `[API:gemini]`. Use SDK-tolerant exception construction (Phase 2 LEARNINGS: catch broader than TypeError when building samples). Mocks only.
    2. `test_main.py`: `run_demo` with mocked `generate_exercises` — success stdout `json.loads` only, stage labels on stderr, generate once; `ValueError` → `SystemExit(1)`, empty stdout, plain stderr (no wrapper prefixes).
    3. `test_logging_security.py`: set distinctive dummy key strings in env; force mapper auth/failure and any relevant refuse/unparseable paths; capture stderr; assert dummy key substrings never appear (LOG-02 / EVAL blocker).
    4. Ensure TEST-02 "chave ausente" is covered via generator missing-key test (not live API).

    Discretion: helper module vs inline `logging.getLogger`; test file names as listed unless a clearer split emerges without adding packages.
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite `pytest exercise-ai -q` passes with mocks/static data only (TEST-01, TEST-02; D-03:; D-04:)
    - Mapper and run_demo cases promoted from Phase 2 Task 2 verifies into durable modules
    - LOG-01 events present for start, params without secrets, success/failure, validation reason (D-05:)
    - After redaction, `[API:openai]` / `[API:gemini]` detail lines do not embed raw exception bodies that can carry keys; dummy key strings absent from stderr in security tests (D-06:; LOG-02)
    - No log files on disk; no Phoenix; no GitHub Actions (D-07:; D-10:)
  </acceptance_criteria>
  <done>Logging events + sanitized API detail logs shipped; full pytest suite green without live LLM</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: Expand — root README (SCAF-04)</name>
  <reversibility rating="reversible">D-08/D-09: docs-only; easy to rewrite.</reversibility>
  <files>README.md</files>
  <read_first>
    .planning/phases/03-tests-logging-docs/03-CONTEXT.md,
    .planning/REQUIREMENTS.md,
    exercise-ai/requirements.txt,
    exercise-ai/.env.example,
    README.md
  </read_first>
  <action>
    Create root `README.md` in Portuguese (D-08:; D-09:; SCAF-04).

    Required sections:
    1. What the project is (short).
    2. Setup: `pip install -r exercise-ai/requirements.txt`.
    3. Env vars: `LLM_API_KEY`, `GEMINI_API_KEY`, `LLM_PROVIDER` (point to `.env` / `.env.example`; never paste real secrets).
    4. How to run: `python exercise-ai/main.py`.
    5. How to test: canonical `pytest exercise-ai -q` (D-04:); note that tests must not call live LLMs.
    6. Note: success JSON on stdout; stage labels / validation / API detail logs on stderr.

    Per D-10: document the pytest command only — do **not** add `.github/workflows` or CI config.
    Discretion: optional minimal JSON output example.
  </action>
  <verify>
    <automated>pytest exercise-ai -q</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root in Portuguese (D-08:; SCAF-04)
    - Documents pip install path, env var names, `python exercise-ai/main.py`, `pytest exercise-ai -q`, and stdout vs stderr (D-09:; D-04:)
    - No GitHub Actions added (D-10:)
    - Suite still green: `pytest exercise-ai -q`
  </acceptance_criteria>
  <done>Root README documents setup, env, run, and pytest; suite remains green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | API keys enter via `.env` / environment; must never cross into stdout JSON or durable logs |
| LLM provider → generators | Untrusted exception bodies / response text may embed key fragments |
| CLI stderr → developer | Detail logs (`[API:*]`, `[VALIDAÇÃO]`) are trusted for ops but must not contain secrets |
| test suite → network | Tests must stay offline (mocks/static only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `map_openai_error` / `map_gemini_error` `[API:*]` lines | high | mitigate | Log type + status/code only; never raw `str(exc)`; pytest anti-leakage with dummy keys (D-06, Task 2) |
| T-03-02 | Information Disclosure | LOG-01 parameter / failure logs in `main.py` | medium | mitigate | Log request fields only; never env key values (D-05, D-06) |
| T-03-03 | Information Disclosure | README / docs | low | mitigate | Document var names only; no sample secrets (D-09) |
| T-03-04 | Tampering | pytest suite vs live API | medium | mitigate | Mocks/static fixtures only; no network in tests (TEST-01) |
| T-03-SC | Tampering | pip installs | low | accept | No new packages this phase; `pytest` already in requirements.txt |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `pytest exercise-ai -q` passes from repo root
- [ ] TEST-02 cases present (valid, invalid, missing key, missing/whitespace field, wrong quantity, empty list)
- [ ] Mapper + run_demo + secret-leakage tests exist and pass without live LLM
- [ ] LOG-01 events observable on generation paths; LOG-02 redaction verified by tests
- [ ] Root README covers setup, env, run, pytest, stdout/stderr
- [ ] No GitHub Actions, Phoenix, log files, RELY retry, argparse, or MATH-01 added
</verification>

<success_criteria>
- Roadmap Phase 3 success criteria 1–4 satisfied
- Requirements TEST-01, TEST-02, LOG-01, LOG-02, SCAF-04 satisfied
- Locked decisions D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: D-10: implemented; deferred items explicitly not built
- EVAL-REVIEW must-fix items (durable suite, fixtures/factories, secret-leakage eval, mapper/stdout coverage) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-tests-logging-docs/03-01-SUMMARY.md`
</output>
