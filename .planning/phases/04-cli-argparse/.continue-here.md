---
context: phase
phase: 04-cli-argparse
task: 1
total_tasks: 3
status: planned_ready_to_execute
last_updated: 2026-09-04T15:05:54.370Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| JSON-only stdout regression | ROADMAP success criterion #1 still says JSON on stdout; CONTEXT D-14 supersedes it | blocking | Executor must implement text stdout + required `--out`; Task 3 updates ROADMAP criteria |
| Skip D-14 checkpoint | One-way contract break; plan has `gate=blocking-human` | blocking | Task 1 must get `proceed-d14` before tracer edits |
| Prompt edits in Phase 4 | D-16 forbids prompt changes | advisory | Do not touch `prompts.py`; reuse `generate_exercises` |

<current_state>
Phase 4 (CLI argparse) is **planned and checker-passed**. Milestone v1.1. Research skipped by user. Execution not started. User explicitly requested no auto-execute after plan.
</current_state>

<completed_work>

Session completed:
- Discuss-phase 4 → `04-CONTEXT.md` + `04-DISCUSSION-LOG.md` (D-01..D-20)
- Plan-phase 4 (no research) → `04-01-PLAN.md` + `COVERAGE.md`
- Plan-checker → **VERIFICATION PASSED** (0 blockers, 5 warnings)
- STATE marked planned (`docs(04): mark phase planned in STATE`)
</completed_work>

<remaining_work>

- Task 1: Human checkpoint for D-14 (`proceed-d14` vs `abort-revisit`) — **blocking-human**
- Task 2: Tracer — argparse, `run(request)`, text stdout, required `--out`
- Task 3: Expansion — qty ceiling, `--provider`, PT help/errors, README, ROADMAP criteria fix
- Later: Phases 5 (RELY) and 6 (MATH) still unplanned
</remaining_work>

<decisions_made>

- Flags PT long-only; demo defaults; `--out` required; stdout = D-15 text layout
- `--provider openai|gemini`; max quantidade 40; help/errors PT
- `run_demo` → `run(request)`; prompt unchanged
- Skip research for this plan run
</decisions_made>

<blockers>
- None technical. Human must confirm D-14 at execute Task 1.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-cli-argparse/04-CONTEXT.md` — locked D-01..D-20
2. `.planning/phases/04-cli-argparse/04-01-PLAN.md` — executable tasks
3. `.planning/HANDOFF.json` — machine resume state
4. `exercise-ai/main.py`, `models.py`, `tests/test_main.py`, `README.md`

## Infrastructure State
- Branch: `gsd/ship-phase-03-tests-logging-docs` (planning commits ahead of origin)
- No async jobs / servers
- Live LLM not required for planned pytest path (mocks)

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/04-cli-argparse/04-01-PLAN.md`
- Critique focus: D-14 one-way door accepted? PT argparse error mechanism named? `--provider` → `_resolve_provider` wiring explicit?
- Gate: Do NOT begin Task 2 until Task 1 checkpoint returns `proceed-d14`

<context>
Planner produced one coarse wave-1 plan. Checker warnings (optional): provider key_link, PT ArgumentParser.error() mechanism, estimate confidence low, VALIDATION.md N/A. User asked plan without auto, then pause + push.
</context>

<next_action>
Start with: `$gsd-execute-phase 4` — stop at Task 1 D-14 checkpoint; only after `proceed-d14`, implement tracer (Task 2).
</next_action>
