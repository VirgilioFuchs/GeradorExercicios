# API Coverage — Provider Failover

> Full coverage by default. Opt-outs are explicit, reasoned decisions.

No external API integration: Phase 8 only adds a failover envelope over existing OpenAI/Gemini SDK calls already in the lab; no new endpoints, SDKs, or Admin APIs.
